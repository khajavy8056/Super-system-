# Release Audit — v4.5.0 (2026-09-24)

**موضوع دوره:** سه مشکل گزارش‌شدهٔ مدیر — خطای ساخت نصب‌کنندهٔ ویندوز، محیط چت اندروید، و حضور مدل در بخش هوشمندی.

## ۱) ویندوز — چرا بیلد FAIL می‌شد و چرا حالا پاس می‌شود

لاگ مدیر: بعد از دانلود کامل ۱۴.۱ مگابایتیِ آرشیو رسمی (sha256 `4a0ec902…`) و استخراج، تأییدگر این سه خطا را داد:

| DLL | چیست | چرا نباید باندل شود | حکم |
|---|---|---|---|
| `wldap32.dll` | WinLDAP — جزء System32 ویندوز | import آن از `libcurl-x64.dll` رسمیِ داخل آرشیو است | system |
| `psapi.dll` | Process Status API — جزء System32 | import آن از خود موتور | system |
| `msvcp140_codecvt_ids.dll` | عضو خانوادهٔ VC++ 2015+ redist | `msvcp140.dll` آن را delay-load می‌کند (مسیر codecvt که llama.cpp استفاده نمی‌کند)؛ همان خانوادهٔ msvcp140/vcruntime140 که از v4.3.3 مجاز بودند | redist |

- فهرست `SYSTEM_DLLS` در `scripts/model/verify_engine.py` با همین سه مورد + بقیهٔ خانوادهٔ VC++ (`msvcp140_2`, `msvcp140_atomic_wait`, `concrt140`, `vcomp140`) + `ncrypt`/`dnsapi`/`wintrust` کامل شد.
- **سخت‌گیری حفظ شد:** DLLهای خود llama.cpp (`llama.dll`, `ggml-*.dll`, `libcurl-x64.dll`) هنوز باید داخل نصب‌کننده باشند وگرنه بیلد FAIL — تست مستقل.
- سناریوی دقیق لاگ مالک با PEهای سنتزی بازتولید شد: قبل از فیکس همان سه FAIL، بعد از فیکس **PASS**.
- **تور ایمنی اجرا:** `runtime.py` اگر ویندز VC++ redist نداشت، پیام فارسی + لینک رسمی `aka.ms/vs/17/release/vc_redist.x64.exe` می‌دهد.
- روی دستگاه مدیر: دانلود نیمه‌کاره سر جای خود است؛ فقط `ساخت-فایل-نصب.bat` را دوباره اجرا کنید.

## ۲) اندروید — محیط چت

**باگ ریشه‌ای:** صفحهٔ چت `view()` اختصاصی دارد ولی کلاس پایه خطاها را به `body` (کانتینری که هرگز به درخت نمایش وصل نیست) می‌فرستاد → خطای خواندن تاریخچه (PC قطع/403) صفحه را برای همیشه روی «در حال خواندن گفت‌وگو…» قفل می‌کرد.

بازسازی:
- نوار سازندهٔ همیشه‌نمایان: ورودی گرد + «بفرست» اصلی + میکروفون (طلایی هنگام شنیدن) + کلید ارسال کیبورد.
- سربرگ با وضعیت زندهٔ «چه کسی پاسخ می‌دهد» + کلید حالت صوتی.
- حالت خوش‌آمد با ۴ پیشنهاد لمسی (وضعیت امروز / انقضاها / یادآوری فردا / متن پیامک).
- مجوز میکروفون: بعد از گرنت، شنیدن خودش شروع می‌شود (کد ۷۸ → permCb).
- دکمهٔ اصلی «💬 گفت‌وگو با مغز فروشگاه» در صفحهٔ مغز (هر دو حالت).
- دکمه‌های تأیید/ویرایش/رد و چیپ یادآوری v4.4.0 دست‌نخورده.

## ۳) مدل در بخش هوشمندی

- `GET /api/brain/briefing` — روایت مدل محلی روی نتایج ممیزی‌شده (کارت‌های پرواکتیو، تصمیم‌های باز، یادآوری‌ها) با دروازهٔ عدد؛ fallback قطعی؛ کش ۱۵ دقیقه‌ای در `brain.briefing`.
- ورکر مغز هر پاس، بریفینگ را refresh می‌کند.
- اندروید: کارت «تحلیل مغز فروشگاه» + بج «زنده» + «گفت‌وگو دربارهٔ این تحلیل» بالای هوش فروشگاه.
- پنل وب: همان کارت + دکمهٔ به‌روزرسانی (`/brain/briefing?refresh=1`).
- برای کاربر عملیاتی (بدون مجوز مغز) کارت بی‌صدا مخفی می‌ماند — بخش هوشمندی مثل قبل کار می‌کند.

## صحت‌سنجی

- **مجموعهٔ کامل: ۸۰۲ passed / 1 skipped** (۴.۴.۰ → ۷۸۸/۱).
- جدید: `tests/test_v45_round13.py` (۱۴ تست).
- به‌روزرسانی تست‌های قدیمی: برچسب میکروفون در `test_v43_voice` (گفتار → 🎤 با حالت طلایی).
- `node --check frontend/insights.js` پس از تغییر ✓.

## APK 4.5.0

| فیلد | مقدار |
|---|---|
| فایل | `releases/android/SupermarketMobile-4.5.0.apk` |
| حجم | ۹٬۶۸۰٬۴۷۹ بایت (~۹.۳M) |
| versionCode / Name | **40500 / 4.5.0** (badging تأیید شد) |
| ABIها | arm64-v8a + armeabi-v7a (موتور llama.cpp static) |
| گواهی | همان گواهی همهٔ نسخه‌ها (`18b4ab18…`) → به‌روزرسانی درجا |
| preflight | PASS |
| sha256 | `ebcf8c8ae261a37ccdc47ad3e177ca0988ccdc9a8994a130689a2293529a83b1` |
| idsig | کنار APK |

## ویندوز

`setup.iss` → 4.5.0. بدون مهاجرت جدید این دوره (هد همان `b7e9f1a3c5d8`). موتور/مدل/برنامه بدون تغییر — فقط اصلاح تأییدگر و نکتهٔ vc_redist.
