# RELEASE AUDIT — 4.0.0

**نسخه:** 4.0.0 · **منبع یکتای نسخه:** `backend/app/__init__.py::__version__`
**شاخه:** `arena/01a0ca3c-super-system` · **پایه:** `c7079af` (3.8.0)
**تاریخ:** 2026-09-22

> این سند فقط چیزهایی را می‌گوید که **اجرا و دیده** شده‌اند. هر موردی که در این محیط قابل ساخت نبود،
> با همان صراحت و با مسیر فعال‌سازی ثبت شده است.

---

## ۱. آنچه ساخته و اثبات شد

### ۱.۱ مغز کسب‌وکار
- سرویس جدید `backend/app/services/business_brain/` با ۲۶ ماژول (فهرست در `docs/AI_BRAIN_ARCHITECTURE.md`).
- ۶ جدول جدید (`brain_messages`, `brain_decisions`, `brain_followups`, `brain_policies`,
  `brain_model_installs`, `brain_memory_facts`) در migration `d4f6a8b1c2e3` (head جدید، prev `c5d9e2f7a4b6`)،
  idempotent و بدون حذف داده.
- ۳۷ ابزار در Tool Registry + ۵ ابزار اقدامی (`ACTION_TOOLS`) — `docs/AI_TOOL_REGISTRY.md`.
- ۸ متخصص که فقط `Evidence` می‌دهند؛ تشخیص تضاد فعال.
- ۲۴ مسیر `/api/brain/*` با گارد مدیر؛ ۳۲ روتر قبلی دست‌نخورده.

### ۱.۲ تست‌ها (اجرای واقعی)

| مجموعه | نتیجه |
|---|---|
| `tests/test_v40_brain.py` | **16 passed** |
| `tests/test_v40_model_manager.py` | **17 passed** |
| `tests/test_v40_security.py` | **12 passed** |
| `tests/test_v40_proactive.py` | **10 passed** |
| `test_v40_api.py` | **3 passed** (مسیر HTTP واقعی + persist در سشن تازه) |
| `test_v40_model_seed.py` | **7 passed** (الحاق مدلِ نصب‌کننده: تأیید، idempotent، خرابی، صف بدون دانلود مجدد) |
| **جمع v4.0** | **65 passed** |
| **کل مجموعه روی v4.0.0 (پس از مدل/اندروید)** | **۶۶۰ passed, 1 skipped (۳۹۵ ثانیه)** |
| کل مجموعه، پیش از رفع باگ‌های اجرای زنده | ۶۴۸ passed, 1 skipped (۳۳۱ ثانیه) |
| پایهٔ v3.8.0 (پیش از تغییرات) | 593 passed, 1 skipped (۳۳۸ ثانیه) |

### ۱.۳ سنجش روی فروشگاه نمونه (سناریوی ۱ بریف)
- صندوق ۱۸۶M؛ چک فردا ۱۲۰M؛ چک +۳ روز ۸۰M؛ مطالبات ۱۵۴M، وصول انتظاری ۴۹٫۷M (۴۶٫۲M در ۳ روز).
- پیش‌بینی: کمترین موجودی **۳۲٫۲M**، **۱۲ روز** زیر کف امن ۴۰M، بدون وصول **−۱۴M**.
- فلگ‌ها: `CASH_GAP_AHEAD`, `CASH_FRAGILE`, `EXPIRY_RISK`.
- تصمیم‌ها: «فشار نقدینگی» (chase_receivable +۷٫۸M | negotiate_supplier_terms +۱٫۳۶۵M)،
  «مدیریت چک سررسید»، «وضعیت فروشگاه».
- فروشگاه خالی → `NO_DATA_TEXT`، صفر تصمیم، ناظرها ساکت.

### ۱.۴ امنیت
- ۲۴ مسیر: ناشناس 401، صندوق‌دار 403، هیچ 404.
- ابزار مالی/سیاست برای صندوق‌دار `ToolDenied` + ثبت در `ctx.trace` + `known_numbers()` خالی.
- بدون تأیید: `execution.executions == []` (هیچ اجرایی رخ نمی‌دهد).
- `search_web` فقط خواندن؛ ابزارهای نوشتنی دقیقاً ۵ مورد کم‌خطر.

### ۱.۵ مدل
- رجیستری: `qwen2.5-1.5b-instruct-q4_k_m` (پیش‌فرض، ۱٫۰۴ گیگابایت) و `q3_k_m` (۴ گیگابایت رم).
- sha256 هر دو از مخزن رسمی Qwen پین شده؛ سقف ۲ گیگابایت تست‌شده; Q8 و Qwen3-4B ممنوع.
- `scripts/model/fetch_model.py` ساخته و اجرا شد (`--list` روی همین دستگاه: انتخاب Q3_K_M درست).
- چرخهٔ download → verify → install → benchmark → activate با rollback.

---

## ۲. انحراف‌های ثبت‌شده (شفاف، بدون لاپوشانی)

### ۲.۱ مدل: Qwen2.5-1.5B-Instruct به‌جای Qwen3-1.7B Q4_K_M
بریف «Qwen3-1.7B GGUF Q4_K_M (~۱٫۲۸ گیگابایت)» را خواسته بود. بررسی مستقیم API هوگینگ‌فیس نشان داد
مخزن رسمی `Qwen/Qwen3-1.7B-GGUF` **فقط `Qwen3-1.7B-Q8_0.gguf`** را منتشر کرده است (۱٫۸۳ گیگابایت) و
هیچ فایل Q4_K_M/Q3_K_M ندارد. چون Q8 ممنوع است و دانلود از آینهٔ غیررسمی هم ممنوع، دو انتخاب مانده بود:
(الف) نقض یکی از دو قاعده، (ب) انتخاب مدل رسمی سبک دیگر. گزینهٔ (ب) انتخاب شد:
**Qwen2.5-1.5B-Instruct-GGUF** (مخزن رسمی Qwen، Q4_K_M = ۱٬۱۱۷٬۳۲۰٬۷۳۶ بایت).
شناسه‌های ممنوع با دلیل در `model_registry.FORBIDDEN` ثبت شده‌اند تا اشتباهی برنگردند.
**تأیید مالک:** پذیرفته شد (۲۰۲۶-۰۹-۲۳) — پیش‌فرض همان Qwen2.5-1.5B-Instruct رسمی می‌ماند.

### ۲.۲ Windows Setup.exe — ماشین‌آلات «مدل داخل نصب‌کننده» کامل شد؛ خود EXE همچنان فقط روی ویندوز واقعی
خواستهٔ مالک (۲۰۲۶-۰۹-۲۳): «فایل نصبی ویندوز که مدل مورد نیاز در همین‌جا دانلود و در فایل نصبی تولیدشده
الحاق شده». آنچه ساخته شد:
- `scripts/model/prepare_windows_installer.py` — مدل رسمی را می‌گیرد، sha256 را با رجیستری تطبیق می‌دهد،
  payload + `seed.json` و قطعهٔ `model_payload.iss` را می‌سازد؛ `--engine` موتور llama.cpp ویندوز را هم
  از ریلیز رسمی GitHub اضافه می‌کند. عدم تطابق هش = حذف فایل + شکست بیلد (مدل تأییدنشده هرگز وارد
  Setup.exe نمی‌شود). برای بیلد آفلاین: `--from-file`.
- `setup.iss` payload را در `%USERPROFILE%\SupermarketSystem\brain\models` نصب می‌کند (دادهٔ کاربر،
  نه پوشهٔ برنامه) و `run_supermarket.py` حالا `SUPERMARKET_DATA_DIR` را همان‌جا ست می‌کند تا نسخهٔ
  frozen مدل گم نشود. در اولین اجرا `ModelManager.adopt_preinstalled()` فایل را **دوباره هش می‌کند** و
  فقط در صورت تطابق با manifest و رجیستری نصب/فعال می‌کند (تست‌شده در `test_v40_model_seed.py`، ۷ تست).
- `BUILD-SETUP.bat`/`builder-lib.ps1` مرحلهٔ مدل را قبل از ISCC اجرا می‌کنند و بدون مدلِ تأییدشده،
  Setup.exe نمی‌سازند (نسخهٔ قابل‌حمل می‌سازد و صادقانه می‌گوید). workflow آماده (`installer/ci/`) هم
  همین مرحله را دارد و CI بدون مدلِ تأییدشده fail می‌شود.
- **محدودیت صادقانهٔ این محیط:** اینجا لینوکس است؛ PyInstaller کراس‌کامپایل نمی‌کند و Inno Setup فقط
  ویندوزی است، پس خود فایل Setup.exe (با مدل الحاق‌شده) باید روی ویندوز واقعی ساخته شود: یا runner
  `windows-latest` (نیازمند فعال‌سازی workflow با `scripts/activate-ci.sh` — توکن فعلی مجوز `workflow`
  ندارد؛ دوباره تست شد و همان ۴۰۳ برگشت) یا `BUILD-SETUP.bat` روی ویندوز مالک. هیچ EXE جعلی ساخته نشد.

### ۲.۳ Android APK — ✅ در این محیط واقعاً ساخته شد (به‌روزرسانی ۲۰۲۶-۰۹-۲۳)
با خط لولهٔ بدون-Gradle خود مخزن (`scripts/android/fetch-tools.sh` + `build-apk.sh`؛ ابزارها فقط از
PyPI/npm/GitHub) نسخهٔ ۴٫۰٫۰ واقعاً کامپایل، بسته‌بندی و امضا شد:

- `releases/android/SupermarketMobile-4.0.0.apk` — ۱٬۳۴۸٬۹۴۰ بایت، versionCode **40000**،
  sha256 `992684569918c4cc9d6a55691011e60a75dd52b264bc0286ca0d30805e68158e` (+ فایل `.sha256` و `.idsig`).
- **آزمون ارتقا (آنچه در این محیط قابل اثبات است):** نام پکیج یکسان (`ir.khajavy.supermarket`)،
  versionCode 40000 > 30800، و گواهی امضا **بیت‌به‌بیت یکسان با 3.8.0**
  (SHA-256: `18b4ab1868c026476c4027fe823d38a8e3965f70194cc0d65e60eac4e7321fa5`) — یعنی اندروید آن را
  به‌عنوان به‌روزرسانی روی نسخهٔ قبلی نصب می‌کند، نه نصب جدید. تست روی گوشی فیزیکی (نصب روی 3.8.0 و
  باز کردن «مغز فروشگاه») همچنان روی دستگاه واقعی باید انجام شود؛ شبیه‌ساز/SDK رسمی در این محیط نیست.
- **مدل مطابق خواستهٔ مالک داخل APK نیست؛** پس از نصب از تب «مدل محلی» دانلود می‌شود (منبع رسمی Qwen،
  هش SHA-256 پین‌شده، دریافت قابل‌ادامه، تأیید هش روی خود گوشی، حذف خودکار فایل ناهمخوان).
- کد جدید: `BrainModel.java` (مدیریت مدل محلی) و `BrainScreens.java` (مرکز تصمیم فقط-مدیر، گفت‌وگو،
  پیگیری‌ها، مدل) + ثبت در `Screens`/`AppActivity`. صف آفلاین با ثبت تعارض (هرگز بازنویسی بی‌صدا).

### ۲.۴ Vision
فقط اینترفیس (`VisionProvider`/`VisionAgent`). عمداً هیچ پیاده‌سازی و هیچ تصمیم مالی از تصویر وجود ندارد.

---

## ۳. چه چیزی تغییر **نکرد**

هیچ فیچر موجودی حذف یا بازنویسی نشد: POS، حسابداری، انبار، مشتریان، تأمین‌کنندگان، قیمت‌گذاری، پیامک،
سخت‌افزار، sync، گزارش‌ها، auth/RBAC، پشتیبان‌گیری، آزمایش‌ها، بینش‌ها، Action Engine.
تحلیل‌گرها **به‌عنوان ابزار** وصل شدند (`run_existing_analyzer`) و Action Engine فقط با یک تابع سازگاری
(`execute_actions`/`ActionOwner`/`_ref`) استفاده شد، بدون تغییر منطق تراکنش.

---

## ۴. چک‌لیست انتشار — وضعیت واقعی

| مورد | وضعیت |
|---|---|
| همهٔ منابع نسخه روی 4.0.0 | ✅ `__init__.py` + `setup.iss` (Android از همان می‌خواند) |
| تست‌های قدیمی سبز | ✅ کل مجموعه روی همین درخت کد سبز است (اجرای کامل v4.1.0: ۶۹۸ passed, 1 skipped) |
| تست‌های جدید سبز | ✅ ۱۰۵ passed (۶۷ در v4.0 + ۲۲ در v4.0.1 + ۱۶ در v4.1 — پاریتی اندروید ۷ + اسکریپت‌های نصب‌کننده ۹) |
| مستندات v4.0 | ✅ ۷ سند + CHANGELOG + README |
| بدون فایل موقت/مدل در گیت | ✅ |
| tag/GitHub Release | ⛔ تصمیم مالک: تا ساخته‌شدن بیلد ویندوز و APK واقعی، تگ زده نمی‌شود |
| APK واقعی امضاشده | ✅ `SupermarketMobile-4.1.0.apk` (با موتور llama.cpp داخل APK) — ساخته، امضاشده (همان کلید 3.8.0)، checksum شده؛ آزمون روی گوشی فیزیکی باقی است |
| Setup.exe ویندوز (با مدل الحاق‌شده) | ⛔ فقط روی ویندوز واقعی — ماشین‌آلات کامل (اسکریپت + iss + builder + CI) آماده و تست‌شده |

### ۲.۵ سه باگ واقعی که فقط اجرای زندهٔ محصول پیدا کرد

تست‌های واحد سبز بودند، اما سرور واقعی روی یک فروشگاه واقعی سه چیز را نشان داد که هیچ تستی ندیده بود.
هر سه رفع شد و برای دومی یک فایل تست اختصاصی نوشته شد:

1. **`GET /api/brain/decisions` با ۵۰۰ می‌افتاد** وقتی اولین تصمیم واقعی روی میز بود —
   `list_decisions` دیکشنری برمی‌گرداند و لایهٔ façade دوباره `to_dict` صدا می‌زد
   (`'dict' object has no attribute 'options'`). روی فروشگاه خالی (فهرست خالی) هیچ خطایی دیده نمی‌شد.
   رفع: `list_decisions` ردیف‌های ORM برمی‌گرداند و `offset` هم (که روتر می‌پذیرفت اما نادیده گرفته می‌شد)
   اعمال می‌شود، با یک سریالایزر واحد.
2. **تأیید مدیر ذخیره نمی‌شد.** `get_db` سشن را می‌بندد **بدون commit** — در این محصول هر روتر خودش
   commit می‌کند — پس `POST /decisions/{id}/approve` پاسخ `{"ok": true, "executed": [...]}` می‌داد و بعد همه
   چیز برمی‌گشت: کارت روی میز، صف پیامک خالی، بدون قرارداد اندازه‌گیری. رفع: هر مسیر نوشتنی از
   `_write(db, …)` می‌گذرد و commit می‌کند (۱۱ مسیر). تست جدید `tests/test_v40_api.py` این را از یک سشن
   **جدید** چک می‌کند تا یک commit فراموش‌شده دیگر نتواند پشت همان سشن قایم شود.
3. **سؤال‌های واقعی مدیر به جواب عمومی می‌رسیدند.** «کدوم کالا داره خراب می‌شه؟» الگوهای قصد را
   نمی‌شناخت (فقط «انقضا» و «منقضی» را می‌شناخت) و جواب عمومی وضعیت فروشگاه می‌گرفت؛
   «جشنوارهٔ قبلی جواب داد؟» هم در برنامهٔ ابزار خود `get_campaign_outcome` را نداشت و عدد نتیجهٔ واقعی
   هرگز خوانده نمی‌شد — و روی فروشگاهی که کمپینی نداشت، جملهٔ «فروش ۰ تومان در برابر ۰ تومان» ساخته
   می‌شد که شبیه یک «صفر اندازه‌گیری‌شده» است، نه «کمپینی ثبت نشده». سه رفع: واژه‌های محاوره‌ای
   (خراب/فاسد/کپک/تاریخ گذشته/دیت) به قصد EXPIRY، ابزار نتیجهٔ کمپین به برنامهٔ همان قصد، و یک جواب
   صریح برای «کمپینی ثبت نشده» + خواندن اعداد از شواهد بازاریابی (نه از وضعیت جاری فروشگاه).
   تست‌های `test_colloquial_expiry_question_reaches_the_expiry_answer` و
   `test_a_real_campaign_outcome_is_answered_with_real_numbers` هر دو حالت را قفل می‌کنند.

دروس ثبت‌شده: (۱) هر مسیر API که چیزی می‌نویسد باید با یک تست «سشن تازه» تأیید شود؛
(۲) «فهرست خالی» هرگز اثبات یک مسیر نیست؛ (۳) جملهٔ صفر جعلی از خود عدد جعلی خطرناک‌تر است، چون
واقعی به‌نظر می‌رسد.

### ۲.۵ یادداشت اجرای کامل تست‌ها
دو شکست اولیه در اجرای کامل پیدا و رفع شد:

1. `test_v37_migrations.py::test_head_revision_is_v37_catchup` و
   `test_downgrade_one_step_and_reupgrade_preserves_shop_data` — این دو تست مقدار head را پین می‌کنند؛
   با ورود migration مغز، head به `d4f6a8b1c2e3` منتقل شد. تست‌ها به‌روزرسانی شدند (همان الگوی v3.8 که در
   خود فایل توضیح داده شده) و اکنون ثابت می‌کنند فقط جدول‌های مغز در `-1` حذف و همهٔ اشیای قدیمی‌تر
   دست‌نخورده می‌مانند.
2. پنج تست `test_v40_brain.py` در اجرای انفرادی سبز بودند اما در مجموعهٔ کامل (روی دیتابیس مشترک که
   صدها فروشگاه دیگر در آن بود) نتیجهٔ متفاوت می‌دادند — یعنی تست‌ها به دادهٔ دیگران وابسته بودند.
   برای رفع ریشه‌ای، ماژول اکنون دیتابیس اختصاصی خودش را می‌سازد (engine + sessionmaker مستقل و
   هدایت `SessionLocal` به آن) تا مغز همیشه روی «یک فروشگاه» تست شود. زمان همان ماژول از ~۱۰ ثانیه به
   ~۲ ثانیه رسید و اجرای کل مجموعه از ~۱۳ دقیقه به ۵:۳۱ کاهش یافت.

**اعلام صریح:** این نسخه «آمادهٔ تولید» اعلام **نمی‌شود** تا (۱) کل مجموعهٔ تست‌ها سبز شود ✅
(اجرای کامل v4.0.1: ۶۸۲ passed, 1 skipped)، (۲) بیلد ویندوز روی runner واقعی ساخته شود ⛔، (۳) APK واقعی امضا شود ✅
(ساخته و امضاشده؛ آزمون نصب روی گوشی فیزیکی باقی است). انحراف مدل به تأیید مالک رسید ✅ (۲۰۲۶-۰۹-۲۳). تصمیم مالک دربارهٔ انتشار: تگ و Release تا آماده‌شدن
هر دو آرتیفکت واقعی به تعویق افتاد — چیزی که روی همین شاخه هست «سورس تأییدشده با تست سبز» است، نه
یک انتشار نهایی.

### ۲.۶ v4.0.1 — مدیر دانلود مدل (درخواست مالک ۲۰۲۶-۰۹-۲۳: «برای دانلود مدل از دانلود منیجر استفاده کن که یک نوار پیشرفت هم بده»)

خطای دانلود روی ویندوز مالک (اسکرین‌شات ارسالی به این محیط نرسید، پس همهٔ حالت‌های شکست محتمل پوشش داده شد).
دانلود قبلی یک `urlopen` تک‌اتصالی بدون ادامه و بدون نوار پیشرفت بود؛ حالا `download_manager.py`
(فقط کتابخانهٔ استاندارد) همه‌جا — بیلد نصب‌کننده و دانلود داخل برنامه — استفاده می‌شود:

- **نوار پیشرفت واقعی**: درصد، مگابایت، سرعت لحظه‌ای، زمان باقی‌مانده؛ با جایگزین ASCII برای کنسول‌های
  ریدایرکت‌شدهٔ ویندوز (به‌علاوهٔ `PYTHONUTF8=1` در `builder-lib.ps1` تا خروجی پایتون هیچ‌وقت روی کدپیج
  ویندوز خراب نشود).
- **ادامهٔ دانلود**: فایل کنترل `*.part.dl.control.json` وضعیت هر سگمنت را نگه می‌دارد؛ قطعی اینترنت یا
  کنسل‌کردن بیلد، دفعهٔ بعد از همان نقطه ادامه می‌یابد. `DOWNLOAD_FAILED` دیگر فایل ناقص را حذف نمی‌کند.
- **چند اتصال موازی** (پیش‌فرض ۴، تا ۱۶) با Range؛ سروری که Range نمی‌فهمد تک‌اتصالی می‌شود.
- **منبع رسمی جایگزین**: Hugging Face سپس ModelScope (انتشار رسمی خود Qwen — sha256 هر دو فایل در
  ModelScope با پین رجیستری بایت‌به‌بایت یکی است؛ ۲۰۲۶-۰۹-۲۳ از API رسمی بررسی شد). آینهٔ غیررسمی ممنوع
  باقی می‌ماند و `allowed_source` سخت‌گیرتر شد (هاست دقیق یا زیردامنهٔ رسمی؛ `xhuggingface.co` رد می‌شود).
- **IDM**: اگر Internet Download Manager روی ویندوز نصب باشد (`--downloader auto` پیش‌فرض)، URL به
  IDMan.exe سپرده می‌شود و پنجرهٔ خود IDM نوار پیشرفت را نشان می‌دهد؛ ما منتظر می‌مانیم و در پایان
  sha256 را خودمان تأیید می‌کنیم. `builtin`/`idm` حالت‌های اجباری‌اند؛ CI روی `builtin` پین شده.
- **تأیید واقعی در همین محیط**: دانلود سگمنتی ۴-اتصالی از CDN واقعی، قطع عمدی با SIGINT و ادامه از
  همان نقطه، و fallback خودکار وقتی منبع اول ۴۰۴ داد — هش نهایی هر بار با پین مستقل یکی بود.
  ۲۲ تست آفلاین (`test_v40_download_manager.py`): سرور HTTP محلی با پشتیبانی Range + ساعت قابل تزریق
  برای ماشین حالات IDM.
- **صادقانه**: رفتار واقعی IDM روی ویندوزِ مالک و دانلود واقعی ~۱ گیگابایتی همچنان فقط روی همان ماشین
  قابل تأیید است؛ اینجا Hugging Face/ModelScope مسدودند و چیزی جعل نشد. اجرای کامل: **۶۸۲ passed,
  1 skipped**.

### ۲.۷ v4.1.0 — موتور استنتاج روی اندروید + رفع رگرسیون BOM ویندوز (۲۰۲۶-۰۹-۲۳)

**رگرسیون BOM (خطای مالک، فایل eror.txt):** ویرایش v4.0.1 ناخواسته BOM یونیکدِ `builder-lib.ps1` را
برداشت؛ PowerShell 5.1 ویندوز فایل را ANSI خواند و کل بیلد قبل از هر کاری با خطای «Unexpected token»
مُرد. BOM برگردانده شد (کامیت `4812598`، فوراً پوش شد تا مالک بتواند BUILD-SETUP.bat را دوباره اجرا کند)
و `test_installer_scripts.py` (۹ تست) آن را برای همیشه قفل کرد: هر `.ps1` زیر installer باید BOM داشته
باشد، UTF-8 معتبر باشد و CRLF یکدست داشته باشد؛ فایل‌های `.bat` نباید BOM داشته باشند (cmd بایت‌بایت
می‌خواند). علت ریشه‌ای: ابزار ویرایش فایل را بدون BOM بازنویسی کرد — حالا تست مانع است.

**موتور روی اندروید (درخواست مالک: مدل روی گوشی فعال شود؛ جفت = یک سیستم؛ مدل کامل روی اندروید):**
- llama.cpp `llama-server` با همان تگ b6283 از سورس رسمی (codeload.github.com) برای arm64 کامپایل شد —
  **بدون NDK** (dl.google.com در این محیط مسدود است): کامپایلر متقابل zig (پکیج رسمی `ziglang` از PyPI)،
  خروجی باینری کاملاً static با aarch64-linux-musl؛ اندروید آن را از nativeLibraryDir اجرا می‌کند.
  `lib/arm64-v8a/libllamaserver.so` (~۷ مگابایت) داخل خود APK حمل می‌شود؛ APK فقط ۵٫۳ مگابایت.
- `BrainEngine.java` همان معماری رایانه را روی گوشی می‌آورد: زیرفرآیند + HTTP روی 127.0.0.1:8081، همان
  پرچم‌ها، همان /health، همان بدنهٔ /v1/chat/completions و preset «fast»، همان حذف <think>. جفت با رایانه →
  مغز رایانه جواب می‌دهد؛ مستقل → همان فایل مدل روی گوشی، با برچسب صادقانهٔ «بدون دادهٔ زندهٔ فروشگاه».
- **تأیید واقعی:** بیلد دوقلوی x86_64 با همان سورس/فلگ‌ها اجرا و `--version` چاپ شد (6283 (b6283))؛
  یک GGUF معتبر Qwen2 (تولیدشده با پکیج رسمی gguf) بارگذاری شد و /v1/chat/completions با همان بدنهٔ
  جاوا **واقعاً ۲۴ توکن تولید کرد**. باینری arm64: ELF استاتیک (بدون PT_INTERP/PT_DYNAMIC) تأیید شد.
  ساخت مجدد: `scripts/android/build-engine.sh` (سورس با sha256 پین شده).
- **پاریتی قفل‌شده:** `test_v41_android_parity.py` (۷ تست) برابری پین‌های BrainModel.java با
  model_registry.py (شناسه/فایل/URLها/sha256/حجم/حداقل رم)، برابری تگ موتور با نصب‌کنندهٔ ویندوز،
  استاتیک‌بودن ELF، بسته‌بودن موتور در APK (extractNativeLibs) و قواعد پرامپت محلی را اجباری می‌کند.
- APK واقعی: `SupermarketMobile-4.1.0.apk` — versionCode 40100، 5.3MB، sha256
  876bec4a459b930e2557d5e5399450132d4dd409923a64fb9dc2d63e3ac18b95، همان گواهی امضا (به‌روزرسانی روی
  نسخهٔ قبلی نصب می‌شود).
- **صادقانه:** اجرای روی گوشی فیزیکی ARM در این محیط ممکن نیست (نه گوشی هست نه شبیه‌ساز)؛ آزمون نهایی:
  نصب 4.1.0، دانلود مدل، «روشن‌کردن و آزمایش موتور محلی»، قطع رایانه و پرسیدن سؤال (برچسب «مدل محلی»
  باید بیاید). پاسخ به پرسش مالک دربارهٔ OBB: لازم نیست — OBB سازوکار پلی‌استور برای بازی‌هاست؛ برای نصب
  دستی بدون مزیت و با حریم خصوصی ضعیف‌تر است؛ دانلود پس از نصب در حافظهٔ خصوصی همان تجربه را امن‌تر می‌دهد.

### ۲.۸ v4.1.1 — رفع «برنامه نصب نشد» اندروید (گزارش مالک، ۲۰۲۶-۰۹-۲۳)

APK 4.1.0 از نظر apksigner/aapt2 سالم بود (تأیید شد: sha256، CRC، امضای v2+v3، منیفست)، اما نصب روی گوشی
مالک شکست می‌خورد. بررسی ساختاری علت محتمل سمت فایل را پیدا کرد: **APK فقط arm64-v8a داشت**؛ گوشی ۳۲ بیتی
(armeabi-v7a) کل بسته را رد می‌کند (INSTALL_FAILED_NO_MATCHING_ABIS). رفع:
- موتور حالا برای **هر دو ABI** ساخته می‌شود: arm64-v8a + armeabi-v7a (باینری استاتیک با zig،
  cortex-a7 + NEON، musl سخت-شناور — باگ بک‌اند zig برای NEON در حالت soft/softfp ثبت شد؛ باینری کاملاً
  استاتیک ABI خودش را همراه دارد و سخت-شناور روی همهٔ ARMv7 اجرا می‌شود). `build-engine.sh` هر دو را
  می‌سازد و `build-apk.sh` هر دو را بسته‌بندی می‌کند.
- **پیش‌پرواز نصب** جدید (`scripts/android/verify-apk.py`، فقط کتابخانهٔ استاندارد): همان چیزهایی که
  PackageManager واقعاً چک می‌کند — resources.arsc باید stored و ۴-بایت هم‌تراز باشد (اندروید ۱۱+ در غیر
  این صورت کل APK را رد می‌کند؛ بررسی شد و تراز بود)، classes.dex، CRC کامل zip، و فهرست ABIها (بدون
  armeabi-v7a هشدار می‌دهد). بیلد بدون PASS تمام اعلام نمی‌شود و تست‌ها آن را روی APK منتشرشده اجرا
  می‌کنند.
- **علل سمت گوشی که هیچ APKای نمی‌تواند رفع کند** (اگر باز هم نصب نشد): نسخهٔ نصب‌شدهٔ قبلی با **کلید امضای
  متفاوت** (اول حذفش کنید)، Play Protect («جزئیات ← نصب به هر حال»)، فایل خراب‌شده حین انتقال (sha256 را
  مقایسه کنید)، اندروید زیر ۷ (زیر minSdk).
- تست‌ها: پاریتی حالا وجود هر دو ABI را به‌عنوان ELF استاتیک با ماشین درست (EM_AARCH64=183 /
  EM_ARM=40) اجباری می‌کند + پیش‌پرواز روی APK منتشرشده.
