# ZXing core 3.5.3 (Apache-2.0) — vendored sources
Unmodified `core/src/main/java` from https://github.com/zxing/zxing (tag zxing-3.5.3).
Compiled into the APK by `scripts/android/build-apk.sh` for the native camera barcode scanner (`ScanActivity`).
