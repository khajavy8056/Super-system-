package ir.khajavy.supermarket;

import android.app.Dialog;
import android.content.Context;
import android.view.Gravity;
import android.widget.LinearLayout;
import android.widget.TextView;

/** v2.0 — guided tour per section: auto on first visit, replayable from the "?" header button. */
public final class Tour {
    private Tour() {}
    static final String[][] STEPS = {
        {"home", "داشبورد", "۱۲ بلوک وضعیت فروشگاه: فروش امروز، فاکتورها، موجودی، انقضا، مطالبات، پرفروش‌ها، روند ۷ روز و سلامت سامانه.", "نقطهٔ سبز بالای صفحه یعنی به رایانه متصل هستید؛ زرد/قرمز یعنی آفلاین — کارتان ادامه می‌یابد و بعداً همگام می‌شود.", "دکمهٔ ☰ همهٔ بخش‌ها را باز می‌کند؛ نوار پایین دسترسی سریع به فروش، کالا و انبار است."},
        {"pos", "صندوق فروش", "کالا را با نام/بارکد جست‌وجو یا با دکمهٔ «اسکن» با دوربین بخوانید؛ بلافاصله به سبد اضافه می‌شود.", "با + و − تعداد را تغییر دهید؛ روی عدد ضربه بزنید تا مقدار اعشاری (مثلاً ۱٫۵ کیلوگرم) وارد کنید.", "«نگه‌داشتن» فاکتور را کنار می‌گذارد (تا ۱۰ فاکتور) و از «نگه‌داشته‌ها» دوباره باز می‌شود.", "«پرداخت»: نقدی/کارت/کارت‌به‌کارت یا نسیه برای مشتری ثبت‌شده. فاکتور همان لحظه روی گوشی ثبت و به رایانه فرستاده می‌شود."},
        {"held", "فاکتورهای نگه‌داشته", "هر فاکتور با زمان، مشتری و مبلغ فهرست شده؛ ضربه بزنید تا به صندوق برگردد.", "نگه‌داشتن طولانی → حذف."},
        {"invoices", "فاکتورها", "فهرست فاکتورهای رایانه و فاکتورهای ثبت‌شده روی همین گوشی (با وضعیت همگام‌سازی).", "داخل هر فاکتور: رسید، چاپ روی چاپگر رایانه، ابطال (با رمز مدیر برای فاکتور پرداخت‌شده) و مرجوعی با ضربه روی قلم."},
        {"customers", "مشتریان", "مشتری آزاد نیاز به ثبت ندارد؛ مشتری ثبت‌شده دفتر حساب، سقف اعتبار و پیامک دارد.", "در پروندهٔ مشتری: مانده، تسویهٔ جزئی/کامل، اصلاح دستی و فاکتورهای او."},
        {"marketing", "جشنواره و کوپن", "جشنواره قواعد تخفیف (درصدی/ثابت، حداقل خرید، سقف، بازه) را تعریف می‌کند.", "کوپن با کد و شرایط صادر می‌شود و در صندوق با دکمهٔ «کوپن» اعتبارسنجی می‌شود."},
        {"products", "کالاها", "کالا فقط مشخصات (نام، بارکد، واحد) است؛ قیمت و موجودی به «بچ» تعلق دارد.", "کالای بدون بارکد کد داخلی INT- می‌گیرد. اسکن کالا مستقیم پروندهٔ آن را باز می‌کند.", "تاریخچهٔ قیمت غیرقابل ویرایش است؛ «قیمت سریع» قیمت بچ‌های فعال را عوض می‌کند."},
        {"receive", "ورود کالا", "بارکد را اسکن کنید، مقدار و قیمت خرید/مصرف‌کننده/فروش و انقضا را وارد کنید → یک بچ جدید (حرکت IN).", "آخرین قیمت‌های کالا خودکار پر می‌شود؛ واحدهای صحیح اعشار نمی‌پذیرند."},
        {"inventory", "انبار", "موجودی هر کالا با رنگ: سبز کافی، زرد زیر حد هشدار، قرمز صفر.", "تب انقضا کالاها را در بازه‌های منقضی/امروز/۳/۷/۳۰ روز نشان می‌دهد."},
        {"stocktake", "انبارگردانی", "یک جلسه بسازید و «شروع» را بزنید؛ کالاها را اسکن و تعداد فیزیکی را وارد کنید — قابل توقف و ادامه.", "پس از «پایان شمارش»، گزارش اختلاف را ببینید و با «تأیید» اصلاحات ADJUSTMENT اعمال می‌شود."},
        {"stockops", "ضایعات / اصلاح / انتقال", "بچ را با اسکن انتخاب کنید؛ ضایعات (WASTE)، اصلاح موجودی (ADJUSTMENT) یا انتقال به انبار دیگر (TRANSFER) با ذکر دلیل ثبت می‌شود."},
        {"warehouses", "انبارها", "انبارها و قفسه‌ها؛ موجودی و ارزش هر انبار."},
        {"movements", "گردش موجودی", "همهٔ حرکات: IN، SALE_OUT، RETURN_IN، WASTE، ADJUSTMENT، TRANSFER با مرجع."},
        {"reports", "گزارش‌ها", "۱۰ گزارش: فروش (روزانه/کالا)، صندوق‌داران، موجودی، انقضا، کمبود، بچ‌ها، سود، هزینهٔ خرید، اصلاحات، انبارگردانی‌ها.", "بازهٔ فروش را با چیپ‌های بالای گزارش عوض کنید."},
        {"accounting", "حسابداری", "نمای کلی، صندوق روز (باز/بستن شیفت)، هزینه‌ها، تأمین‌کنندگان، چک‌ها، دفتر روزنامه، تراز آزمایشی، سود و زیان، ترازنامه و سرفصل حساب‌ها.", "روی هر سند ضربه بزنید تا ردیف‌ها را ببینید یا سند معکوس بزنید."},
        {"users", "کاربران", "کاربر بسازید، نقش‌ها را انتخاب کنید، رمز عوض کنید یا کاربر را غیرفعال کنید."},
        {"audit", "لاگ حسابرسی", "هر رویداد مهم (ورود، فروش، ابطال، ورود کالا، تنظیمات، همگام‌سازی) با قبل/بعد ثبت شده؛ با چیپ‌ها فیلتر کنید."},
        {"settings", "تنظیمات", "همهٔ دسته‌های تنظیمات رایانه (صندوق، انبار، انقضا، قیمت، مشتری، پیامک، پرینتر، امنیت، پشتیبان، ظاهر…) اینجاست؛ روی هر مورد ضربه بزنید تا ویرایش شود."},
        {"store", "مشخصات فروشگاه", "نام، تلفن، آدرس، شناسهٔ مالیاتی و پیام رسید — روی رسیدها چاپ می‌شود."},
        {"sms", "پیامک", "صف ارسال، تلاش مجدد ناموفق‌ها، ویرایش الگوها و ارسال دستی."},
        {"hardware", "سخت‌افزار", "وضعیت چاپگر/بارکدخوان/کشوی رایانه، چاپ آزمایشی، باز کردن کشو و آزمایش دوربین گوشی."},
        {"diagnostics", "تست اتصالات", "تست کامل سامانه با I/O واقعی؛ نتیجه با جزئیات هر بررسی ذخیره می‌شود."},
        {"support", "پشتیبانی", "درخواست با نوع و اولویت ثبت کنید؛ پاسخ پشتیبانی همین‌جا در گفت‌وگو می‌آید؛ عکس هم می‌توانید بفرستید."},
        {"license", "لایسنس", "وضعیت، انقضا، شناسهٔ سخت‌افزار و بررسی مجدد."},
        {"sync", "همگام‌سازی", "صف عملیات این گوشی، موارد ردشده توسط رایانه و دستگاه‌های جفت‌شده. همگام‌سازی خودکار هر ۲۰ ثانیه و بعد از هر ثبت انجام می‌شود."},
        {"cloud", "همگام‌سازی ابری", "اختیاری؛ پشتیبان روی Google Drive از رایانه."},
        {"device", "تنظیمات دستگاه", "آدرس رایانه، جفت‌سازی دوباره، پوسته، آزمایش اسکنر و پاک‌کردن داده‌های محلی."},
        {"about", "درباره", "نسخهٔ اپ و رایانه، مجوزها."},
    };
    static String[] find(String key) { for (String[] s : STEPS) if (s[0].equals(key)) return s; return null; }
    public static void maybe(Context c, String key) { if (find(key) == null) return; if ("1".equals(Prefs.get("tour_seen_" + key, ""))) return; Prefs.set("tour_seen_" + key, "1"); Api.ui(() -> show(c, key), 500); }
    public static void resetAll() { for (String[] s : STEPS) Prefs.set("tour_seen_" + s[0], ""); }
    public static void show(Context c, String key) {
        String[] s = find(key); if (s == null) s = find("home");
        final String[] steps = s; final int[] idx = {0};
        LinearLayout l = Ui.col(c); TextView body = Ui.body(c, ""); body.setLineSpacing(0, 1.35f); l.addView(body); TextView pos = Ui.muted(c, ""); pos.setGravity(Gravity.CENTER); pos.setPadding(0, Ui.dp(10), 0, Ui.dp(6)); l.addView(pos);
        LinearLayout br = Ui.row(c); Dialog[] d = new Dialog[1];
        android.widget.Button prev = Ui.ghost(c, "قبلی", null), next = Ui.primary(c, "بعدی", null); prev.setLayoutParams(Ui.margin(Ui.weight(1), 0, 0, 4, 0)); next.setLayoutParams(Ui.margin(Ui.weight(1), 4, 0, 0, 0)); br.addView(prev); br.addView(next); l.addView(br);
        Runnable draw = () -> { body.setText(steps[2 + idx[0]]); pos.setText("گام " + Ui.fa(String.valueOf(idx[0] + 1)) + " از " + Ui.fa(String.valueOf(steps.length - 2))); prev.setEnabled(idx[0] > 0); prev.setAlpha(idx[0] > 0 ? 1f : 0.4f); next.setText(idx[0] == steps.length - 3 ? "فهمیدم" : "بعدی"); };
        prev.setOnClickListener(v -> { if (idx[0] > 0) { idx[0]--; draw.run(); } }); next.setOnClickListener(v -> { if (idx[0] < steps.length - 3) { idx[0]++; draw.run(); } else d[0].dismiss(); });
        draw.run(); d[0] = Ui.sheet(c, "راهنما: " + steps[1], l);
    }
}
