# Optional: native WebView2 desktop window on Windows (v1.3+).
# Installed by installer/windows/builder-lib.ps1 on a best-effort basis; when the
# download fails (e.g. pypi.org unreachable) the build continues and the app
# opens in an Edge app-mode window instead.
pywebview>=5.0,<6.0; sys_platform == "win32"
pythonnet>=3.0; sys_platform == "win32"
