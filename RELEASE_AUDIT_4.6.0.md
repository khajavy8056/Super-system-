# Release Audit — v4.6.0 (2026-09-24)

**موضوع دوره:** (۱) حذف کامل مغز فروشگاه/مدل زبانی از اندروید و ویندوز (تصمیم صریح صاحب فروشگاه)، (۲) هوش فروشگاه به‌عنوان مشاور اصلی با توضیحات کامل و قابل‌فهم برای هر پیشنهاد، (۳) آنالیز کل سیستم.

## ۱) حذف کامل مغز فروشگاه

| لایه | چه حذف شد |
|---|---|
| بک‌اند | پکیج `business_brain` (۲۵ فایل)، `routers/brain.py`، `models/brain.py`، اتوستارت مدل، ورکر ۱۵ دقیقه‌ای، همهٔ مسیرهای `/api/brain/*` |
| پایگاه‌داده | مهاجرت `c9e1f2a4b6d8` (هد جدید): شش جدول `brain_*` حذف می‌شوند؛ downgrade آن‌ها را دقیقاً مثل تاریخچه بازسازی می‌کند (قابل بازگشت)؛ دادهٔ فروشگاه دست نمی‌خورد |
| اندروید | BrainScreens، BrainModel، BrainModelService، BrainEngine، BrainVoice، ReminderRx، مجوز RECORD_AUDIO، سرویس دانلود پس‌زمینه، گیرندهٔ یادآوری، دو مسیر ناوبری، و **libllamaserver.so از هر دو ABI** |
| ویندوز | مرحلهٔ «آماده‌سازی مدل» از بیلدر، فراخوانی verify_setup، include مدل در setup.iss، و کل `scripts/model/` — بیلد دیگر هیچ دانلود/هش/GGUF ندارد |
| پنل وب | brain.js، تگ اسکریپت، warm-key وضعیت مغز، دکمه و کارت‌های مرتبط |

**نتیجهٔ قابل‌اندازه‌گیری:** APK از ۹.۳ مگابایت به **۱.۴ مگابایت** رسید (موتور llama.cpp حذف شد)؛ Setup.exe ویندوز نیز ~۱ گیگابایت مدل و ~۱۵ مگابایت موتور را دیگر حمل نمی‌کند و مرحلهٔ دانلود کاملاً حذف شد.

**برای دستگاه‌های موجود:** پوشهٔ `brain` در دادهٔ کاربر (ویندوز: `%USERPROFILE%\SupermarketSystem\brain`، اندروید: فضای برنامه) دیگر استفاده نمی‌شود؛ حذف دستی آن ~۱ گیگابایت آزاد می‌کند.

## ۲) هوش فروشگاه با راهنمای کامل

- ماژول جدید `insight_guides.py`: برای هر ۱۳ نوع اصلی پیشنهاد یک راهنمای دست‌نویس با پنج بخش: **تعریف** (VIP یعنی چه و فرقش با مشتری معمولی)، **چرا مهم است**، **گام‌های اجرا** (۳-۴ قدم مشخص)، **اگر انجام نشود چه می‌شود**، **مثال ملموس**. برای انواع PRO راهنمای عمومی صادقانه.
- `insights.to_dict` راهنما را روی **هر** آیتم می‌گذارد؛ اندروید (کارت «این پیشنهاد یعنی چه؟ (راهنمای کامل)» با گام‌های شماره‌دار) و پنل وب (کارت راهنما در مودال جزئیات) هر دو رندرش می‌کنند.
- کاملاً قطعی: بدون مدل، بدون شبکه، بدون وابستگی به سرویس‌ها (تست‌شده).

## ۳) آنالیز کل سیستم

- **مجموعهٔ کامل: ۶۱۴ passed / 1 skipped.**
- مسیرهای حذف‌شدهٔ `/api/brain/*` → ۴۰۴ تمیز (تست‌شده، نه ۵۰۰).
- تست مهاجرت‌ها: حذف جدول‌ها در هد + بازگشت با downgrade + سلامت داده — پاس.
- ۱۷ فایل تست عصر مغز حذف و تست‌های نصب‌کننده/مهاجرت بازنویسی شدند.
- `node --check` روی app.js و insights.js پاس.
- UTF-8 output پایتون در بیلدر ویندوز حفظ شد (جابجایی env به مرحلهٔ venv).

## APK 4.6.0

| فیلد | مقدار |
|---|---|
| فایل | `releases/android/SupermarketMobile-4.6.0.apk` |
| حجم | **۱٬۳۹۸٬۱۶۳ بایت (~۱.۴M)** — بدون هیچ باینری بومی |
| versionCode / Name | **40600 / 4.6.0** (badging تأیید شد) |
| native ABIs | none (pure-java) |
| گواهی | همان گواهی همهٔ نسخه‌ها (`18b4ab18…`) → به‌روزرسانی درجا |
| preflight | PASS |
| sha256 | `feea39dcdc33b4565663fe15d8abde68949374da67b3f48d8eeef0fd462d93ac` |

## ویندوز

`ساخت-فایل-نصب.bat` → بدون مرحلهٔ مدل، مستقیم تا Setup.exe. مهاجرت v4.6.0 هنگام اولین اجرا خودکار اعمال می‌شود (جدول‌های مغز حذف می‌شوند؛ دادهٔ فروشگاه دست نمی‌خورد).
