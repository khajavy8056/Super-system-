# Changelog

همه تغییرات مهم این پروژه در این فایل ثبت می‌شود. فرمت بر اساس [Keep a Changelog](https://keepachangelog.com) و نسخه‌گذاری [SemVer](https://semver.org).


## [2.4.0] - 2026-09-10 (اندروید — استقلال کامل از رایانه)

### اندروید
- **API داخلی گوشی (`Local.java`)**: در حالت مستقل، همهٔ مسیرهایی که صفحه‌ها از رایانه می‌خواستند، حالا روی خود گوشی و از SQLite پاسخ داده می‌شود (فروش/فاکتور/ابطال/مرجوعی، مشتریان و دفتر حساب و تسویه و یادآوری بدهی، گزارش‌ها و داشبورد کامل، انبار/انبارگردانی/ضایعات/اصلاح/انتقال/انبارها/گردش موجودی، جشنواره و کوپن، حسابداری کامل (شیفت صندوق، هزینه‌ها، تأمین‌کنندگان، چک‌ها، دفتر روزنامه دوطرفه، تراز آزمایشی، سود و زیان، ترازنامه)، کاربران و نقش‌ها و لاگ حسابرسی، تنظیمات همهٔ دسته‌ها، سخت‌افزار، تست اتصالات، پیامک). رایانه فقط یک گزینهٔ همگام‌سازی است.
- **پایگاه دادهٔ گوشی نسخهٔ ۲**: جدول‌های اقلام فاکتور، گردش موجودی، دفتر حساب، تاریخچهٔ قیمت، انبارگردانی، انبارها، هزینه‌ها، تأمین‌کنندگان، چک‌ها، سند حسابداری، شیفت صندوق، کاربران، حسابرسی، تنظیمات — ارتقای خودکار بدون از دست رفتن داده.
- **پیامک هنگام فروش**: در صفحهٔ پرداخت، شمارهٔ موبایل مشتری پرسیده می‌شود (برای مشتری انتخاب‌شده پر می‌شود)؛ با ثبت فاکتور، پیامک از خود گوشی ارسال می‌شود و شماره به رایانه هم می‌رود (اگر متصل باشد).
- **تنظیمات کامل روی گوشی**: همهٔ دسته‌ها (فروشگاه، زمان، صندوق، انبار، انقضا، محصولات، بارکد، قیمت‌گذاری، مشتریان، دفتر حساب، جشنواره، پیامک، پرینتر، شبکه، امنیت، پشتیبان، ظاهر، به‌روزرسانی، لایسنس، ...) با پیش‌فرض‌های همان ویندوز، ذخیره در گوشی.
- **ورود محلی و کاربران**: مدیرِ ساخته‌شده در ویزارد کاربر شمارهٔ ۱ است؛ کاربر جدید با نقش‌های استاندارد (مدیر سیستم/مدیر فروشگاه/صندوق‌دار/انباردار/ناظر) روی گوشی ساخته می‌شود؛ خروج از حساب به صفحهٔ ورود محلی می‌رود (نه ویزارد).
- وقتی گوشی به رایانه جفت است ولی رایانه در دسترس نیست، خواندن‌ها از دادهٔ خود گوشی پاسخ داده می‌شود (نوشتن‌ها مثل قبل در صف همگام‌سازی).
- APK: `releases/android/SupermarketMobile-2.4.0.apk` (همان کلید امضای 2.3 → نصب مستقیم روی 2.3).

## [2.3.0] - 2026-09-10 (ویندوز + اندروید — پیامک مستقل از چاپ، راهنمای ملی‌پیامک، نصب پس‌زمینه، اثر انگشت، اتصال آنلاین پایدار، تمام‌صفحه)

### پیامک فاکتور (هر دو سامانه)
- **پیامک به محض تأیید فاکتور و مستقل از چاپ**: `pos.print_after_checkout=false` دیگر پیامک را خاموش نمی‌کند؛ کلید جداگانهٔ `sms.send_invoice` (پیش‌فرض روشن) و `sms.send_immediately` (ارسال فوری + صف با تلاش مجدد). در اندروید حالت مستقل هم `SmsLocal` پس از تأیید فاکتور پیامک را با همان تنظیمات ارسال می‌کند (`تست: test_invoice_sms_sent_when_printing_off_and_skipped_when_disabled`).
- **راهنمای گام‌به‌گام ملی‌پیامک داخل برنامه**: `GET /api/sms/guide` وضعیت فعلی (چه چیزی هنوز ثبت نشده) + ۸ گام با آدرس دقیق بخش‌های پنل ملی‌پیامک (ثبت‌نام، احراز هویت، وب‌سرویس، خط خدماتی، ساخت الگو و گرفتن bodyId، …). کارت راهنما در تب پیامک ویندوز و صفحهٔ «پیامک» اندروید با نشانگر «آمادهٔ ارسال / گام‌های مانده».

### اندروید «سوپری من» 2.3.0 (versionCode 20300)
- **راه‌اندازی اولیه در پس‌زمینه**: `InstallService` (Foreground Service با اعلان پیشرفت در نوار بالا)؛ کاربر می‌تواند از برنامه خارج شود؛ با بازگشت، همان صفحهٔ لودینگ از جای واقعی ادامه می‌یابد (پیشرفت روی `install_t0/install_total` ذخیره می‌شود).
- **ورود با اثر انگشت**: `Biometric` (BiometricPrompt ≥ Android 9، در غیر این صورت قفل صفحهٔ سیستم)؛ دو کلید در «دستگاه»: «ورود با اثر انگشت» (دکمه در صفحهٔ ورود) و «قفل با اثر انگشت هنگام بازگشت» (پس از ۳۰ ثانیه دور بودن).
- **اتصال پایدار / چند دستگاه**: نردبان مسیر LAN → بیکن رله → رله؛ با هر تغییر شبکهٔ گوشی (`Sync.watchNetwork`) مسیر دوباره پیدا می‌شود؛ پورت از QR/کد جفت‌سازی/بیکن خوانده می‌شود (**هرگز 8000 پیش‌فرض نیست**). چند گوشی با کاربر و نقش خودشان (صندوق‌دار، انباردار…) به همان فروشگاه وصل و از طریق رایانه یا رله با هم همگام می‌شوند.
- کارت «اتصال از راه دور» در «دستگاه» (وضعیت رله، رایانه آنلاین است؟، صف).

### ویندوز 2.3.0
- **تمام‌صفحه از لحظهٔ باز شدن**: پنجرهٔ WebView2 `fullscreen + frameless` (بدون نوار عنوان و دکمه‌های کوچک/بزرگ/بستن)؛ حالت Chromium با `--kiosk`؛ خروج فقط با دکمهٔ «خروج» برنامه. `SUPERMARKET_WINDOWED=1` برای پشتیبانی.
- **پورت ثابت 8765** (`stable_port`: env → آخرین پورت → 8765…8784) و صادر کردن `PORT` واقعی به بک‌اند، پس بیکن LAN و QR جفت‌سازی همان پورت واقعی را اعلام می‌کنند (ریشهٔ باگ «گوشی 8000 را می‌زند»).

### اتصال آنلاین (رایگان + پولی)
- **رایگان ۱ — LAN**: کلید اتصال + بیکن UDP 48765 + پورت ثابت؛ IP عوض شود گوشی خودش پیدا می‌کند.
- **رایگان ۲ — Google Drive** (`appDataFolder`, از v2.1): همگام‌سازی چند‌دقیقه‌ای بدون سرور، مناسب وقتی رایانه خاموش است.
- **پولی/خودمیزبان — رلهٔ «سوپری من»** (`relay/`, FastAPI بدون‌حالت، Docker): رایانه بیرون‌زن (long-poll) وصل می‌شود، گوشی درخواست را به `/r/{store}/call` می‌فرستد و پاسخ همان API محلی است؛ شناسهٔ فروشگاه + کلید خودکار در QR جفت‌سازی و `/api/mobile/link`؛ رایانه هر ۶۰ ثانیه آدرس LAN خود را روی رله منتشر می‌کند تا گوشی در فروشگاه دوباره به LAN برگردد. تنظیم در «ابر ← اتصال از راه دور» (فقط آدرس سرور). راهنما: `relay/README.md`.

### آزمون‌ها
- pytest: **345 passed, 1 skipped** (۶ تست جدید `test_v23_online_sms.py`: راهنمای پیامک، پیامک بدون چاپ، پیکربندی رله + بار جفت‌سازی با پورت 8765، رفت‌وبرگشت رلهٔ واقعی، منبع لانچر تمام‌صفحه/پورت ثابت، قراردادهای منبع اندروید).
- کامپایل اندروید (aapt2 + ecj + d8) بدون خطا.
- **NOT VERIFIED روی دستگاه فیزیکی**: BiometricPrompt، اعلان Foreground Service، تغییر شبکه؛ **NOT VERIFIED**: بیلد Setup.exe (فقط از CI ویندوز `installer/ci/release-windows.yml`).
- **توجه به امضا**: کلید امضای ثابت 2.1/2.2 در محیط بیلد از دست رفت (در مخزن نبود — gitignore). 2.3.0 با کلید جدیدی (SHA-256 گواهی `18b4ab18…`) امضا شده که این بار **داخل مخزن** نگه‌داری می‌شود (`mobile-android/keystore/supery-release.jks`)؛ برای ارتقا از 2.2.0 **یک‌بار** حذف/نصب لازم است (قبلش «پشتیبان‌گیری» در تنظیمات دستگاه)؛ از 2.3.0 به بعد نسخه‌ها روی هم نصب می‌شوند.

## [2.2.0] - 2026-09-10 (فقط اندروید — نصب به‌روزرسانی روی نسخهٔ قبل، اعلان‌های سیستمی، بازطراحی UI/UX)

**فقط اپ اندروید تغییر کرده؛ نسخهٔ ویندوز همان 2.1 است و نیازی به به‌روزرسانی ندارد.**

### چرا نسخه‌های قبلی روی هم نصب نمی‌شد؟ (ریشهٔ مشکل + رفع)
- کلید امضای APK در `installer/output/` (پوشهٔ gitignore) ساخته می‌شد و هر بار بیلد، کلید **جدید** تولید می‌کرد؛ اندروید بستهٔ هم‌نام با امضای متفاوت را «به‌روزرسانی» نمی‌پذیرد و Play Protect هم هر بار «توسعه‌دهندهٔ ناشناخته» می‌دید. (امضاهای 1.7/1.9/2.0/2.1 همگی متفاوت بودند.)
- **رفع:** کلید ثابت `mobile-android/keystore/supery-release.jks` در مخزن نگه‌داری می‌شود و `build-apk.sh` همیشه از آن استفاده می‌کند. **2.2.0 با همان کلید 2.1.0 امضا شده** (SHA-256 گواهی `0f9d69b2…`)، پس روی 2.1.0 بی‌دردسر نصب می‌شود و از این پس همهٔ نسخه‌ها روی هم نصب خواهند شد. کاربران 2.0 و قبل‌تر یک‌بار باید نسخهٔ قدیمی را حذف کنند (داده‌های گوشی با «پشتیبان‌گیری» در تنظیمات دستگاه حفظ می‌شود).
- Play Protect: هشدار «این توسعه‌دهنده را نمی‌شناسد» برای هر APK خارج از Google Play بار اول نمایش داده می‌شود و فقط با انتشار در Play کاملاً حذف می‌شود؛ با کلید ثابت، پس از یک‌بار «نصب به‌هرحال»، به‌روزرسانی‌های بعدی با همان کلید دیگر «توسعه‌دهندهٔ جدید» محسوب نمی‌شوند. راهنمای نصب در README به‌روز شد.

### اعلان‌های سیستمی (نوار بالا) + صداها
- `Notify`: سه کانال اعلان (پاسخ پشتیبانی / انقضا و موجودی / سامانه و همگام‌سازی)، مجوز `POST_NOTIFICATIONS` اندروید ۱۳+، لمس اعلان → باز شدن همان بخش.
- بررسی پس‌زمینه هر ~۳۰ دقیقه با `AlarmManager` (بدون وابستگی Gradle) و پس از ریستارت گوشی (`BOOT_COMPLETED`)؛ حتی وقتی برنامه بسته است: پاسخ‌های پشتیبانی (ربات یا رایانه)، بچ‌های منقضی/تا ۷ روز، کمبود موجودی، ردشدن آیتم‌های همگام‌سازی.
- `Sfx`: صداهای ملایم سنتزشده مانند رایانه (ثبت فروش، افزودن به سبد، خطا، ابطال، نگه‌داشتن/بازیابی، هشدار، اعلان، جینگل خوش‌آمد روزانه) با کنترل بلندی و خاموش‌کردن. بخش جدید «اعلان‌ها و صداها» در منو (اعلان آزمایشی، بررسی اکنون).

### بازطراحی UI/UX طبق طرح‌های مرجع
- پالت آرام: تیرهٔ سرمه‌ای عمیق (`#1B2536`) با لهجهٔ فیروزه‌ای، روشنِ خاکستری‌ملایم با کارت‌های گرادیانی لطیف و گوشهٔ ۲۰dp؛ بدون نئون.
- منوی کشویی: آواتار + نام فروشگاه + «فروشگاه من»، گروه‌های بازشونده (فروش و مشتری / فاکتورها / کالا و موجودی / جشنواره و کوپن / مدیریت و سیستم)، سوییچ پوستهٔ روشن/تیره (با گزینهٔ خودکار ۷/۱۹ در تنظیمات دستگاه)، «خروج از حساب» قرمزِ توخالی.
- داشبورد: کارت خوش‌آمد گرادیانی (سلام + نام فروشگاه + تاریخ شمسی + میان‌برها)، کاشی‌های ۲×۲ (فروش امروز با نمودار ۷ روزه، کالاها، مشتریان، فاکتور امروز)، پرفروش‌ترین‌ها (طلایی)، نزدیک انقضا، کارت «حالت مستقل».
- صندوق فروش: ورودی نام/بارکد + دکمهٔ فیروزه‌ای «اسکن»، چیپ‌های مشتری/کوپن/تخفیف، کارت اقلام با آیکن، −/تعداد/+، تخفیف/حذف، خلاصهٔ پایین با «مبلغ قابل پرداخت» و دکمهٔ بزرگ «پرداخت».
- نوار پایین (خانه/فروش/کالاها/انبار/بیشتر) با کاشی فعال گرد.

### تست
- `backend/tests/test_v22_android_release.py` (کلید ثابت، اعلان‌ها/صداها، پوستهٔ جدید) + همهٔ تست‌های قبلی: **339 passed / 1 skipped**. APK با ecj/d8/apksigner ساخته و تأیید شد (`versionCode 20200`).
- NOT VERIFIED روی گوشی واقعی: نمایش اعلان‌ها و پخش صدا در سخت‌افزار (فقط کامپایل + بررسی منابع).

## [2.1.0] - 2026-09-10 («سوپری من» — ویزارد راه‌اندازی اندروید، لایسنس روی گوشی، اتصال ساده‌تر و پایدارتر)

### Fixed
- **QR جفت‌سازی در ویندوز بریده می‌شد** (کادر ۲۳۲px با `overflow:hidden` و تصویر ۲۶۰px + حاشیه → بخشی از کد پنهان و غیرقابل اسکن). اکنون کادر سفید ۳۲۰px با حاشیهٔ آرام (quiet zone) واقعی، SVG مقیاس‌پذیر با `preserveAspectRatio`، سطح تصحیح L (کد کوچک‌تر) — هم در تنظیمات → موبایل و هم در ویزارد نصب. اسکرین‌شات واقعی Chromium با jsQR رمزگشایی شد (`docs/screenshots/v21-mobile-pair.png`).

### Added — اتصال گوشی ↔ رایانه
- **کد ۶ رقمی** به‌جای اسکن: `POST /api/mobile/pair/code` (۱۰ دقیقه، یک‌بارمصرف) → گوشی `POST /api/mobile/pair/claim` (بدون احراز؛ فقط از همان شبکه). در تنظیمات ویندوز و ویزارد نصب کنار QR نمایش داده می‌شود.
- **کلید اتصال پایدار** (`link_key`، ۱۲ کاراکتر، یک‌بار ساخته می‌شود) در همهٔ payloadهای جفت‌سازی؛ **بیکن شبکهٔ داخلی** روی رایانه (`services/discovery.py`، UDP 48765: `SMKT-FIND <key>` → `SMKT-HERE <port> <key> <store>`) فقط به کلید خودش پاسخ می‌دهد. گوشی (`Discovery.java`) هر بار که آدرس ذخیره‌شده پاسخ نداد، رایانه را با همان کلید دوباره پیدا می‌کند → **تغییر IP دیگر اتصال را قطع نمی‌کند**؛ به محض ورود به وای‌فای، اتصال و همگام‌سازی خودکار برقرار می‌شود. «پیدا کردن خودکار رایانه» در ویزارد و تنظیمات دستگاه.
- `GET /api/mobile/link` — کلید اتصال + **حکم لایسنس رایانه** + اعتبار Drive برای گوشی جفت‌شده.
- **ورود به حساب Google داخل برنامه (رایانه)**: `POST /api/cloud/oauth/start` (authorization-code + PKCE، redirect به loopback همان رایانه) و `GET /api/cloud/oauth/callback` (صفحهٔ فارسی، بازگشت خودکار به برنامه با `postMessage`). صفحهٔ انتخاب حساب گوگل باز می‌شود، کاربر حساب را می‌زند و تمام؛ روش «کد روی دستگاه دیگر» به‌عنوان جایگزین باقی ماند (برای شبکه‌هایی که گوگل محدود است، همگام‌سازی LAN بی‌نیاز از اینترنت است).
- **گوشی خودش با Drive حرف می‌زند** (`CloudSync.java`): وقتی رایانه در شبکه نیست، صف عملیات به `ops-<device>-<ts>.json` در appDataFolder می‌رود و `snapshot.json` دریافت می‌شود — اعتبار از QR/کد/`/mobile/link` می‌آید.

### Added — اندروید («سوپری من»)
- **تغییر نام برنامه** به «سوپری من» (`strings.xml`؛ بستهٔ `ir.khajavy.supermarket` ثابت ماند تا به‌روزرسانی روی نصب قبلی انجام شود).
- **ویزارد راه‌اندازی اولیه** (`SetupActivity`) مانند ویندوز: خوش‌آمد → انتخاب حالت با عبارت روشن: **«نسخهٔ رایانه (ویندوز) را دارم»** → جفت‌سازی (QR / کد ۶ رقمی / آدرس+نام‌کاربری با پیدا کردن خودکار) و **بررسی لایسنس رایانه در همان لحظه**؛ یا **«فقط گوشی — رایانه ندارم»** → لایسنس (کلید روی گوشی، همان سرور و پروتکل ویندوز، HWID گوشی) → نام فروشگاه → تماس → واحد پول → پوسته (خودکار ۷/۱۹) → بانک اولیهٔ کالا (همان CSV ویندوز، در APK) → حساب مدیر → پایان → **لودینگ نصب یک‌بارهٔ ۴۵ دقیقه‌ای با همان ۱۳ فاز ویندوز، بدون هیچ اشاره‌ای به «دقیقه»**؛ بعد از آن هرگز تکرار نمی‌شود.
- **لایسنس روی گوشی** (`Lic.java`): حالت PC → اعتبار گوشی = لایسنس رایانه (هر همگام‌سازی از `/mobile/link` می‌خواند؛ هر ۴۰۲ رایانه هم قفل می‌کند؛ آفلاین تا تاریخ انقضای شناخته‌شده معتبر است). حالت مستقل → بررسی روزانه با سرور، ۷ روز مهلت آفلاین بدون تاریخ انقضا. **`LockActivity`**: «لایسنس شما به پایان رسیده» با فقط «ارتباط با پشتیبانی» (و در حالت مستقل: کلید جدید)؛ داده‌ها حفظ می‌شوند و با تمدید روی رایانه، گوشی خودکار باز می‌شود.
- **پشتیبانی همه‌جا** با همان سیستم ویندوز: `SupportActivity` قبل از راه‌اندازی، در صفحهٔ قفل و در منو؛ با رایانه → از طریق رایانه (`/support/tickets`)؛ بدون رایانه → `SupportRelay.java` مستقیم به همان صندوق پشتیبانی با همان قالب و کلید مسیریابی `TCK-000001@INSTALL8`، پاسخ اپراتور (Reply یا پیشوند) در گفت‌وگوی همان تیکت روی گوشی نمایش داده می‌شود؛ تیکت‌های ناموفق خودکار دوباره ارسال می‌شوند.
- **انتخاب‌گر تاریخ شمسی** (`DatePicker.java`): همهٔ فیلدهای تاریخ (انقضای بچ، سررسید چک، پایان جشنواره، انقضای کوپن، زمان‌بندی انبارگردانی) به‌جای تایپ، تقویم ماهانه با ‹ ›، چیپ سال، جمعه‌ها قرمز، «امروز» و «پاک کردن» باز می‌کنند.
- صفحهٔ «تنظیمات دستگاه» حالت/کلید اتصال/پیدا کردن رایانه را نشان می‌دهد؛ صفحهٔ «لایسنس» در حالت مستقل وضعیت گوشی را؛ صفحهٔ «همگام‌سازی ابری» وضعیت Drive گوشی را. واحدهای پیش‌فرض در حالت مستقل.

### Tests
- `backend/tests/test_v21_pairing_wizard.py` (۶ تست): کد یک‌بارمصرف + کلید اتصال، `/mobile/link` و ۴۰۱، بیکن UDP فقط به کلید خودش پاسخ می‌دهد، OAuth داخل برنامه (state، تبادل توکن stub، اعتبار Drive به گوشی می‌رسد)، قرارداد سورس اندروید (ویزارد، بدون «دقیقه» در لودینگ، بدون فیلد تاریخ تایپی، نام جدید، پشتیبانی، بیکن)، CSS/JS بدون برش QR. کل مجموعه: **۳۳۶ passed / 1 skipped**.

### Release
- `releases/android/SupermarketMobile-2.1.0.apk` (versionCode 20100، برچسب «سوپری من»، ۵۸۰ KB) + `.sha256`.

## [2.0.0] - 2026-09-09 (اپ اندروید کاملاً بومی — بدون وب‌ویو/PWA)

### Changed (breaking for the phone app only)
- **بازنویسی کامل اپ اندروید به‌صورت Native** (`mobile-android/app/src/main/java/ir/khajavy/supermarket/`، جاوا، ویوهای برنامه‌نویسی‌شده، بدون Gradle، فقط `android.jar` + ZXing vendored): هیچ WebView، هیچ HTML/JS و هیچ PWA در APK نیست (`assets/` فقط قلم Vazirmatn TTF است). گوشی **فقط داده** با رایانه رد و بدل می‌کند (JSON روی LAN).
  - `Api.java` — کلاینت HTTP بومی (Bearer، PATCH از طریق `X-HTTP-Method-Override`، آپلود چندبخشی، fallback به کش آفلاین).
  - `Db.java` — SQLite روی گوشی: کالا/بچ/مشتری (کاتالوگ کامل)، فاکتورهای محلی `M-000001`، صف عملیات، تعارض‌ها، کش پاسخ‌ها. جست‌وجو/اسکن/فروش/ورود/تعریف کالا و مشتری همه آفلاین کار می‌کنند (شناسه‌های موقت منفی، `INT-L…`).
  - `Sync.java` — همگام‌سازی دوطرفهٔ خودکار: هنگام باز شدن، هر ۲۰ ثانیه، بعد از هر ثبت و پس از بازگشت اتصال؛ push صف به `/api/mobile/sync` (idempotent) و pull تغییرات رایانه با cursor؛ نگاشت فاکتور محلی به شمارهٔ رسمی؛ موارد ردشده در بخش «همگام‌سازی».
  - `Ui.java` + `Jalali.java` — سیستم طراحی RTL آرام (تیره/روشن، بدون نئون)، کارت/KPI/شیت پایین/چیپ، اعداد فارسی، تقویم جلالی (پورت `frontend/jalali.js`؛ آزمون JVM: ۱۴۰۵/۰۶/۱۸ ↔ 2026-09-09، ۱۴۰۳/۱۲/۳۰ کبیسه، ۱۴۰۴/۱۲/۳۰ نامعتبر).
  - `AppActivity` (هدر با ☰، نقطهٔ وضعیت اتصال/صف، دکمهٔ «?» راهنما، نوار ۵ تبی، کشوی همهٔ بخش‌ها با فیلتر دسترسی، پشتهٔ صفحات، Back اندروید)، `PairActivity` (اسکن QR `SMKT:`، آدرس دستی + ورود، **حالت مستقل**؛ پشتیبانی `smkt://`)، `LoginActivity` (ورود با کاربر فروشگاه ← توکن دستگاه)، `ScanActivity` (همان اسکنر Camera2+ZXing v1.8.1).
  - **۲۷ بخش بومی**: داشبورد ۱۲ بلوکه (با نمودار روند ۷ روز)، صندوق (viewport ثابت، اسکن/جست‌وجو، FEFO و انتخاب بچ، مقدار اعشاری، تخفیف ردیف/فاکتور، کوپن، مشتری آزاد/ثبت‌شده، نگه‌داشتن تا ۱۰ فاکتور با زمان و بازگردانی، پرداخت نقدی/کارت/کارت‌به‌کارت/نسیه با باقی‌مانده، **بدون هیچ محاسبهٔ سود**)، نگه‌داشته‌ها، فاکتورها (جزئیات، رسید، چاپ روی رایانه، ابطال با رمز مدیر، مرجوعی هر قلم)، مشتریان (بدهکاران، پرونده، دفتر حساب، تسویهٔ جزئی/کامل، اصلاح دستی، پیامک یادآوری، ویرایش)، جشنواره/کوپن/آمار، کالاها (جزئیات، بچ‌ها، تاریخچهٔ قیمت، قیمت سریع، ویرایش/حذف، واحدها، دسته/برند)، ورود کالا، انبار (همه/کمبود/انقضا)، انبارگردانی (ایجاد/شروع/اسکن‌وشمارش/ردیف بعدی/پایان/اختلاف/تأیید/لغو، آفلاین در صف)، ضایعات/اصلاح/انتقال، انبارها و قفسه‌ها، گردش موجودی، ۱۰ گزارش، حسابداری ۱۰ تب (صندوق روز باز/بستن، هزینه، تأمین‌کننده و پرداخت، چک وصول/برگشت، دفتر روزنامه و سند معکوس، تراز، سود و زیان، ترازنامه، حساب‌ها)، کاربران/نقش‌ها، لاگ حسابرسی با فیلتر، تنظیمات ۲۱ دسته (ویرایش هر کلید)، مشخصات فروشگاه و لوگو، پیامک (صف/الگو/دستی)، سخت‌افزار (وضعیت، تست چاپ/کشو، ثبت دستگاه)، تست اتصالات (اجرای واقعی + تاریخچه + صف کارها)، پشتیبانی (تیکت، گفت‌وگوی دوطرفه، پیوست عکس)، لایسنس، همگام‌سازی، ابر، تنظیمات دستگاه، درباره.
  - **راهنمای گام‌به‌گام** برای هر ۲۷ بخش: خودکار در اولین بازدید، همیشه از دکمهٔ «?» قابل پخش مجدد؛ «پخش دوبارهٔ همهٔ راهنماها» در تنظیمات دستگاه.
- `scripts/android/build-apk.sh` دیگر پنل وب را در APK بسته‌بندی نمی‌کند (فقط قلم‌ها)؛ `-encoding UTF-8`. حجم APK: ۵۴۴ KB. پنل وب `/m` برای مرورگر همچنان از رایانه سرو می‌شود.

### Added
- Backend: middleware `X-HTTP-Method-Override: PATCH` (فقط روی POST) پیش از license gate.
- `/api/mobile/sync`: pull با فیلدهای لازم گوشی (`brand_id`, `min_stock_alert`, `has_own_barcode`, `sell/consumer/buy_price`, `last_name`, `credit_limit`)؛ **آشتی‌دهی فروش آفلاین با قیمت قدیمی**: اگر جمع رایانه با مبلغ پرداختی گوشی فرق داشته باشد، فروش با جمع رایانه ثبت و `adjusted{phone_total,pc_total}` برگردانده می‌شود (گوشی آن را در «موارد ردشده/تعارض» نشان می‌دهد) به‌جای دورانداختن فروشی که پولش گرفته شده؛ نتیجهٔ `CUSTOMER_CREATE` اکنون `customer_id` واقعی دارد.
- آزمون‌ها: `backend/tests/test_v20_native_android.py` (بدون WebView و همهٔ بخش‌ها روت‌شده، هر مسیر API که جاوا صدا می‌زند در OpenAPI هست، method-override، فیلدهای pull، آشتی‌دهی + idempotency + کسر موجودی یک‌بار). مجموع: **330 passed, 1 skipped**.

### Fixed
- متن «درباره» دیگر «اپ موبایل PWA» نمی‌گوید.

## [1.9.0] - 2026-09-09 (اندروید = تمام امکانات ویندوز، با پوستهٔ موبایل)

### Added
- **برنامهٔ اندروید اکنون همان پنل کامل ویندوز است** (`frontend/index.html` + `app.js` + `accounting.js` داخل APK): هر ۱۶ بخش — داشبورد، صندوق، کالاها، ورود کالا، انبار/انبارگردانی، فاکتورها، مشتریان، جشنواره و کوپن، گزارش‌ها، حسابداری، سخت‌افزار، کاربران، تنظیمات، تست اتصالات، پشتیبانی، لاگ‌ها — بدون هیچ کپی کد و با همان API رایانهٔ فروشگاه. هر تغییری (کالا، فاکتور، مشتری…) در هر طرف ثبت شود، همان لحظه در طرف دیگر دیده می‌شود (منبع واحد = پایگاه‌دادهٔ رایانه).
- **پوستهٔ موبایل** (`frontend/mobile-shell.css/js`): نوار پنج‌تبی پایین، کشوی «همهٔ بخش‌ها» با گروه‌بندی، نوار عنوان چسبان با ☰، جدول‌ها → کارت‌های برچسب‌دار، مودال‌ها → bottom-sheet، دکمهٔ شناور اسکنر بومی (به مسیر اسکن همان صفحه می‌رود: صندوق/کالا/ورود کالا/انبار)، صندوق تک‌ستونه با ترتیب انگشتی (اسکن ← سبد ← مشتری/کوپن ← جمع ← پرداخت)، دکمهٔ Back اندروید: بستن کشو/شیت ← خانه ← تأیید خروج. راهنمای گام‌به‌گام روی گوشی به نوار پایین اشاره می‌کند.
- **اتصال خودکار هنگام باز شدن**: گوشی جفت‌شده مستقیماً با توکن جفت‌سازی وارد پنل می‌شود (بدون لاگین مجدد، بدون صفحهٔ بارگذاری دوم). اگر رایانه در دسترس نباشد (۳ probe ناموفق `/health` یا در صفحهٔ ورود) خودکار به **حالت سریع گوشی** (`/mobile/`، آفلاین/محلی v1.8.1) می‌رود و با بازگشت رایانه صف را همگام می‌کند. از کشو هم «حالت سریع گوشی» و «اتصال دوباره (QR)» در دسترس است.
- چک‌لیست کامل اندروید: `docs/ANDROID_CHECKLIST.md`. آزمون E2E `scripts/uitest/phone-full-panel.js` (۳۲ بررسی: ورود خودکار، ۱۶ بخش، اسکن←سبد، نگه‌داشتن/بازگردانی، پرداخت←فاکتور رایانه، کالا/رسید/مشتری گوشی→رایانه، کالا رایانه→گوشی، Back).
- آزمون headless در ویوپورت 390×844 با پل اندروید شبیه‌سازی‌شده: ۱۶ بخش بدون خطا و بدون سرریز افقی؛ اسکن بومی ← سبد؛ ثبت کالا از گوشی ← بلافاصله در API رایانه (`docs/screenshots/v19_phone_*.png`).

## [1.8.1] - 2026-09-09 (اسکنر بومی اندروید، برنامهٔ مستقل گوشی، منوی پیشنهاد صندوق)

### Added
- **اسکنر بومی اندروید** (`mobile-android/.../ScanActivity.java`): Camera2 + ImageReader (YUV 1280×720) + **ZXing 3.5.3 جاوا** (Apache-2.0، vendored در `mobile-android/third_party/zxing-core/`) روی صفحهٔ Y بدون کپی؛ ابتدا نوار میانی چرخیده، بعد نوار بدون چرخش، بعد کل فریم؛ فوکوس پیوسته، چراغ‌قوه، بوق+لرزش، بررسی checksum برای EAN/UPC. پل JS: `SupermarketAndroid.hasNativeScanner()/scan(title)` → `window.__nativeScan(code, format)`؛ در همهٔ نقاط اسکن (صندوق، انبار، شمارش، جفت‌سازی QR) استفاده می‌شود و در مرورگر همان مسیر وب قبلی می‌ماند. رمزگشایی روی JVM: ~۳ ms/فریم برای EAN-13 نمونه.
- **برنامهٔ اندروید مستقل (بدون رایانه/بدون شبکه)** (`frontend/mobile/local.js`): نسخهٔ کامل کالاها/بچ‌ها/مشتری‌ها در IndexedDB گوشی؛ جستجو و اسکن، فروش (کسر موجودی محلی + فاکتور محلی `M-000001`)، ورود کالا، **تعریف کالای جدید از روی گوشی** (شناسهٔ موقت منفی/کد `INT-L…`) همه آفلاین کار می‌کنند و در صف عملیات می‌نشینند؛ با اولین اتصال (LAN یا Drive) روی رایانه بازپخش می‌شود: نوع جدید `PRODUCT_CREATE` (هم‌کد ⇒ بازاستفاده؛ `INT-L` ⇒ کد داخلی واقعی روی رایانه)، خطوط فروش فقط با بارکد resolve می‌شوند، فاکتور محلی به شمارهٔ رسمی نگاشت می‌شود. گزینهٔ «شروع مستقل روی همین گوشی» در صفحهٔ اتصال + دکمهٔ «اتصال به رایانهٔ فروشگاه» در تنظیمات دستگاه. وضعیت «آنلاین» یعنی پاسخ `/health` رایانه (نه صرفاً اینترنت). داشبورد آفلاین با فروش امروزِ گوشی/تعداد کالا/آخرین دریافت.
- آزمون `test_v181_offline_product_then_sale_by_barcode_replays` + اجرای سرتاسری headless: همگام → آفلاین → جستجو/اسکن/فروش/کالای جدید → آنلاین → صف خالی، `M-000001 → INV-…-000001`، موجودی بچ ۲۰→۱۹ (`docs/screenshots/v181_mobile_offline_*.png`).

### Fixed
- **منوی پیشنهاد کالا در صندوق** دیگر پشت جمع‌ها/دکمه‌ها پنهان نمی‌شود: شناور روی ستون (absolute, z-index 40, تا ۶+ ردیف با اسکرول داخلی) — `docs/screenshots/v181_pos_suggest.png`.

## [1.8.0] - 2026-09-09 (اسکن سریع اندروید، صندوق بدون اسکرول، گفتگوی دوطرفه با پشتیبانی، منطقهٔ زمانی تهران، راهنمای گام‌به‌گام)

### Added
- **گفتگوی دوطرفه با پشتیبانی** (`support_messages`, مهاجرت `a1b2c3d4e5f6`): پاسخ اپراتور در ربات پشتیبانی به‌صورت خودکار به داخل برنامه می‌رسد (poller هر ۲۰ ثانیه `getUpdates` با `offset_id` ذخیره‌شده؛ دکمهٔ «بررسی پاسخ جدید» هم هست). **مسیریابی دقیق برای صدها نصب**: هر پیام خروجی سرآیند `TCK-000123@INSTALLID — نام فروشگاه #INSTALLID` دارد (شناسهٔ ۸ رقمی پایدار از HWID)؛ پاسخ با **Reply** روی همان پیام (`reply_to_message_id`) یا با نوشتن `TCK-000123@INSTALLID` در ابتدای متن به تیکت درست می‌رسد و پیام‌های فروشگاه‌های دیگر نادیده گرفته می‌شود. **پیوست دوطرفه**: فایل اپراتور با `getFile` دانلود و زیر `media/support/` ذخیره می‌شود؛ پیوست فروشگاه (تصویر/PDF/لاگ/ZIP تا ۵۰ مگابایت) با `requestSendFile` → آپلود چندبخشی → `sendFile` در همان رشته ارسال می‌شود. اعلان `SUPPORT_REPLY`، نشان قرمز تعداد پاسخ‌های خوانده‌نشده روی منوی «درخواست پشتیبانی» (+ صدای ملایم)، مودال گفتگو در ویندوز و صفحهٔ گفتگو (💬) در گوشی. API: `GET/POST /api/support/tickets/{id}/messages` (multipart: `text`, `file`), `POST /api/support/poll`, `GET /api/support/unread`.
- **راهنمای گام‌به‌گام برای همهٔ بخش‌ها** (`frontend/tour.js`, `frontend/mobile/tour.js`): ۱۶ بخش دسکتاپ (داشبورد، صندوق، کالاها، ورود کالا، انبار، فاکتورها، مشتریان، جشنواره، گزارش‌ها، حسابداری، سخت‌افزار، کاربران، تنظیمات، تست اتصالات، پشتیبانی، لاگ‌ها) و صفحات اصلی گوشی؛ **در اولین بازدید هر بخش خودکار اجرا می‌شود** (به‌ازای هر کاربر ذخیره می‌شود) و دکمهٔ همیشگی «راهنما» در نوار بالا (گوشی: «?») آن را دوباره پخش می‌کند؛ نورافکن روی عنصر، کلیدهای ← / → / Esc، جای‌گذاری هوشمند (پایین/بالا/کنار)، گام‌هایی که عنصرشان برای نقش کاربر پنهان است حذف می‌شوند.
- **اسکن سریع بارکد در اندروید**: `BarcodeDetector` بومی در صورت وجود و در غیر این صورت **ZXing** (Apache-2.0، vendored در `frontend/mobile/vendor/zxing.min.js`) روی فریم‌های دوربین (نوار میانی، ۸۰۰px، TRY_HARDER، همهٔ فرمت‌های خرده‌فروشی + QR)؛ دوربین پشت 1280×720، فوکوس پیوسته، زوم ۱٫۵×، دکمهٔ چراغ‌قوه، بوق و لرزش، بررسی checksum برای EAN/UPC. آزمون `scripts/uitest/zxing-decode.js`: EAN-13 واقعی در ۰٫۷ ms/فریم (کوچک/تار و چرخیدهٔ ۱۲° هم رمزگشایی می‌شود).
- `tzdata` در وابستگی‌ها (منطقه‌های زمانی روی ویندوز بدون پایگاه سیستم‌عامل).

### Changed
- **منطقهٔ زمانی پیش‌فرض `Asia/Tehran` (+03:30)** به‌جای UTC در همه‌جا: «امروز»، گروه‌بندی روزانهٔ گزارش‌ها، تاریخ سند حسابداری فروش/خرید، هشدارهای انقضا، انبارگردانی، لایسنس، وضعیت‌نوار و تاریخ‌های نمایشی دسکتاپ/گوشی از `time.timezone` پیروی می‌کنند (ذخیرهٔ زمان همچنان UTC).
- **صندوق (POS)**: سود دیگر در سبد نمایش داده نمی‌شود؛ صفحه به اندازهٔ مانیتور قفل است (بدون اسکرول چرخ ماوس؛ در 1280×720، 1366×768 و 1920×1080 با Chromium اندازه‌گیری شد: `scrollHeight == clientHeight`)؛ **فاکتورهای نگه‌داشته‌شده** در نوار پایین با نام، تعداد قلم، مبلغ و ساعت فهرست می‌شوند و با یک کلیک بازمی‌گردند (تا ۱۰ فاکتور).
- گوشی: `m_tz` هنگام همگام‌سازی از `/settings/time` گرفته می‌شود.

### Fixed
- سند حسابداری فروش‌های بعد از ساعت ۲۰:۳۰ UTC (۰۰:۰۰ تهران) به روز قبل می‌خورد.
- آزمون‌ها: `date.today()` → `local_today()`؛ ۳۲۴ آزمون بک‌اند سبز (+۲ آزمون گفتگو/پیوست)، `smoke-v1_8.js` (۱۰۰ بررسی راهنما + صندوق + گفتگو)، `smoke-android.js` (گفتگو، ZXing، راهنما).

## [1.7.0] - 2026-09-08 (درخواست پشتیبانی، همگام‌سازی اینترنتی اختیاری، برابری کامل امکانات در گوشی)

### Added
- **درخواست پشتیبانی داخل برنامه** (منوی «درخواست پشتیبانی» در ویندوز و «بیشتر → پشتیبانی» در گوشی): ۷ نوع درخواست (ثبت خرابی، درخواست امکان ویژه، سؤال، سخت‌افزار، لایسنس، آموزش، سایر)، ۴ اولویت، شرح، راه تماس، **موقعیت مکانی دقیق دستگاه** (فقط با تیک کاربر؛ روی اندروید مجوز `ACCESS_FINE_LOCATION` هنگام نیاز پرسیده می‌شود)، مشخصات نسخه/دستگاه/فروشگاه. تیکت‌ها **همیشه ابتدا محلی ذخیره می‌شوند** (`support_tickets`, شمارهٔ `SUP-000001`) و صف پس‌زمینه با تلاش مجدد آن‌ها را از طریق کانال رله به صندوق پشتیبانی تولیدکننده می‌فرستد (`SENT` / `در انتظار ارسال مجدد` / `بسته‌شده`; دکمهٔ «ارسال دوباره»). اگر گوشی به رایانه نرسد، درخواست در صف آفلاین گوشی می‌ماند و با اولین اتصال از طریق `POST /api/mobile/sync` (نوع `SUPPORT_TICKET`) ثبت و ارسال می‌شود. API: `GET /api/support/types|tickets|status`, `POST /api/support/tickets`, `POST /api/support/tickets/{id}/resend|close`. مستند: `docs/SUPPORT.md`.
- **همگام‌سازی اینترنتی اختیاری (Google Drive — پوشهٔ خصوصی برنامه)** برای وقتی گوشی و رایانه روی یک وای‌فای نیستند: تنظیمات → «همگام‌سازی ابری»؛ اتصال با **OAuth Device Flow** (کد کوتاه را در مرورگر وارد می‌کنید؛ هیچ رمزی در برنامه تایپ نمی‌شود)، اسکوپ فقط `drive.appdata` (برنامه به بقیهٔ Drive دسترسی ندارد)، پیش‌فرض **خاموش**؛ رایانه هر ۵ دقیقه snapshot کالا/بچ/مشتری + بکاپ `backup-latest.db` را بالا می‌گذارد و فایل‌های عملیات گوشی‌ها را به همان مسیر `mobile._apply` (idempotent) اعمال می‌کند؛ گوشی وقتی رایانه در دسترس نباشد خودکار به همان حساب می‌رود (اعتبارنامه در QR جفت‌سازی نسخهٔ ۲). بدون حساب/پرداخت اجباری: ویژگی کاملاً اختیاری است و بدون آن همه‌چیز مثل قبل روی LAN کار می‌کند. API: `GET /api/cloud/status`, `POST /api/cloud/connect/start|poll`, `POST /api/cloud/sync-now`, `POST /api/cloud/disconnect`. مستند: `docs/CLOUD_SYNC.md`.
- **برابری امکانات در گوشی** (`frontend/mobile/app-more.js`, منوی «بیشتر»): فاکتورهای معلق (نگه‌داشتن/بازکردن تا ۱۰ فاکتور در صندوق گوشی)، کالاها (جست‌وجو/بارکد/قیمت سریع)، عملیات انبار (ضایعات/اصلاح/انتقال بین انبارها/گردش)، انبارها، مشتری‌ها و بدهکاران (تسویه/یادآوری بدهی)، فاکتورها و مرجوعی، کمپین‌ها و کوپن‌ها، گزارش‌ها (فروش/سود/کم‌موجود/انقضا/صندوقدار)، حسابداری (خلاصه/هزینه/چک/تأمین‌کننده/شیفت صندوق)، کاربران، مشخصات فروشگاه، پیامک، سخت‌افزار (تست چاپ/کشو)، لایسنس، رویدادها (audit)، دربارهٔ برنامه، پشتیبانی، ابر، تنظیمات (پوستهٔ روشن/تیره). همهٔ ۲۴ صفحه در `smoke-android.js` رندر و بدون خطای اجرا تأیید می‌شوند.
- **QR جفت‌سازی در خود مرورگر** (`frontend/vendor-qrcode.js`, MIT) — دیگر به Pillow روی رایانه وابسته نیست؛ QR به‌صورت SVG رسم می‌شود و در آزمون `smoke-v1_7.js` با jsQR رمزگشایی و با تجزیه‌گر گوشی (`SMKT:`) تطبیق داده می‌شود. کد متنی برای ورود دستی همیشه هست.
- **صفحهٔ «اتصال گوشی» یک‌بار پس از اولین ورود** (بعد از ویزارد): QR + آدرس‌های LAN + آدرس نسخهٔ وب موبایل، با دکمهٔ «بعداً از تنظیمات».
- تست‌ها: `test_v1_7_support.py` (۵)، `test_v1_7_cloud.py` (۳؛ سرور Drive شبیه‌سازی‌شده)، `smoke-v1_7.js` (پشتیبانی + QR واقعی + پنل ابر + صفحهٔ اتصال)، `smoke-android.js` گسترش‌یافته (۲۴ صفحه، فاکتور معلق، تیکت با موقعیت از گوشی). مجموع: **۳۲۳ تست backend**.

### Changed
- QR جفت‌سازی نسخهٔ ۲ (`v:2`) — سازگار با گوشی‌های ۱.۶ (فیلدهای اضافه نادیده گرفته می‌شود).
- `lan_addresses()` آدرس‌های `169.254.*` (APIPA) را حذف می‌کند.
- Service Worker cache v11؛ `app-more.js` و `vendor-qrcode.js` در پوستهٔ آفلاین.
- Worker همگام‌سازی ابری اولین اجرا را یک بازه (۵ دقیقه) عقب می‌اندازد تا با راه‌اندازی/خوددرمانی اسکیمای پایگاه داده هم‌زمان نشود.
- نسخه در ۵ فایل → 1.7.0؛ Android `versionCode 10700`.

### Fixed
- صفحهٔ «همگام‌سازی» گوشی برای تعارض‌های نوع «عملیات» (فروش/ورود کالای ردشده) فیلدهای شمارش را با `undefined` نشان می‌داد؛ اکنون پیام سرور و دکمهٔ حذف نمایش داده می‌شود.
- فایل موقت `scripts/dbg_tmp.js` از مخزن حذف شد.

### وضعیت صداقت (§57)
- تحویل واقعی تیکت به صندوق پشتیبانی روی کانال رلهٔ واقعی و همگام‌سازی با حساب Google واقعی: **NOT VERIFIED** در سندباکس (اینترنت خروجی محدود)؛ هر دو مسیر با سرور شبیه‌سازی‌شده آزمون شده‌اند و خطاها به‌صورت `FAILED/در انتظار ارسال مجدد` ثبت می‌شوند، نه پنهان.
- نصب APK روی دستگاه فیزیکی و اجرای Setup.exe روی ویندوز: مانند نسخه‌های قبل **NOT VERIFIED** (CI ویندوز/اندروید و ساخت SDK-less).

## [1.6.0] - 2026-09-08 (فاکتورهای معلق، خروج امن، اندروید با همگام‌سازی آفلاین، افق آفلاین لایسنس)

### Added
- **صندوق — نگه‌داشتن فاکتور (تا ۱۰ فاکتور همزمان)**: کلید `F6` یا دکمهٔ «نگه‌دار» فاکتور جاری را به **نوار پایین همان پنجره** می‌فرستد (بدون سربرگ/پنجرهٔ جدید)؛ هر کارت نام مشتری/تعداد اقلام/مبلغ را نشان می‌دهد و با یک لمس دوباره باز می‌شود؛ سقف ۱۰ فاکتور با پیام فارسی؛ در `localStorage` می‌ماند و با خاموش‌شدن برنامه از بین نمی‌رود.
- **دکمهٔ خروج** در سایدبار + `POST /api/system/shutdown`: لودینگ خروج («پشتیبان‌گیری… ذخیرهٔ تنظیمات… بستن»)، **بکاپ خودکار** در `data/backups/`, سپس بستن سرور و پنجره (`SUPERMARKET_ALLOW_SHUTDOWN=1` که راه‌انداز ویندوز خودش ست می‌کند).
- **تمام‌صفحه** هنگام اجرا (پنجرهٔ اختصاصی WebView2/Edge و `requestFullscreen` در وب) و **جای‌گیری ریسپانسیو** (grid های خودتنظیم، عدم سرریز افقی در ۱۰۲۴×۷۶۸ تا ۱۹۲۰×۱۰۸۰).
- **صداها**: جینگل خوش‌آمد بی‌کلام کوتاه هنگام شروع، صدای مخصوص نوتیفیکیشن، صدای مخصوص ابطال فاکتور، نگه‌داشتن/بازکردن فاکتور، خروج؛ بلندی پیش‌فرض ۰٫۳۵ و همهٔ پاکت‌ها نرم (بدون کلیک تیز).
- **لایسنس**: اگر بررسی ۲۴ ساعته شکست بخورد ولی تاریخ انقضای معتبر می‌دانیم، برنامه **تا همان تاریخ** کار می‌کند؛ پس از انقضا/ابطال، برنامه کامل قفل و **دادهٔ کاربر در یک ZIP رمزنگاری‌شده (AES-256)** در `data/vault/` نگه داشته می‌شود (`services/license.py: lock_vault`).
- **اندروید (نسخهٔ ۱.۶)** — `mobile-android/`: پوستهٔ WebView بدون وابستگی (framework-only، minSdk 24) که اپ موبایل را از `assets/www` در مبدأ خصوصی `https://app.local` اجرا می‌کند؛ صفحهٔ **اتصال** (`frontend/mobile/setup.html`): اسکن QR `SMKT:` با دوربین یا ورود دستی آدرس، آزمایش `/health`؛ پل JS `window.SupermarketAndroid` (pair/unpair/version/exit).
- **جفت‌سازی از ویندوز**: تنظیمات → «موبایل (اندروید)» و مرحلهٔ ویزارد: QR حاوی آدرس‌های LAN + توکن دستگاه (`GET /api/mobile/pair/info`, `POST /api/mobile/pair/token`, مدیریت دستگاه‌ها).
- **همگام‌سازی آفلاین دوطرفه بدون اینترنت**: در گوشی، فروش/ورود کالا/شمارش/مشتری هنگام قطع ارتباط در IndexedDB (`ops`) صف می‌شوند و با اولین اتصال Wi‑Fi به رایانه، `POST /api/mobile/sync` آن‌ها را اعمال می‌کند (idempotent با شناسهٔ عملیات؛ نتایج `APPLIED/DUPLICATE/REJECTED`؛ ردشده‌ها در «تعارض‌ها» با پیام سرور) و کالاها/بچ‌ها/مشتری‌ها را به گوشی برمی‌گرداند. صفحهٔ «همگام‌سازی» زمان آخرین همگام‌سازی و دکمهٔ دستی دارد؛ «قطع اتصال از این رایانه» در تنظیمات.
- **ساخت APK بدون Android SDK** (`scripts/android/fetch-tools.sh`, `build-apk.sh`): aapt2 + ecj + d8 + apksigner از مخازن عمومی؛ خروجی `SupermarketMobile-<ver>.apk` امضاشده (v2/v3) + `.sha256`، قابل نصب با هر نصب‌کنندهٔ APK (مثلاً Files/Obtainium/F-Droid Basic).
- تست‌ها: `test_v1_6_*` (shutdown/backup، vault، pair/sync)، `smoke-v1_6.js` (فاکتور معلق/خروج/صداها), `smoke-android.js` (پل اندروید، CORS `https://app.local`، صف آفلاین → sync).

### Changed
- راه‌انداز ویندوز روی `0.0.0.0` گوش می‌دهد (`SUPERMARKET_HOST` برای تغییر) تا گوشی در LAN وصل شود؛ CORS شامل `https://app.local`.
- تست «مهلت آفلاین ۷ روزه» → «مجاز تا تاریخ انقضا، سپس قفل» (۷ روز فقط وقتی تاریخ انقضا ناشناخته باشد).
- نسخه در ۵ فایل → 1.6.0؛ `build.gradle` نسخه را از `backend/app/__init__.py` می‌خواند؛ Service Worker cache v8.

## [1.5.1] - 2026-09-08 (اصلاح لودینگ، افکت‌های صوتی، پالت آرام‌تر)

### Fixed
- **لودینگ ۴۵ دقیقه‌ای فقط یک بار و فقط داخل ویزارد راه‌اندازی** نمایش داده می‌شود؛ `POST /api/setup/complete` پرچم `setup.first_loading_done` را ست می‌کند و هر ورود بعدی (حتی پس از نصب مجدد روی دیتابیس قبلی) فقط لودینگ **۲ دقیقه‌ای** دارد (`onboarding.js`: `afterLogin` همیشه `120s`؛ `GET /api/setup/status` → `loading_seconds=120`, `install_loading_seconds=2700`).
- روی صفحهٔ لودینگ هیچ متنی از «دقیقه»/زمان تخمینی نوشته نمی‌شود («لطفاً صبر کنید»).
- **خوانایی پوستهٔ روشن**: متغیرهای `--text/--muted` و رنگ کارت‌ها/سایدبار در حالت روشن صریحاً تعریف شدند (قبلاً بلوک `:root` انتهایی آن‌ها را بازنویسی می‌کرد).

### Added
- **افکت‌های صوتی** (`frontend/sfx.js`, Web Audio؛ بدون فایل خارجی): فروش موفق، افزودن به سبد، خطا، ورود، هشدار بحرانی، مراحل ویزارد/فعال‌سازی، پایان لودینگ، نصب. کارت «صداها» در تنظیمات → ظاهر (روشن/خاموش، بلندی، تست هر صدا؛ ذخیره در `localStorage`).
- **پالت آرام‌تر**: سبز/فیروزه‌ای ملایم‌تر، حذف نئون و کاهش شدت glow/text-shadow در پوستهٔ تیره؛ اسکرین‌شات‌های `docs/screenshots/v151-*.png` (روشن و تیره).

### Changed
- نسخه در ۵ فایل → 1.5.1؛ `sw.js` cache v7؛ smoke `scripts/uitest/smoke-v1_5.js` (بررسی لودینگ کوتاه، نبود واژهٔ «دقیقه»، بارگذاری Sfx).

## [1.5.0] - 2026-09-08 (لایسنس آنلاین، ویزارد راه‌اندازی اولیه، لودینگ نصب، نوتیفیکیشن انقضا)

### Added
- **لایسنس آنلاین** (`services/license.py`, `routers/setup.py`, `docs/LICENSING.md`): فعال‌سازی با Worker مالک (`?api_action=validate&key=&hwid=`)، ذخیرهٔ نتیجه (نوع/مالک/انقضا/HWID) در `system_settings`، **بررسی مجدد هر ۲۴ ساعت** (thread پس‌زمینه + هنگام هر ورود)، تحمل ۷ روز قطعی اینترنت، قفل کامل برنامه بدون لایسنس (middleware → `402 LICENSE_REQUIRED`؛ UI صفحهٔ فعال‌سازی را نشان می‌دهد)، پیام‌های سرور (`INVALID`, `MAX_DEVICES_REACHED`, …) عیناً نمایش داده می‌شوند. زبانهٔ «لایسنس» در تنظیمات (وضعیت، نوع، مالک، انقضا، روز باقی‌مانده، HWID، آخرین بررسی، بررسی اکنون، تغییر کلید).
- **ویزارد راه‌اندازی اولیه** (`frontend/onboarding.js`): ۱۰ مرحلهٔ گرافیکی و متحرک — خوش‌آمد، لایسنس (اجباری)، نام فروشگاه، لوگو (drag&drop)، اطلاعات تماس، واحد پول، پوسته + عرض کاغذ، بانک اولیهٔ کالا (اختیاری)، حساب مدیر (نام کاربری/رمز؛ اجباری)، خلاصه؛ هر مرحلهٔ غیرضروری قابل «رد کردن». فقط بار اول نمایش داده می‌شود (`setup.done`).
- **صفحهٔ لودینگ نصب**: بار اول ~۴۵ دقیقه (حلقهٔ پیشرفت، ۱۳ فاز، لاگ زنده، زمان باقی‌مانده)؛ ورودهای بعدی ~۲ دقیقه با بررسی اجباری لایسنس؛ در حین لودینگ تنظیمات واقعاً اعمال می‌شوند.
- **نوتیفیکیشن پایین صفحه** (`GET /api/setup/alerts`): کالاهای در آستانهٔ انقضا با «X ساعت مانده / X روز مانده»، و شمارش معکوس لایسنس از ۱۴ روز قبل؛ قابل بستن، لینک به انبار/تنظیمات، به‌روزرسانی هر ۵ دقیقه.
- `SUPERMARKET_LICENSE_GATE`, `SUPERMARKET_LICENSE_SERVER` (فقط آزمون). اسموک `smoke-v1_5.js` + stub محلی `scripts/uitest/license_stub.py`؛ ۱۴ اسکرین‌شات `docs/screenshots/v15-*.png`.

### Changed
- `frontend/app.js`: boot/login از طریق `Onboarding.gate/afterLogin`؛ پاسخ 402 → صفحهٔ لایسنس. Service Worker cache v6. اپ موبایل پیام 402 را فارسی نشان می‌دهد.
- نسخه → 1.5.0؛ **۳۱۱ آزمون** (`tests/test_v1_5_license_setup.py` جدید: فعال‌سازی/رد/شبکه/بررسی ۲۴ساعته/مهلت آفلاین/گیت/ویزارد/هشدارها).

## [1.4.2] - 2026-09-08 (رفع شکست مرحلهٔ ۵ ساخت نصب‌کننده: `ValueError: not enough values to unpack (expected 3, got 2)`)

### Fixed
- **PyInstaller در `EXE()` با `ValueError: not enough values to unpack (expected 3, got 2)` می‌ایستاد** (گزارش کاربر، مرحلهٔ «ساخت فایل اجرایی خودکفا»). ریشه در `installer/windows/app.spec`: خروجی `collect_data_files()/collect_dynamic_libs()` (فرمت ۲تایی هوک `(src, dest_dir)`) مستقیماً به `a.datas`/`a.binaries` (فرمت ۳تایی TOC `(dest, src, typecode)`) اضافه می‌شد. تا v1.3 این مسیر با `except: pass` پنهان بود؛ در v1.4.1 که pywebview اختیاری شد و نصب شد، خطا آشکار شد.
  - تابع `_as_toc()` ورودی‌های هوک را به TOC صحیح تبدیل می‌کند؛ جمع‌آوری فایل‌های pywebview/clr_loader فقط وقتی بسته نصب باشد انجام می‌شود (بدون try/except پنهان‌کننده).
  - `hiddenimports` مربوط به pywebview/pythonnet هم شرطی شدند (`_optional_desktop_hiddenimports()`) تا در نبود بسته، هشدار/خطای ماژول گم‌شده ندهد.
  - آزمون رگرسیون `tests/test_v1_4_2_spec_toc.py`: با PyInstaller واقعی ثابت می‌کند ۲تایی خام همان `ValueError` را می‌دهد و خروجی `_as_toc` در `normalize_toc` پذیرفته می‌شود.

### Changed
- نسخه → 1.4.2؛ ۳۰۲ آزمون.

## [1.4.1] - 2026-09-08 (رفع شکست ساخت نصب‌کننده: `No matching distribution found for pywebview`)

### Fixed
- **BUILD-SETUP.bat با خطای `Could not find a version that satisfies the requirement pywebview<6.0,>=5.0` / `Read timed out ... pypi.org` متوقف می‌شد** (گزارش کاربر با اسکرین‌شات). ریشه: pywebview در `requirements.txt` اصلی و اجباری بود و روی شبکه‌ای که به pypi.org نمی‌رسد، pip بعد از ۵ تلاش ۱۵ ثانیه‌ای شکست می‌خورد و کل ساخت با کد 1 می‌ایستاد.
  - pywebview/pythonnet به `backend/requirements-desktop.txt` (**اختیاری**) منتقل شدند؛ لانچر بدون آن‌ها به پنجرهٔ Edge app-mode می‌رود (رفتار موجود، اکنون با آزمون `test_launcher_falls_back_when_pywebview_missing`).
  - سازندهٔ ویندوز (`builder-lib.ps1`): هر فراخوانی pip با `--timeout 60 --retries 8`؛ در صورت شکست، تلاش خودکار با آینه‌های جایگزین (runflare، iranrepo، tuna، aliyun)؛ پشتیبانی از پوشهٔ wheel آفلاین `installer\windows\wheels\` (`--find-links`); نصب وابستگی‌های اختیاری پنجرهٔ بومی داخل try/catch با هشدار فارسی — **ساخت دیگر به‌خاطر pywebview متوقف نمی‌شود**.
  - CI ویندوز: نصب `requirements-desktop.txt` به‌صورت best-effort.
  - `installer/windows/README.md`: دو ردیف عیب‌یابی جدید (pywebview / Read timed out + راهکار آفلاین).

### Changed
- نسخه → 1.4.1؛ ۲۹۷ آزمون (`tests/test_v1_4_1_optional_desktop_deps.py` جدید).

## [1.4.0] - 2026-09-08 (حسابداری دوطرفه، داشبورد و صندوق نئون، شناسایی خودکار بارکدخوان)

### Added
- **ماژول حسابداری کامل دوطرفه** (`app/models/accounting.py`, `services/accounting.py`, `routers/accounting.py`، migration `e1f2a3b4c5d6`): کدینگ حساب‌ها (دارایی/بدهی/سرمایه/درآمد/هزینه، ۱۱۰۱ صندوق … ۶۱xx هزینه‌ها)، دفتر روزنامه با ثبت متوازن اجباری (`UNBALANCED` → 422)، ابطال/برگشت سند، دفتر کل هر حساب، تراز آزمایشی، صورت سود و زیان (بازهٔ شمسی)، ترازنامه، دورهٔ مالی و بستن دوره (`PERIOD_CLOSED` → 409)، دسته‌های هزینه و ثبت هزینه، تأمین‌کنندگان و پرداخت به آن‌ها، چک‌های دریافتی/پرداختی (وصول/برگشت)، شیفت صندوق (باز/بست با مغایرت).
- **ثبت خودکار اسناد**: هر فروش POS → بدهکار صندوق/بانک/کارت‌خوان/حساب مشتری، بستانکار فروش (+ مالیات) و سند بهای تمام‌شده (Dr 5101 / Cr 1301 موجودی)؛ هر ورود کالا → بدهکار موجودی، بستانکار حساب پرداختنی تأمین‌کننده یا صندوق/بانک/کارت (`paid_from`).
- **رابط حسابداری** (`frontend/accounting.js`): ۱۱ زبانهٔ فارسی (نمای کلی با KPI و گیج حاشیهٔ سود، دفتر روزنامه + سند دستی، دفتر کل، تراز آزمایشی، سود و زیان با نمودار میله‌ای و چاپ، ترازنامه، هزینه‌ها، چک‌ها با سررسید امروز/گذشته، تأمین‌کنندگان، شیفت صندوق، کدینگ، دوره‌ها). مجوزهای `accounting.view/post/close` (مدیر/سرپرست)؛ صندوق‌دار فقط شیفت خودش.
- **داشبورد جدید (نئون/شیشه‌ای)** مطابق طرح مرجع: گیج فروش امروز با مقایسهٔ دیروز، پرفروش‌ترین‌ها با حلقهٔ سهم فروش، کالاهای کم‌موجود، نمودار SVG روند فروش/سود ۷ روزه، تراکنش‌های اخیر، دسترسی سریع (ثبت هزینه، ورود کالا، انبارگردانی…)، شاخص‌های حسابداری، به‌علاوهٔ کارت‌های انقضا/بدهی مشتریان/پیامک/سیستم/قیمت. `GET /api/reports/dashboard` با `top_products`, `trend`, `accounting` گسترش یافت.
- **صندوق (POS) بازطراحی‌شده**: سبد به‌صورت کارت‌های کالا با تصویر، +/−، حذف؛ ستون کناری با فیلد اسکن، مشتری/کوپن، جمع‌ها، سود تخمینی، دکمه‌های بزرگ پرداخت (F2)/مشتری (F8)/کوپن (F9)/تخفیف (F4)/لغو (Esc)؛ سربرگ با نام فروشگاه، صندوق‌دار، ساعت و وضعیت شیفت.
- **بارکدخوان**: شناسایی خودکار بارکدخوان‌های USB (HID/سریال) در «سخت‌افزار» (`GET /api/hardware/scanner/discover`، پیشنهاد درایور سازنده)، تشخیص «ویج» اسکنر از سرعت تایپ (`POST /api/hardware/scanner/detect`) و آیکون وضعیت اسکنر روی فیلدهای بارکد دسکتاپ؛ اسکن با دوربین فقط در PWA موبایل.
- **ورود کالا**: انتخاب تأمین‌کننده و نحوهٔ پرداخت (نسیه/صندوق/بانک/کارت).
- `Jalali.setValue(el, iso)` برای مقداردهی برنامه‌ای فیلدهای تاریخ شمسی.

### Changed
- لایهٔ تم «نئون گلس» در `theme-pro.css`؛ Service Worker cache v5 (`accounting.js`).
- نسخه در backend / Inno / standalone / Android → 1.4.0. **۲۹۴ آزمون** (`tests/test_v1_4_accounting.py`, `tests/test_v1_4_dashboard_scanner.py` جدید)، اسموک UI جدید `scripts/uitest/smoke-v1_4.js`، ۷ اسکرین‌شات واقعی `docs/screenshots/v14-*.png`.

### Fixed
- محور عمودی نمودار روند داشبورد بریده نمی‌شود؛ شبکهٔ ۳ستونهٔ کارت‌ها (`grid-3`) تعریف شد.

## [1.3.0] - 2026-09-07 (تقویم شمسی، بازطراحی ورود کالا، زمان‌بندی انبارگردانی، پنجرهٔ بومی)

### Added
- **تقویم شمسی (Jalali) در همهٔ فیلدهای تاریخ** (`frontend/jalali.js`): انتخاب‌گر ماه/سال/روز فارسی، دکمهٔ «امروز»، تبدیل دوطرفه به ISO؛ در دسکتاپ و PWA موبایل (انقضای بچ، تاریخ شروع انبارگردانی، فیلترهای گزارش).
- **زمان‌بندی انبارگردانی** (`stocktake_sessions.scheduled_for`, `reminder_note`؛ migration `d9e4f5a6b7c8`): ایجاد جلسهٔ زمان‌بندی‌شده، هشدار «امروز/عقب‌افتاده» در نوار بالای انبار و داشبورد (`GET /api/inventory/stocktake/alarms`)، ویزارد شمارش قدم‌به‌قدم با تصویر کالا، قابل ادامه.
- **پنجرهٔ بومی ویندوز**: لانچر با pywebview/WebView2 (`open_native_window`) پنجرهٔ اختصاصی با آیکون برنامه باز می‌کند؛ در نبود WebView2 به Edge app-mode/مرورگر برمی‌گردد (`SUPERMARKET_BROWSER_MODE=system|edge-app`).
- تم حرفه‌ای `theme-pro.css` (روشن/تیره)، آیکون‌های SVG به‌جای ایموجی، Service Worker cache v4.

### Changed
- **ورود کالا** بازطراحی شد: پیشنهاد زنده هنگام تایپ بارکد/نام با تصویر، موجودی و آخرین قیمت؛ فیلد «قیمت تأمین‌کننده» حذف شد (قیمت خرید/مصرف‌کننده/فروش به ازای هر بچ).
- نسخه در backend / Inno / standalone / Android → 1.3.0. ۲۷۸ آزمون (`tests/test_v1_3_stocktake_schedule.py` جدید)، ۵ اسکرین‌شات جدید در `docs/screenshots/v13-*.png`.

## [1.2.6] - 2026-09-07 (رفع «کلیک می‌کنم، هیچ اتفاقی نمی‌افتد» در برنامهٔ نصب‌شده)

### Fixed
- **پس از نصب، اجرای برنامه هیچ پنجره و هیچ خطایی نشان نمی‌داد.** علت: فایل اجرایی ویندوز بدون کنسول ساخته می‌شود (`console=False`) و در این حالت `sys.stdout`/`sys.stderr` برابر `None` هستند. لانچر `print` و `logging.StreamHandler(sys.stdout)` داشت و uvicorn در پیکربندی پیش‌فرض لاگ خود `sys.stdout.isatty()` را صدا می‌زند → `AttributeError` داخل thread سرور که بی‌صدا بلعیده می‌شد؛ پروسه زنده می‌ماند اما سرور هرگز بالا نمی‌آمد و پنجره‌ای باز نمی‌شد. (سناریو با `sys.stdout=None` بازتولید و رفع شد.)
  - لانچر در شروع، جریان‌های استاندارد را به `logs\supermarket.log` متصل می‌کند؛ uvicorn با `log_config=None` اجرا می‌شود؛ هر خطای thread سرور ثبت و به کاربر با پیام فارسی (همراه مسیر لاگ) نشان داده می‌شود — دیگر هیچ شکست بی‌صدایی وجود ندارد.
  - **تک‌نمونه:** اگر برنامه از قبل در حال اجرا باشد، کلیک دوباره فقط پنجره را باز می‌کند (فایل `server.port`).
  - آزمون رگرسیون `tests/test_launcher_windowed.py`: لانچر با `stdout/stderr = None` باید سالم (`/health` = 200) بالا بیاید.

### Changed
- نسخه در backend / Inno / standalone / Android → 1.2.6. ۲۷۷ آزمون، ۱۵۸ عملیات API.

## [1.2.5] - 2026-09-07 (رفع کرش هنگام اجرای برنامهٔ نصب‌شده: «no such column: units.allow_decimal»)

### Fixed
- **برنامهٔ نصب‌شده بلافاصله با `sqlite3.OperationalError: no such column: units.allow_decimal` کرش می‌کرد.** علت: پایگاه دادهٔ موجود در `%USERPROFILE%\SupermarketSystem\supermarket.db` با نسخهٔ قدیمی‌تر (v0.x) ساخته شده بود؛ `create_all` فقط جدول‌های **غایب** را می‌سازد و هرگز ستون به جدول موجود اضافه نمی‌کند، و چون آن پایگاه داده جدول `alembic_version` نداشت، migrationها هم به‌جای اجرا فقط stamp می‌شدند. نتیجه: جدول `units` قدیمی ماند و اولین SELECT شکست خورد.
  - **خوددرمانی اسکیمای پایگاه داده (`_reconcile_schema`)**: در هر شروع، هر ستونی که در مدل‌ها هست ولی در پایگاه داده نیست، به‌صورت افزایشی (بدون حذف داده) با `ALTER TABLE ... ADD COLUMN` و مقدار پیش‌فرض امن اضافه می‌شود. بازتولید سناریو با پایگاه دادهٔ واقعی v0.4.0 → پس از رفع، ورود و همهٔ endpointها 200.
  - آزمون رگرسیون `tests/test_schema_reconcile.py` (حذف ستون → شروع → ستون برمی‌گردد؛ اسکیمای زنده هیچ ستون غایبی ندارد).
- **لانچر ویندوز (`run_supermarket.py`)**: به‌جای پنجرهٔ خام «Unhandled exception in script» PyInstaller، پیام فارسی خوانا با مسیر لاگ و پایگاه داده نمایش می‌دهد و traceback کامل را در `logs\supermarket.log` می‌نویسد.

### Changed
- نسخه در backend / Inno / standalone / Android → 1.2.5. ۲۷۶ آزمون، ۱۵۸ عملیات API.

## [1.2.4] - 2026-09-07 (رفع توقف مرحلهٔ ۵/۶ سازندهٔ نصب‌کننده روی اولین خط لاگ PyInstaller)

### Fixed
- **مرحلهٔ «ساخت فایل اجرایی خودکفا» بلافاصله با خطایی مثل `596 INFO: PyInstaller: 6.22.2` متوقف می‌شد.** علت: کتابخانهٔ ساخت با `$ErrorActionPreference = 'Stop'` اجرا می‌شود و در Windows PowerShell 5.1، `2>&1` هر خط stderr برنامهٔ بومی را در یک `NativeCommandError` می‌پیچد؛ زیر حالت Stop، همان اولین خط به استثنای terminating تبدیل می‌شود. PyInstaller **همهٔ** پیام‌های عادی پیشرفت خود را به stderr می‌نویسد، پس ساخت روی اولین خط لاگ (که اصلاً خطا نبود) قطع می‌شد. pip هم می‌توانست همین را ایجاد کند.
  - `Invoke-Native` اکنون فراخوانی برنامهٔ بومی را با `'Continue'` اجرا می‌کند، رکوردهای خطا را به متن ساده تبدیل و لاگ می‌کند، و **فقط کد خروج** معیار موفقیت/شکست است (همان‌طور که در توضیح تابع وعده داده شده بود).
- بررسی شد که همهٔ `hiddenimports` و `datas` فایل `app.spec` در محیط واقعی import/موجود هستند (uvicorn, app.*, sqlalchemy.dialects.sqlite, multipart, frontend, alembic, app/data).

### Changed
- نسخه در backend / Inno / standalone / Android → 1.2.4. محتوای برنامه بدون تغییر (۱۵۸ عملیات API، ۲۷۴ آزمون).

## [1.2.3] - 2026-09-07 (رفع ریشه‌ای: سازندهٔ نصب‌کننده همهٔ پایتون‌ها را «خراب» تشخیص می‌داد)

### Fixed
- **علت واقعی «پایتون نصب است ولی پیدا نمی‌شود» (حتی بعد از نصب خودکار و ریستارت):** بررسی نسخه با `python -c 'import sys;print("%d.%d.%d"%sys.version_info[:3])'` انجام می‌شد. Windows PowerShell 5.1 هنگام اجرای برنامه‌های بومی، نقل‌قول‌های دوتایی داخل آرگومان را escape نمی‌کند؛ پایتون عملاً `print(%d.%d.%d%sys...)` دریافت می‌کرد → SyntaxError → کد خروج 1 → **هر** کاندیدا (حتی پایتون سالم نصب‌شده و حتی پایتونی که خودمان همین الان نصب کرده بودیم) رد می‌شد. برای همین سیستم می‌گفت «نصب اجرا شد اما در مسیر X پیدا نشد» در حالی که پوشه وجود داشت.
  - بررسی نسخه اکنون با `python -V` (بدون هیچ نقل‌قولی) و در صورت نیاز یک `-c` بدون نقل‌قول انجام می‌شود.
  - **خودتشخیصی داخلی:** اگر هیچ پایتونی پذیرفته نشود، سازنده فهرست همهٔ مسیرهای بررسی‌شده را همراه خروجی خام و کد خروج هر کدام، هم روی صفحه و هم در `build.log` نشان می‌دهد؛ دیگر هیچ ردشدنی بی‌دلیل ثبت نمی‌شود.
  - `TargetDir` نصب سایلنت پایتون داخل نقل‌قول قرار گرفت (مسیرهای دارای فاصله).

### Changed
- نسخه در backend / Inno / standalone / Android → 1.2.3. محتوای برنامه بدون تغییر (۱۵۸ عملیات API، ۲۷۴ آزمون).

## [1.2.2] - 2026-09-06 (رفع باگ «پایتون دانلود شد اما پیدا نمی‌شود» در سازندهٔ نصب‌کننده)

### Fixed
- **سازندهٔ نصب‌کنندهٔ ویندوز، پایتونِ از قبل نصب‌شده را نمی‌دید و بیهوده دانلود می‌کرد؛ سپس می‌گفت «نصب شد اما پیدا نشد، ریستارت کنید».** علت: جست‌وجو فقط PATH و پوشهٔ per-user را می‌دید (پایتون «برای همهٔ کاربران» یا بدون تیک Add to PATH نادیده گرفته می‌شد) و پس از نصب سایلنت هم فقط به تازه‌شدن PATH تکیه می‌کرد که وقتی همان نسخه از قبل نصب باشد اتفاق نمی‌افتد.
  - جست‌وجوی جدید (`Get-PythonCandidates`): متغیر `SUPERMARKET_PYTHON` → رجیستری PEP 514 (HKCU/HKLM/WOW6432Node) → `py -0p` → PATH (با حذف stub فروشگاه ویندوز در `WindowsApps`) → پوشه‌های استاندارد (`%LOCALAPPDATA%\Programs\Python`, `Program Files`, `C:\Python3x`).
  - هر کاندیدا واقعاً اجرا و نسخه‌اش بررسی می‌شود (`Test-PythonExe`)؛ کاندیدای خراب کل جست‌وجو را متوقف نمی‌کند.
  - اگر پایتون پیدا شود، پیام «پایتون از قبل نصب است؛ چیزی دانلود نمی‌شود» نمایش داده می‌شود و هیچ دانلودی انجام نمی‌گیرد.
  - در صورت نیاز به نصب: فایل دانلودشدهٔ قبلی بازاستفاده می‌شود، نصب در مسیر ثابت `%LOCALAPPDATA%\Programs\Python\Python311` (`TargetDir`) انجام و همان مسیر مستقیماً تأیید می‌شود — دیگر نیازی به ریستارت نیست. پیام خطا در بدترین حالت راه‌حل مشخص (متغیر `SUPERMARKET_PYTHON`) و مسیر لاگ نصب‌کننده را می‌دهد.

### Changed
- شمارهٔ نسخه در backend / Inno / standalone / Android → 1.2.2. محتوای برنامه بدون تغییر.
- `backend/requirements-dev.txt`: وابستگی تست‌فقط Pillow اضافه شد (آزمون لوگو).

## [1.2.1] - 2026-09-06 (رفع خطای ساخت فایل نصب ویندوز)

### Fixed
- **BUILD-SETUP.bat / BUILD-SETUP-GUI.bat روی ویندوز واقعی با خطای «Missing closing '}'» متوقف می‌شدند.** علت: فایل‌های `builder-lib.ps1`، `builder-gui.ps1` و `build.ps1` متن فارسی داشتند اما بدون **BOM UTF-8** ذخیره شده بودند؛ Windows PowerShell 5 چنین فایلی را با کدپیج ANSI می‌خواند و بایت‌های بعضی حروف فارسی (مثلاً «ۀ») را به‌عنوان علامت نقل‌قول تفسیر می‌کرد، در نتیجه رشته‌ها ناتمام و بلوک‌ها «بسته‌نشده» تشخیص داده می‌شدند. هر سه اسکریپت اکنون با BOM ذخیره شده‌اند و کاراکترهای مشکل‌ساز (هَمزهٔ ترکیبی و em-dash) حذف شده‌اند.
- هر دو فایل `.bat` پیش از ساخت، هر سه اسکریپت را با پارسر رسمی PowerShell بررسی می‌کنند و در صورت خرابی فایل، شمارهٔ خط و پیام دقیق را نشان می‌دهند (به‌جای کرش نامفهوم پنجرهٔ گرافیکی).
- `setup.iss`: کلید `MinVersion=6.1sp1` اشتباهاً بعد از بخش `[Tasks]` قرار داشت و کامپایل Inno Setup را متوقف می‌کرد؛ به بخش `[Setup]` منتقل شد.

### Changed
- شمارهٔ نسخه در backend، installer (Inno/standalone) و اندروید به 1.2.1 رسید. محتوای برنامه (۱۵۸ endpoint، رابط دسکتاپ/موبایل، فونت آفلاین، بانک اولیهٔ کالا، migrationها) بدون تغییر نسبت به 1.2.0 در فایل اجرایی جاسازی می‌شود (`installer/windows/app.spec`).

## [1.2.0] - 2026-09-06 (بستن همهٔ موارد 🟡 — سخت‌افزار، پیامک، به‌روزرسانی، بانک کالا، اندروید)

### Added
- **§17/§19/§178–182 درایور کامل ESC/POS** (`services/escpos_driver.py`): پرینتر شبکه
  `tcp://host:9100` بدون درایور، `escpos:usb:VID:PID`، `escpos:win:NAME`؛ کدپیج cp1256،
  راست‌چینی بصری خطوط فارسی، عرض کاغذ ۵۸/۷۶/۸۰ (۳۲/۴۲/۴۸ ستون)، برش `GS V`، پالس کشو
  `ESC p` با پین ۲/۵ از طریق همان پرینتر. رسید فارسی با سربرگ فروشگاه، تاریخ شمسی، تومان/ریال،
  و **لوگوی فروشگاه به‌صورت raster (`GS v 0`)**. آزمون با شبیه‌ساز پورت 9100 (بایت‌به‌بایت).
- **§164–165 ملی‌پیامک مطابق کلاینت رسمی**: REST `SendSMS` (خط اختصاصی) و `BaseServiceNumber`
  (الگو/خط خدماتی)، `GetCredit` در تست اتصال، `GetDeliveries2`، جدول خطاهای فارسی `RetStatus`.
  آزمون با سرور شبیه‌ساز.
- **§270 سرور به‌روزرسانی داخلی** (`update.channel=server`, `update.server_url`, `update.server_token`)
  با قرارداد JSON و **اجبار SHA-256** برای هر دو کانال (GitHub: خواندن `SHA256SUMS*`).
- **§80–82 بانک اولیهٔ کالاها با موجودی صفر**: ۱۹۱ کالا / ۱۳ دسته / ۳۰ زیردسته آفلاین
  (`backend/app/data/starter_catalog.csv`)، بدون بارکد جعلی (کد `INT-`)، idempotent؛
  `GET/POST /products/import/starter`, `POST /products/import/csv` برای فایل خود فروشگاه؛ کارت در صفحهٔ کالاها.
- **§214 بارگذاری لوگوی فروشگاه** (`POST/DELETE /settings/store-profile/logo`, PNG/JPEG/SVG/WebP ≤2MB)؛
  لوگو در سایدبار، ورود، «درباره» و رسید چاپی.
- **§137–138 گزارش فروش ماهانه/هفتگی شمسی** (`/reports/sales?group=monthly|weekly`) + تب‌های جدید گزارش.
- **§244 فونت Vazirmatn بسته‌بندی‌شده** (`frontend/fonts/*.woff2`, OFL) — بدون وابستگی به اینترنت/فونت سیستم؛ در Service Worker کش می‌شود.
- **§257 پروژهٔ اندروید بومی** (`mobile-android/`): پوستهٔ WebView با دوربین، صفحهٔ اتصال به سرور LAN
  (probe `/health`)، بنر آفلاین، نسخه از `__init__.py`؛ workflow `installer/ci/release-android.yml`.
- `scripts/activate-ci.sh|.bat` برای فعال‌سازی یک‌بارهٔ workflowهای Windows/Android.
- مستندات: `docs/DATABASE.md` (تولیدشده از مدل‌ها، ۳۷ جدول + ER)، `HARDWARE.md`, `RESOLVER.md`, `SMS.md`,
  `UPDATE.md`, `DIAGRAMS.md` (۷ نمودار Mermaid: معماری، جریان داده، فروش، ثبت کالا، انبارگردانی، سرویس‌های خارجی، به‌روزرسانی).

### Changed
- `POST /hardware/test/print` از پروفایل واقعی پرینتر استفاده می‌کند و در نبود دستگاه صادقانه `PRINTER_NOT_CONFIGURED` می‌دهد.
- عیب‌یابی پیامک، اعتبار واقعی پنل را گزارش می‌کند.
- فرم ثبت دستگاه راهنمای اسکیم اتصال دارد؛ تب «به‌روزرسانی» کانال و تنظیمات `update.*` را نشان می‌دهد.
- PyInstaller spec: `app/data` (بانک اولیه) همراه بسته می‌شود.

### Fixed
- آزمون‌های وابسته به ترتیب اجرا (صف پیامک مشترک، سینک پرینتر مشترک) مستقل از ترتیب شدند؛
  شبیه‌ساز پرینتر تا تخلیهٔ کامل بافر صبر می‌کند (حذف flaky).

### Verification
- `pytest`: **273 passed** (۲۶۴ قبلی + ۹ جدید). اسموک jsdom: ۱۴ نما + موبایل + v1.1 + v1.2 بدون خطای runtime.
- NOT VERIFIED (محیط): چاپگر/کشوی فیزیکی، ارسال زندهٔ پیامک، دانلود زندهٔ GitHub، Setup.exe روی ویندوز، ساخت APK (نیازمند اجرای CI).

## [1.1.0] - 2026-09-06 (ممیزی ۳۵۰ قابلیت — تکمیل شکاف‌ها)

نسخهٔ ۱٫۱٫۰ نتیجهٔ تطبیق تک‌به‌تک کد با «فهرست کامل امکانات سیستم» (۳۵۰ مورد)
است. وضعیت هر مورد در `docs/FEATURE_CHECKLIST_350.md` ثبت شده است. مواردی که
واقعاً کم بودند یا فقط «نیمه» بودند در این نسخه پیاده و آزمون شدند.

### Added
- **§12 تخفیف روی فاکتور** به‌عنوان یک تخفیف واقعیِ سطح فاکتور
  (`invoice_discount` در `POST /pos/checkout` و `/pos/cart/validate`، ستون جدید
  `invoices.invoice_discount`). پیش‌تر فقط تخفیف خطی وجود داشت و «کل سبد» به‌صورت
  تقسیم متناسب روی خط‌ها شبیه‌سازی می‌شد.
- **§19 کشوی پول در فروش نقدی**: پس از هر فروش نقدی پالس کشو ارسال می‌شود و نتیجهٔ
  صادقانهٔ آن (`drawer.ok/message`) در پاسخ checkout برمی‌گردد.
- **§109/§130/§131 چند انبار، محل نگهداری و انتقال موجودی**: جداول
  `warehouses` و `storage_locations`، مسیرهای `GET/POST/PATCH /warehouses`،
  `POST /warehouses/{id}/locations`، `POST /warehouses/transfer`. انتقال
  با جفت حرکت `TRANSFER_OUT/TRANSFER_IN` ثبت می‌شود و Batch مقصد همان بهای
  تمام‌شده را حفظ می‌کند (§104). انبار «اصلی» به‌صورت خودکار ساخته می‌شود.
  کارت «انبارها و محل نگهداری» در صفحهٔ انبار.
- **§79 زیردسته**: `parent_id` در `POST /products/categories`؛ `GET` مسیر
  `path` («لبنیات / پنیر») برمی‌گرداند؛ فرم کالا ورودی «والد / فرزند» را می‌پذیرد.
- **§166 الگوهای پیامک قابل ویرایش** برای فاکتور، بدهی، کوپن، هشدار انبار و گزارش
  مدیریت (`GET /sms/templates` + کلیدهای `sms.template.*`).
- **§175 پیامک گزارش مدیریت** (`POST /sms/daily-report`) به شمارهٔ مدیر.
- **§176 پیامک هشدار انبار**: اسکن دوره‌ای (`/jobs/expiry-scan`) کالاهای زیر حداقل را
  می‌شمارد و در صورت فعال‌بودن `sms.low_stock_alert` به مدیر پیامک می‌فرستد.
- **§171 تلاش مجدد دستی پیامک** (`POST /sms/{id}/retry`) و **§177 تست اتصال
  سرویس پیامک** (`POST /sms/test-connection`) + ابزار و صف پیامک در تنظیمات.
- **§209 تأیید رمز مدیر برای ابطال فاکتور پرداخت‌شده** (`admin_password` در
  `POST /invoices/{id}/void`، قابل تنظیم با `security.require_admin_for_void_paid`،
  رد شدن در لاگ `VOID_DENIED`). مودال ابطال با علت + رمز.
- **§215–§229 تنظیمات دسته‌بندی‌شده**: ۲۲ تب فارسی (پروفایل فروشگاه، عمومی و زمان،
  صندوق، واحد پول، انبار، انقضا، محصولات، بارکد، قیمت‌گذاری، مشتریان، حساب دفتری،
  جشنواره و تخفیف، پیامک، الگوهای پیامک، پرینتر، کشوی پول، شبکه، امنیت،
  پشتیبان‌گیری، ظاهر، به‌روزرسانی، درباره) با کلیدهای پیش‌فرض جدید.

### Fixed
- **`GET /products/categories` و `GET /products/brands` همیشه 422 می‌دادند**:
  مسیر `/{product_id}` پیش از آن‌ها تعریف شده بود و «categories» را به‌عنوان id
  می‌بلعید. ترتیب مسیرها اصلاح شد (باگ از v0.x).
- مهاجرت `c8d1e2f3a410` فقط افزودنی است؛ ارتقای پایگاه‌دادهٔ v1.0.0 با ۲۰ کالا و
  ۴ فاکتور آزمایش و بدون از دست رفتن داده تأیید شد (§274).

### Tests
- `tests/test_v1_1_features.py` (۱۴ آزمون جدید) — مجموع: **۲۶۴ آزمون سبز**.
- آزمون ابطال (`test_void_restocks`) برای §209 به‌روزرسانی شد (رگرسیون §46).
- اسموک UI با jsdom: ۱۴ نمای مدیریت + موبایل، کارت انبارها، ۲۲ تب تنظیمات، ابزار
  پیامک، مودال ابطال با رمز.


## [1.0.0] - 2026-09-06 (یکپارچه‌سازی نهایی — Consolidation Release)

نسخهٔ ۱٫۰٫۰ **یکپارچه‌سازی کامل دو خط توسعهٔ موازی** است. تا پیش از این، قابلیت‌ها
بین شاخه‌ها پراکنده بودند: خط `v0.3.1` (فازهای ۷ تا ۱۱) همهٔ موتور کسب‌وکار را داشت
اما ابزار ساخت نصب قدیمی‌اش را، و خط `v0.4.0` ابزار ساخت نصب اصلاح‌شده را داشت اما
بدون فازهای ۷ تا ۱۱. هیچ‌کدام به‌تنهایی کامل نبود.

### Merged — ادغام دو خط توسعه
- همهٔ ۶۹ فایل تغییریافتهٔ خط `v0.3.1` (کوپن/کمپین، حساب دفتری، عیب‌یابی،
  واحدها، به‌روزرسانی، زمان مورد اعتماد، صف همگام‌سازی، ۱۲ فایل آزمون جدید)
  با اصلاحات installer خط `v0.4.0` ادغام شد.
- تعداد آزمون‌ها: ۸۸ (خط ۰٫۴٫۰) ← **۲۲۸** (پس از ادغام).
- مهاجرت‌های پایگاه‌داده: ۳ → **۶** (`e5b21c74a903`, `a1c93f4d7e10`,
  `b7e4d2019f31` از خط ۰٫۳٫۱ به خط اصلی برگردانده شدند).

### Fixed — اشکالات ریشه‌ای که در ادغام کشف شد
- **`builder-gui.ps1` پیش از تعریف `$ScriptDir` آن را می‌خواند.** با
  `Set-StrictMode -Version Latest` این یک خطای پایانی است، یعنی هم
  `BUILD-SETUP.bat` و هم `build.ps1` خط `v0.3.1` در همان لحظهٔ شروع می‌شکستند.
- `build.ps1` دیگر کپی جداگانهٔ منطق ساخت نیست؛ یک موتور مشترک
  (`builder-lib.ps1`) هم CLI و هم GUI را می‌راند تا این دو هرگز از هم جدا نیفتند.
- **پیش‌پرواز کامل‌بودن مخزن** (۱۰ فایل حیاتی، از خط ۰٫۴٫۰) به موتور مشترک
  منتقل شد؛ پیش از این فقط یک فایل بررسی می‌شد.
- **خروجی Portable همیشه ساخته می‌شود** حتی وقتی Inno Setup نصب نیست؛
  نبودِ Inno دیگر ساخت را شکست نمی‌دهد (رفتار خط ۰٫۴٫۰ در موتور مشترک).
- **دانلود خودکار پیش‌نیازها اختیاری شد** (`-NoDownload`): زنجیرهٔ دانلود
  خودکار منبع اصلی شکست گزارش‌شدهٔ کاربران بود.
- `setup.iss`: `x64compatible` ← `x64` (سازگاری با Inno 6.0–6.2) در کنار
  AppId واقعی، `UsePreviousAppDir`، `WizardSizePercent` و `MinVersion` حفظ شد.
- workflow ویندوز بازنویسی و در `installer/ci/release-windows.yml` تثبیت شد؛
  نام artifact لینوکس دیگر عدد نسخهٔ سخت‌کدشدهٔ `0.2.0` را ندارد و از
  `backend/app/__init__.py` خوانده می‌شود. فعال‌سازی آن در
  `.github/workflows/` به یک توکن با مجوز `workflows` نیاز دارد (توکن نگهدارندهٔ
  خودکار این مجوز را ندارد و GitHub چنین push ای را رد می‌کند)؛ دستور یک‌خطی
  در `installer/ci/README.md` آمده است.


## [0.4.0] - 2026-09-06 (فاز ۶ — رفع ریشه‌ای خطاهای ساخت نصب)

### Fixed — ریشه‌یابی خطاهای «کامپایلری + کد ۱» کاربران
- **ریشهٔ اصلی:** شاخهٔ پیش‌فرض گیت (`main`) تقریباً خالی بود (۲ فایل) و نسخهٔ
 builder قدیمیِ یک شاخهٔ موازی (با خطاهای خطوط ۲۴۹–۳۱۴ و کد خروج ۱) بین
  کاربران پخش شده بود. این نسخه روی `main` ادغام شد تا دانلود پیش‌فرض کامل باشد.
- builder جدید عمداً مینیمال و دفاعی است: `BUILD-SETUP.bat` (ASCII خالص، دابل‌کلیک)
  + `build.ps1` (BOM دار برای پیام‌های فارسی)؛ بدون GUI/WPF و بدون دانلود
  خودکار پیش‌نیاز (زنجیرهٔ دانلود خودکار، منبع اصلی شکست بود).
- `build.ps1`: پیش‌پرواز کامل‌بودن مخزن (۱۰ فایل حیاتی)، یافتن پایتون از PATH
  یا py launcher + چک نسخه، چک کد خروج هر مرحله (pip/PyInstaller/ISCC)،
  تأیید وجود واقعی فایل‌های خروجی، خروجی Portable حتی بدون Inno Setup،
  لاگ کامل `build.log`.
- `app.spec`: افزودن `sqlalchemy.dialects.sqlite` (دیالکت از entry-point لود
  می‌شود — در frozen قطعی NoSuchModuleError می‌داد؛ در تست boot واقعی cx_Freeze
  اثبات شد) و `multipart`/`python_multipart` (استارلت هنگام فرم لاگین lazy لود می‌کند).
- `setup.iss`: سازگار با همهٔ نسخه‌های Inno 6 (`x64` به‌جای `x64compatible` که در
  ۶.۰–۶.۲ خطای کامپایل می‌دهد)، کامنت‌های ASCII خالص، حذف taskkill تهاجمی، نسخه ۰.۴.۰.

### Added
- `installer/windows/README.md` — راهنمای فارسی گام‌به‌گام + جدول عیب‌یابی خطاهای رایج.
- خروجی `SupermarketSystem-0.4.0-portable.exe` (تک‌فایل بدون نصب) همیشه ساخته می‌شود.


## [0.3.1] - 2026-09-05

انتشار ابزار ساخت فایل نصب ویندوز.

### Added
- **`ساخت-فایل-نصب.bat` در ریشهٔ پروژه** — نقطهٔ شروع واضح. با دوبار کلیک، یک
  پنجرهٔ گرافیکی با نوار پیشرفت باز می‌شود که همهٔ پیش‌نیازها را دانلود و نصب
  می‌کند و در پایان یک `SupermarketSystem-Setup-0.3.1.exe` قابل توزیع می‌سازد.
- بررسی خودکار **خودکفا بودن** خروجی: اگر فایل اجرایی کمتر از ۱۵ مگابایت یا
  فایل نصب کمتر از ۱۰ مگابایت باشد، ساخت با خطای صریح متوقف می‌شود — چون یعنی
  مفسر پایتون جاسازی نشده و نصب روی سیستم بدون پایتون شکست می‌خورد.
- `.gitattributes` — فایل‌های `.bat`/`.ps1`/`.iss` همیشه با خط‌پایان CRLF
  چک‌اوت می‌شوند.

### Fixed
- **`console=False` در `app.spec`** — کاربر نهایی دیگر یک پنجرهٔ سیاه cmd
  نمی‌بیند. گزارش‌ها همچنان در `%USERPROFILE%\SupermarketSystem\logs\` ثبت می‌شوند.
- خط‌پایان‌های `.bat` به CRLF اصلاح شد؛ فایل با خط‌پایان LF روی ویندوز
  می‌توانست نادرست تفسیر شود.

### Verified
- **بستهٔ فریزشده واقعاً بوت شد.** یک build کامل با cx_Freeze ساخته و در محیطی
  **بدون هیچ پایتون سیستمی** اجرا شد: مهاجرت‌ها اجرا شدند، ورود انجام شد، یک
  فروش کامل ثبت شد (موجودی ۸ ← ۵)، پنل و نسخهٔ موبایل هر دو ۲۰۰ برگرداندند، و
  داده‌ها بیرون از پوشهٔ نصب ساخته شدند.
- ۲۲۸ آزمون موفق.

## [0.3.0] - 2026-09-05

فاز ۱۱ — هویت کالا، قیمت‌گذاری در سطح بچ، و رفع اشکالات ممیزی.

### حسابرسی: چه چیزی از قبل درست بود

پیش از هر تغییری، معماری «یک کالا / چند بچ» با آزمون راستی‌آزمایی شد
(`tests/test_phase11_product_batch_identity.py`). نتیجه: قواعد غیرقابل‌مذاکرهٔ
§۳۰ و §۳۲ **از قبل درست پیاده‌سازی شده بودند** و باگ نبودند — دریافت دوبارهٔ یک
بارکد، یک کالا و دو بچ می‌سازد، قیمت‌ها ادغام نمی‌شوند، و هر سطر فاکتور
`batch_id` خود را نگه می‌دارد.

### Added
- **§۶ — قیمت‌گذاری کامل در سطح بچ:** فیلدهای `supplier_price`، `discount` و
  `tax` به بچ اضافه شد. هیچ مبلغی روی خودِ کالا ذخیره نمی‌شود تا تاریخچهٔ سود
  هر خرید برای همیشه قابل محاسبه بماند.
- **§۵ — صفحهٔ جزئیات کالا** (دسکتاپ و موبایل): یک هویت کالا به‌همراه همهٔ
  بچ‌هایش. بچ‌های تمام‌شده حذف نمی‌شوند بلکه جداگانه به‌عنوان «تاریخچهٔ قیمت»
  نمایش داده می‌شوند.
- **§۱۶ — بارکد داخلی برای کالای فله:** کالای بدون بارکد کارخانه‌ای، کد
  `INT-000001` می‌گیرد. تخصیص از طریق سطر Counter اتمیک انجام می‌شود.
- **§۳۳ — تشخیص کالای تکراری:** هشدار پیش از ثبت، با یکسان‌سازی حروف عربی/فارسی
  (ي/ی، ك/ک، آ/ا) و ارقام. صرفاً مشورتی است و هرگز ثبت را مسدود یا ادغام خودکار
  نمی‌کند.
- **API برندها و دسته‌ها** — تا پیش از این هیچ نقطهٔ پایانی نداشتند.

### Fixed
- **§۳۱ — به‌روزرسانی بیرونی نباید کالای تکراری بسازد.** `POST /api/barcode/apply`
  برای بارکد موجود خطای ۴۰۹ می‌داد و اپراتور نمی‌توانست رکورد ناقص را از منبع
  دوم غنی کند؛ اکنون همان کالا را به‌روزرسانی می‌کند.
- منبع بیرونی ناقص دیگر `min_stock_alert` را صفر نمی‌کند و فیلدهای خالی، دادهٔ
  محلی معتبر را پاک نمی‌کنند.
- **فرم ثبت کالا، برند و دسته را ارسال نمی‌کرد** — این کلیدهای خارجی همیشه
  خالی می‌ماندند.
- **§۱۸ — جست‌وجوی صندوق بر اساس نام برند** اضافه شد.
- `scripts/shoot.py` اکنون مقدار بازگشتی JS را لاگ می‌کند؛ پیش از این یک گام
  بی‌اثر از یک گام موفق قابل تشخیص نبود.

### Migration
- `b7e4d2019f31` — کاملاً افزایشی. روی دادهٔ واقعی قدیمی آزمون شد: هیچ ستونی
  حذف، تغییرنام یا تغییرنوع نمی‌شود (§۲۹).

### Tests
- ۲۲۸ آزمون موفق (۱۶ آزمون جدید در فاز ۱۱).

## [0.2.0] - 2026-09-05

نخستین انتشار عمومی. نسخهٔ `0.2.0` مجموع فازهای ۶ تا ۱۰ است.

### فاز ۱۰ — بازیابی خودکار اطلاعات کالا با اسکن بارکد

#### Added
- `POST /api/barcode/scan` — یک فراخوانی، یک «پیش‌نویس» قابل بازبینی: مقدار هر
  فیلد به‌همراه **منبع** و **درجهٔ اطمینان**، به‌علاوهٔ تصویر ذخیره‌شدهٔ محلی.
- ثبت خودکار منابع پیش‌فرض در نخستین اجرا: OpenFoodFacts برای متن و تصویر
  (مجوز ODbL، عمومی و بدون کلید).
- بازطراحی صفحهٔ «کالاها»: اسکن ⇒ پرشدن خودکار نام/برند/دسته، پیش‌نمایش تصویر
  با ذکر منبع و ابعاد، و ستون تصویر در فهرست کالاها.

#### Fixed
- **باگ ریشه‌ای: اسکن بارکد هیچ اطلاعاتی جمع نمی‌کرد.** هیچ منبعی در
  `bootstrap` ثبت نمی‌شد، بنابراین روی هر نصب تازه نتیجه همیشه
  `origin="none"` با فهرست منابع خالی بود؛ ضمناً فرم ثبت کالا اصلاً
  resolver را صدا نمی‌زد. Providerها، منطق ادغام و دانلود تصویر همگی
  پیاده و تست‌شده بودند اما به هیچ چیز وصل نشده بودند.
- شکست منابع دیگر بی‌صدا نیست: نوع خطای هر منبع (`UNREACHABLE`,
  `NOT_FOUND`, `TIMEOUT`, …) به کاربر نمایش داده می‌شود.
- انتساب درست منبع تصویر: منبعی که بایت‌هایش واقعاً ذخیره شده، نه اولین نامزد.

### فاز ۹ — حساب دفتری، پروفایل، زمان، پوسته، به‌روزرسانی

#### Added
- **حساب دفتری مشتری (§30–35)**: دفتر فقط-افزودنی با مانده در گردش، فروش نسیه
  در صندوق، تسویهٔ جزئی/کامل، سقف اعتبار و بررسی یکپارچگی.
- **پیامک یادآوری بدهی** با قالب قابل‌ویرایش (`sms.template.debt_reminder`).
- **پروفایل فروشگاه (§25)** و **دربارهٔ سامانه (§59)**.
- **زمان مورد اعتماد (§22)**: `services/timeservice.py` با تقویم جلالی
  (جدول Borkowski، بدون وابستگی بیرونی) و بررسی اختیاری NTP.
- **پوستهٔ روشن/تیره (§23)** با زمان‌بندی خودکار.
- **سامانهٔ به‌روزرسانی (§27–29)**: بررسی → **پشتیبان‌گیری مسدودکننده** →
  دانلود → صحت‌سنجی. شکست پشتیبان ⇒ توقف کامل.
- نوار وضعیت: اتصال، تاریخ شمسی، ساعت فروشگاه، کاربر، پایگاه‌داده، چاپگر.

#### Fixed
- **همهٔ سطرهای دفتر با تاریخ ۱۳۴۸/۱۰/۱۱ نمایش داده می‌شدند** — مهاجرت ستون
  `created_at` را بدون `DEFAULT now()` ساخته بود و مدل فقط به `server_default`
  تکیه داشت، پس مقدار `NULL` درج می‌شد.
- **ساعت نوار وضعیت و صندوق زمان اشتباه نشان می‌داد** — زمان محلی فروشگاه
  دوباره با منطقهٔ زمانی مرورگر تفسیر می‌شد.
- **آیکون‌های منو روی ویندوزِ بدون فونت ایموجی مربع خالی بودند** — جایگزینی با
  آیکون‌های SVG درون‌خطی.
- **دکمهٔ «پیامک یادآوری» همیشه خطای ۴۲۲ می‌داد** — ناسازگاری `message`/`text`.

### فاز ۶ — Installer و رندر واقعی UI

### Added
- **Installer/Launcher**: لانچر standalone (`installer/windows/run_supermarket.py`) با پورت آزاد،
  دادهٔ کاربر در `~/SupermarketSystem` (DB+logs+secret پایدار بین ری‌استارت‌ها)،
  health-probe واقعی قبل از باز کردن مرورگر؛ spec پکی‌جینگ PyInstaller + اسکریپت Inno Setup
  + `build.ps1` + icon.ico؛ بیلد cx_Freeze برای تست frozen در لینوکس
  (**boot-test کامل**: health/login/frontend/DB/ری‌استارت). ویندوز واقعی: UNTESTED (مستند).
- **رندر واقعی UI**: `scripts/make_qt_stublibs.py` (تولید stub-libs از خود PySide6 با
  verneed parsing درست)، `scripts/shoot.py` (WebEngine offscreen، غیرblank-verified)،
  `scripts/shots.json` (۲۱ تصویر از همه بخش‌ها + PDF)؛
  `docs/SCREENSHOTS.md` + `docs/SCREENSHOTS.pdf` (معرفی فارسی هر بخش با تصویر واقعی،
  ۲۳ صفحه)؛ `scripts/make_pdf.py`.
- **مستندات**: `docs/BUILD.md` (روش build ویندوز/لینوکس + وضعیت تست‌شدگی صادقانه).

### Fixed
- PyInstaller spec: مسیر repo root اشتباه (سه parent به جای دو parent) و
  hiddenimports منسوخ → اصلاح شد.
- cx_Freeze: `sqlalchemy.dialects.sqlite` باید صریحاً باندل شود (entry-point loader).


## [0.2.0] - 2026-09-04 (فاز ۶)

### Added
- **Installer/Launcher**: لانچر standalone (`installer/windows/run_supermarket.py`) با پورت آزاد،
  دادهٔ کاربر در `~/SupermarketSystem` (DB+logs+secret پایدار بین ری‌استارت‌ها)،
  health-probe واقعی قبل از باز کردن مرورگر؛ spec پکی‌جینگ PyInstaller + اسکریپت Inno Setup
  + `build.ps1` + icon.ico؛ بیلد cx_Freeze برای تست frozen در لینوکس
  (**boot-test کامل**: health/login/frontend/DB/ری‌استارت). ویندوز واقعی: UNTESTED (مستند).
- **رندر واقعی UI**: `scripts/make_qt_stublibs.py` (تولید stub-libs از خود PySide6 با
  verneed parsing درست)، `scripts/shoot.py` (WebEngine offscreen، غیرblank-verified)،
  `scripts/shots.json` (۲۱ تصویر از همه بخش‌ها + PDF)؛
  `docs/SCREENSHOTS.md` + `docs/SCREENSHOTS.pdf` (معرفی فارسی هر بخش با تصویر واقعی،
  ۲۳ صفحه)؛ `scripts/make_pdf.py`.
- **مستندات**: `docs/BUILD.md` (روش build ویندوز/لینوکس + وضعیت تست‌شدگی صادقانه).

### Fixed
- PyInstaller spec: مسیر repo root اشتباه (سه parent به جای دو parent) و
  hiddenimports منسوخ → اصلاح شد.
- cx_Freeze: `sqlalchemy.dialects.sqlite` باید صریحاً باندل شود (entry-point loader).


## [0.1.0] - 2026-09-04

### Added
- **هسته**: احراز هویت JWT، کاربران، نقش‌ها و مجوزهای گرانولار، Audit Log.
- **کالا**: Product / Category / Brand / Unit + ثبت با بارکد.
- **Batch**: ورود کالا → Batch جدید با قیمت خرید/مصرف‌کننده/فروش و تاریخ تولید/انقضا؛ بدون بازنویسی تاریخچه.
- **قیمت**: PriceVersion با تاریخچه کامل؛ تشخیص و فروش هم‌زمان قیمت قدیم/جدید.
- **موجودی**: Stock Movements (PURCHASE_IN/SALE_OUT/WASTE/ADJUSTMENT/STOCKTAKE/...)، Adjustment، Waste.
- **انبارگردانی**: Stocktaking + تطبیق فیزیکی/سیستمی + Audit.
- **انقضا**: موتور Expiry با آستانه‌های قابل تنظیم و مسدودسازی فروش کالای منقضی.
- **POS**: اسکن بارکد، انتخاب Batch/قیمت (HYBRID/FIFO/FEFO)، Checkout اتمیک، Void، Return، سود واقعی Batch.
- **گزارش**: داشبورد (فروش/سود/موجودی/انقضا/تعارض قیمت)، گزارش سود Batch، گردش کالا.
- **سخت‌افزار**: لایه انتزاعی پرینتر/کشو، تشخیص اسکنر بر اساس زمان‌بندی، تست چاپ/کشو.
- **رزولورها**: بیکد/تصویر/قیمت بازار با ترتیب محلی → کش → خارجی → دستی (بدون داده جعلی).
- **پنل وب**: SPA فارسی/RTL (داشبورد، POS، کالا، ورود، انبار، فاکتور، گزارش، سخت‌افزار، کاربر، تنظیمات، لاگ).
- **زیرساخت**: مهاجرت Alembic (۲۹ جدول)، seed دمو، backup آنلاین SQLite، ۲۱ تست خودکار.
- **مستندات**: README، ARCHITECTURE، API، INSTALL.

### Security
- بدون Hard-Code کردن اعتبارنامه‌ها؛ همه از متغیرهای محیطی/تنظیمات رمزنگاری‌شده.
