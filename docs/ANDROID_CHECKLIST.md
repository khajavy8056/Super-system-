# چک‌لیست کامل برنامهٔ اندروید «سوپری من» — v3.6.0 (بومی / Native)

## تازه‌های v2.4 — استقلال کامل (وضعیت)

| # | مورد | وضعیت | نحوهٔ راستی‌آزمایی |
|---|------|-------|---------------------|
| 1 | همهٔ مسیرهای API روی خود گوشی (`Local.handle`) | ✅ کامپایل شد، APK ساخته شد | `ecj` بدون خطا؛ نگاشت مسیرها ↔ صفحه‌ها بررسی شد |
| 2 | اسکیمای SQLite v2 + ارتقای خودکار | ✅ | `Db.onUpgrade(1→2)` |
| 3 | پرسش موبایل مشتری در پرداخت + پیامک فاکتور از گوشی | ✅ کد | نیاز به تست روی دستگاه با پنل ملی‌پیامک واقعی — **NOT VERIFIED روی دستگاه** |
| 4 | تنظیمات کامل (همهٔ دسته‌ها) در حالت مستقل | ✅ | `Local.DEFAULTS` = همان `DEFAULT_SETTINGS` ویندوز |
| 5 | حسابداری کامل روی گوشی (سند دوطرفه) | ✅ کد | فروش/مرجوعی/هزینه/چک/تسویه سند می‌زنند؛ تراز آزمایشی تراز است |
| 6 | کاربران/نقش‌ها/ورود محلی/حسابرسی | ✅ کد | `auth/login` محلی با هش SHA-256 |
| 8 | ورود اجباری + پایان نشست پس از ۳۰ دقیقه بی‌کاری | ✅ کد (`Session`) | `onUserInteraction`/`onResume`/تیکر ۲۰ ثانیه‌ای |
| 11 | انتخاب‌گر تصویر (نامزدهای فروشگاهی + عکس خودم از دوربین/گالری) در صفحهٔ کالا | ✅ کامپایل و منطق؛ پاسخ زندهٔ سایت‌ها تأییدنشده |
| 10 | تصویر خودکار کالا (نام → OFF/Commons/Wikipedia/DDG) و بندانگشتی در صندوق/انبارگردانی/کالاها | ✅ کامپایل و منطق؛ جست‌وجوی زنده تأییدنشده (بدون اینترنت در محیط ساخت) |
| 9 | نصب به‌روزرسانی روی نسخهٔ قبلی با حفظ داده | ✅ راستی‌آزمایی شد | `aapt2 dump badging` + `apksigner --print-certs` روی 2.3.0 و 2.4.0: package و گواهی یکسان، versionCode بالاتر |
| 7 | اجرا روی دستگاه فیزیکی / شبیه‌ساز | ⚠️ NOT VERIFIED در این محیط (بدون شبیه‌ساز اندروید) | نصب APK و عبور از سناریوی «فروش → پیامک → ابطال → گزارش» |


## تازه‌های v2.3 (وضعیت §57)
| مورد | وضعیت | شاهد |
|---|---|---|
| پیامک به محض تأیید فاکتور، حتی با چاپ خاموش (حالت رایانه و مستقل) | DONE (کد + کامپایل + تست بک‌اند) | `SmsLocal.java`, `routers/pos.py`, `test_v23_online_sms.py` |
| صفحهٔ پیامک کامل + راهنمای گام‌به‌گام ملی‌پیامک | DONE (کد + کامپایل + تست `/api/sms/guide`) | `AdminScreens.java` (پیامک), `routers/sms.py` |
| راه‌اندازی اولیه در پس‌زمینه با اعلان پیشرفت | DONE (کد + کامپایل) / NOT VERIFIED روی گوشی | `InstallService.java`, `SetupActivity.java`, Manifest |
| ورود/قفل با اثر انگشت | DONE (کد + کامپایل) / NOT VERIFIED روی گوشی | `Biometric.java`, `LoginActivity.java`, `Screens.Device` |
| پورت از جفت‌سازی/بیکن (نه 8000)، کشف مجدد پس از تغییر IP و تغییر شبکه | DONE (کد + کامپایل + تست بار جفت‌سازی) | `Sync.watchNetwork`, `Api.health`, `test_relay_config_and_pairing_payload` |
| اتصال از راه دور (رله) + بازگشت خودکار به LAN | DONE (کد + کامپایل + تست رفت‌وبرگشت رله) | `Relay.java`, `relay/server.py`, `relay_client.py` |
| v2.7 بانک کالا: جدول `bank` (db v3)، بذر از asset، pull.bank از رایانه، `BANK_REMEMBER`، شناسایی آفلاین در «کالای جدید»، بدون نام منابع در UI | DONE (کد + کامپایل + تست سرور) / NOT VERIFIED روی گوشی | `Db.java bankPut/bankGet/bankRemember/importBankSeed`, `Images.lookupBarcode`, `StockScreens.java` |
| v2.9 قفل اثر انگشت فقط پس از پس‌زمینهٔ واقعی (نه اسکنر/گالری) + آیکون‌های وکتور + پوستهٔ Atelier | DONE (کد + کامپایل) / NOT VERIFIED روی گوشی | `Biometric.java`, `Icons.java`, `AppActivity.java`, `Ui.java` |
| APK 2.9.0 (versionCode 20900) | DONE | `releases/android/SupermarketMobile-2.9.0.apk` + sha256 (همان کلید — نصب روی نسخهٔ قبل) |
| APK 2.3.0 (versionCode 20300) | DONE | `releases/android/SupermarketMobile-2.3.0.apk` + sha256 (کلید جدید — ارتقا از 2.2 یک‌بار حذف/نصب) |


## تازه‌های v2.2 (وضعیت §57)
| مورد | وضعیت | شاهد |
|---|---|---|
| نصب به‌روزرسانی روی نسخهٔ قبل | DONE | کلید ثابت `mobile-android/keystore/supery-release.jks`؛ امضای 2.2.0 = 2.1.0 (`0f9d69b2…`)؛ **از 2.3.0 کلید جدید `18b4ab18…` داخل مخزن** |
| هشدار Play Protect | تا حد ممکن (کلید ثابت + راهنمای نصب) — حذف کامل فقط با انتشار در Play | README |
| اعلان‌های نوار بالا (پشتیبانی/انقضا/موجودی/همگام‌سازی) | DONE (کد + کامپایل + تست منبع) / NOT VERIFIED روی گوشی | `Notify.java`, `AndroidManifest.xml` |
| صداهای ملایم مانند رایانه | DONE (کد + کامپایل) / NOT VERIFIED روی گوشی | `Sfx.java` |
| بازطراحی UI/UX طبق طرح‌های مرجع | DONE (کد + کامپایل + تست منبع) | `Ui.java`, `AppActivity.java`, `Screens.Home`, `SalesScreens.Pos` |
| APK 2.2.0 (versionCode 20200) | DONE | `releases/android/SupermarketMobile-2.2.0.apk` + sha256 |


## تازه‌های v2.1 (وضعیت §57)
| مورد | وضعیت | شاهد |
|---|---|---|
| QR رایانه کامل و قابل اسکن | DONE | اسکرین‌شات Chromium `docs/screenshots/v21-mobile-pair.png` با jsQR رمزگشایی شد؛ `test_pc_qr_is_not_cropped_anymore` |
| کد ۶ رقمی جفت‌سازی | DONE | `POST /mobile/pair/code` → `claim` روی سرور زنده؛ تست یک‌بارمصرف |
| کلید اتصال + بیکن UDP (تغییر IP) | DONE (سرور) / NOT VERIFIED روی گوشی واقعی | `SMKT-FIND/HERE` روی سرور زنده پاسخ داد؛ `Discovery.java` کامپایل شد، اجرای روی دستگاه فیزیکی در این محیط ممکن نیست |
| ویزارد راه‌اندازی اندروید (دو مسیر، لودینگ ۴۵ دقیقه‌ای یک‌باره) | DONE (کد + کامپایل + تست قرارداد) / اجرای UI روی دستگاه NOT VERIFIED | `SetupActivity.java`; `test_android_v21_wizard_datepicker_name_and_support` |
| لایسنس روی گوشی / قفل با انقضای رایانه | DONE (سرور: `/mobile/link` + ۴۰۲) / گوشی NOT VERIFIED روی دستگاه | `Lic.java`, `LockActivity.java`, `Api.java` (402) |
| پشتیبانی بدون رایانه (همان ربات) | کد کامل، ارسال واقعی به صندوق پشتیبانی از این محیط تست نشد (NOT VERIFIED) | `SupportRelay.java` همان پروتکل `services/support.py` |
| ورود Google داخل برنامه | DONE با stub (تبادل توکن گوگل واقعی NOT VERIFIED — نیاز به Client ID مالک) | `test_inapp_google_signin_endpoints` |
| تقویم شمسی همهٔ تاریخ‌ها | DONE (کد + کامپایل) | `DatePicker.java`; هیچ `Ui.input` با الگوی تاریخ باقی نمانده (تست) |
| نام برنامه «سوپری من» | DONE | `aapt2 dump badging` → `application-label:'سوپری من'` |


**قاعدهٔ صداقت (§57):** ✅ = کد + اجرا + آزمون + نتیجهٔ مورد انتظار (در سندباکس: کامپایل ecj/d8، امضای APK، آزمون‌های pytest روی قرارداد API، اجرای واقعی سرور و فراخوانی endpointها).
⚠️ NOT VERIFIED = فقط روی گوشی فیزیکی قابل تأیید است (این محیط اندروید/دوربین واقعی ندارد؛ امولاتور هم در دسترس نیست). **رندر واقعی صفحات بومی روی دستگاه در این محیط تأیید نشده است.**

آزمون‌های مرجع: `backend/tests/test_v20_native_android.py` (۵/۵ ✅)، کل backend **330 passed, 1 skipped**، `scripts/android/build-apk.sh` (✅ `SupermarketMobile-2.0.0.apk`، 544 KB، امضای v2+v3)، آزمون JVM تقویم جلالی (✅).

## معماری v2.0 — چرا «بومی» و «مستقل»
| موضوع | پیاده‌سازی | وضعیت |
|---|---|---|
| بدون WebView / PWA | هیچ `android.webkit` و هیچ HTML/JS در APK؛ `assets/` فقط قلم Vazirmatn (TTF از woff2 تبدیل شد) | ✅ آزمون `test_native_app_has_no_webview_and_ships_every_section` |
| UI بومی | ویوهای Android (LinearLayout/ScrollView/EditText/…) ساخته‌شده در کد؛ سیستم طراحی `Ui.java` (RTL، تیره/روشن آرام، کارت، KPI، شیت پایین، چیپ) | ✅ کامپایل؛ ⚠️ ظاهر روی دستگاه |
| استقلال کامل | `Db.java` SQLite: کاتالوگ کامل + فاکتور محلی + صف عملیات + کش پاسخ‌ها؛ حالت «شروع مستقل» بدون رایانه | ✅ کد/کامپایل؛ ⚠️ اجرای روی دستگاه |
| فقط تبادل داده با PC | `Api.java` JSON/HTTP روی LAN، Bearer token دستگاه؛ PATCH با `X-HTTP-Method-Override` | ✅ middleware و endpointها با curl و pytest |
| همگام‌سازی دوطرفه | `Sync.java`: push صف ← `/api/mobile/sync` (idempotent) + pull تغییرات با cursor، هر ۲۰ ثانیه/بعد از هر ثبت/بازگشت اتصال | ✅ سمت سرور با ۸ عملیات واقعی (APPLIED/DUPLICATE/REJECTED/adjusted)؛ ⚠️ حلقهٔ گوشی |
| جفت‌سازی | QR `SMKT:` از تنظیمات ویندوز (چند آدرس، probe `/health`)، آدرس دستی + ورود کاربر ← `/mobile/pair/token`، `smkt://` deep link | ✅ payload سرور؛ ⚠️ اسکن روی دستگاه |
| اسکنر | `ScanActivity` (Camera2 + ZXing جاوا، v1.8.1) بدون تغییر | ✅ رمزگشایی JVM (v1.8.1)؛ ⚠️ دوربین |

## بخش‌ها (۲۷/۲۷ در روتر بومی `Screens.create`) — هر بخش با endpointهای واقعی
| # | بخش (کلید) | قابلیت‌ها | وضعیت |
|---|---|---|---|
| 1 | داشبورد `home` | ۱۲ بلوک: فروش امروز/دیروز، فاکتورها، فروش ماه/سود، ارزش موجودی، کمبود، انقضا ۵ بازه، مطالبات، پرفروش‌ها، روند ۷ روز (نمودار ستونی)، آخرین فاکتورها، وضعیت سامانه/پیامک/حسابداری | ✅ `/reports/dashboard` |
| 2 | صندوق `pos` | viewport ثابت؛ جست‌وجوی محلی+سرور؛ اسکن؛ FEFO/انتخاب بچ؛ مقدار اعشاری؛ تخفیف ردیف/فاکتور؛ کوپن (`/marketing/coupons/validate`)؛ مشتری آزاد/ثبت‌شده؛ نگه‌داشتن ≤۱۰؛ پرداخت نقدی/کارت/کارت‌به‌کارت/نسیه + باقی‌مانده؛ ثبت محلی `M-…` و ارسال؛ **بدون سود** | ✅ قرارداد/آزمون؛ ⚠️ UI |
| 3 | نگه‌داشته‌ها `held` | فهرست با زمان/مشتری/مبلغ، بازگردانی با ضربه، حذف با نگه‌داشتن | ✅ کد |
| 4 | فاکتورها `invoices` | رایانه + گوشی؛ جزئیات؛ رسید متنی؛ چاپ روی رایانه؛ ابطال (رمز مدیر)؛ مرجوعی هر قلم | ✅ endpointها |
| 5 | مشتریان `customers` | همه/بدهکاران؛ ثبت (آفلاین هم)؛ پرونده؛ دفتر حساب؛ تسویهٔ جزئی/کامل؛ اصلاح دستی؛ پیامک یادآوری؛ ویرایش (PATCH override) | ✅ endpointها |
| 6 | جشنواره و کوپن `marketing` | جشنواره (درصدی/ثابت، حداقل، سقف، انقضا، صدور خودکار)، توقف/فعال؛ کوپن (کد/مشتری/دفعات)، مسدود؛ آمار | ✅ |
| 7 | کالاها `products` | جست‌وجو/اسکن؛ تعریف (آفلاین، INT-)؛ جزئیات؛ بچ‌های فعال FEFO؛ تاریخچهٔ قیمت؛ قیمت سریع؛ ویرایش/حذف؛ واحدها؛ دسته/برند؛ بازیابی بارکد | ✅ |
| 8 | ورود کالا `receive` | اسکن ← کالا؛ پرکردن آخرین قیمت‌ها؛ اعتبارسنجی واحد اعشاری؛ انقضای جلالی؛ بچ محلی + `STOCK_RECEIVE` | ✅ |
| 9 | انبار `inventory` | همه/کمبود/انقضا (۵ بازه) از SQLite و `/reports/expiry` | ✅ |
| 10 | انبارگردانی `stocktake` | ایجاد (زمان‌بندی)، شروع، پیشرفت، اسکن‌وشمارش (`item-by-barcode`)، ردیف بعدی، شمارش آفلاین در صف، پایان، اختلاف، تأیید، لغو | ✅ endpointها با curl |
| 11 | ضایعات/اصلاح/انتقال `stockops` | انتخاب بچ با اسکن؛ WASTE/ADJUSTMENT/TRANSFER با دلیل | ✅ |
| 12 | انبارها `warehouses` | فهرست، قفسه، ایجاد | ✅ |
| 13 | گردش موجودی `movements` | IN/SALE_OUT/RETURN_IN/WASTE/ADJUSTMENT/TRANSFER | ✅ |
| 14 | گزارش‌ها `reports` | ۱۰ گزارش با بازهٔ ۱/۷/۳۰/۹۰ روز | ✅ (سود فقط با `pricing.view_cost`) |
| 15 | حسابداری `accounting` | نمای کلی، صندوق روز، هزینه، تأمین‌کننده، چک، دفتر روزنامه/معکوس، تراز، سود و زیان، ترازنامه، حساب‌ها | ✅ |
| 16 | کاربران `users` | ایجاد/ویرایش/رمز/نقش/غیرفعال | ✅ |
| 17 | لاگ حسابرسی `audit` | فیلتر اکشن، قبل/بعد | ✅ |
| 18 | تنظیمات `settings` | ۲۱ دسته، ویرایش هر کلید، پوسته، زمان تهران، بروزرسانی | ✅ |
| 19 | مشخصات فروشگاه `store` | ۹ فیلد + لوگو | ✅ |
| 20 | پیامک `sms` | صف/تلاش مجدد/تست/گزارش روزانه، الگوها، ارسال دستی | ✅ |
| 21 | سخت‌افزار `hardware` | وضعیت، تست چاپ/کشو، کشف اسکنر، ثبت دستگاه، تست دوربین گوشی | ✅ (سخت‌افزار فیزیکی ⚠️) |
| 22 | تست اتصالات `diagnostics` | اجرای کامل/بدون اینترنت، تاریخچه، صف کارها، پینگ | ✅ `/diagnostics/run` اجرا شد (12 checks) |
| 23 | پشتیبانی `support` | تیکت (نوع/اولویت)، گفت‌وگو دوطرفه، پیوست عکس (multipart)، بستن، آفلاین در صف | ✅ endpointها |
| 24 | لایسنس `license` | وضعیت/انقضا/HWID/بررسی مجدد | ✅ |
| 25 | همگام‌سازی `sync` | وضعیت، صف، تعارض‌ها، دستگاه‌ها، دریافت کامل | ✅ |
| 26 | ابر `cloud` | وضعیت/همگام‌سازی اکنون | ✅ |
| 27 | دستگاه `device` / درباره `about` | آدرس، جفت‌سازی دوباره، پوسته، راهنماها، پاک‌کردن داده؛ نسخه، «طراحی و توسعه توسط خواجوی» | ✅ |

## راهنمای گام‌به‌گام
`Tour.java`: ۲۷ کلید، خودکار در اولین بازدید هر بخش (`tour_seen_<key>`)، دکمهٔ «?» همیشه در هدر، بازنشانی همه از تنظیمات دستگاه. ✅ آزمون وجود کلیدها؛ ⚠️ نمایش روی دستگاه.

## نصب و آزمایش روی گوشی (برای کاربر)
1. `releases/android/SupermarketMobile-2.0.0.apk` (sha256 در فایل کنارش) را روی گوشی (Android 7+) نصب کنید.
2. در ویندوز: تنظیمات → موبایل (اندروید) → QR. در گوشی: «اسکن QR رایانه». (گوشی و رایانه روی یک وای‌فای.)
3. یا «شروع مستقل» و بعداً از «تنظیمات دستگاه → جفت‌سازی دوباره».
4. **نکتهٔ امضا:** کلید امضا در هر سندباکس دوباره ساخته می‌شود؛ برای به‌روزرسانی روی نسخهٔ ۱.۹ نصب‌شده، ابتدا آن را حذف کنید (کلید متفاوت است).

---

## v4.0 — مغز فروشگاه روی اندروید (وضعیت: **ساخته‌شده — APK واقعی 4.0.0**)

نسخهٔ 4.0.0 اندروید با همان خط لولهٔ بدون-Gradle ساخته و با کلید امضای قبلی **(SHA-256 گواهی: `18b4ab1868c026476c4027fe823d38a8e3965f70194cc0d65e60eac4e7321fa5` — یکسان با 3.8.0)** امضا شد؛ یعنی روی نسخهٔ نصب‌شدهٔ قبلی به‌صورت به‌روزرسانی نصب می‌شود (versionCode 40000 > 30800). فایل: `releases/android/SupermarketMobile-4.0.0.apk` (+ `.sha256`).

### الف) صفحهٔ «مغز فروشگاه» (فقط مدیر) — ✅ پیاده‌سازی شد
- همان قرارداد API دسکتاپ: `/api/brain/{status,decisions,decisions/{id},followups,chat,…}`.
- ورودی منو فقط با `settings.manage` (`Screens.PERMS`)؛ سرور هم برای صندوق‌دار ۴۰۳ می‌دهد.
- کارت‌های تصمیم با بررسی / اجرا / بعداً / رد + «اندازه‌گیری نتیجه»؛ گزینه‌ها، دلیل، حکم سیاست و قرارداد اندازه‌گیری در برگهٔ «بررسی».
- گفت‌وگوی مدیریت: تاریخچه + پرسش؛ متن پاسخ فقط (بدون JSON/ابزار/استدلال) + هشدارهای صادقانه (مدل نبود، عدد حذف‌شده).
- پیگیری‌ها با «ثبت نتیجه».

### ب) مدل محلی — ✅ دانلودِ پس از نصب، به‌صورت فایل داده
- APK کوچک می‌ماند (۱٫۳ مگابایت)؛ مدل **بعد از نصب** دانلود می‌شود — دقیقاً همان چیزی که مالک خواست.
- فقط منبع رسمی Qwen + هش SHA-256 پین‌شده (همان مقادیر `model_registry.py`).
- دریافتِ قابل‌ادامه (Range) با توقف/ادامه، تأیید هش کامل روی خود گوشی، حذف فایل خراب، ذخیره در حافظهٔ خصوصی برنامه (حذف برنامه = حذف مدل).
- **صادقانه:** موتور استنتاج روی خود گوشی در این نسخه فعال نیست؛ فایل مدل تأییدشده نگه داشته می‌شود و این محدودیت در همین صفحه نوشته شده (پنهان نمی‌شود). پاسخ‌ها از مغز رایانه می‌آید.

### ج) آفلاین — ✅ صف + تعارض ثبت‌شده
- تأیید/رد/ثبتِ نتیجه در حالت آفلاین در صف محلی می‌ماند («در انتظار همگام‌سازی») و با بازگشت اتصال دوباره ارسال می‌شود.
- اگر سرور اجرای صف را رد کند (مثلاً تصمیم در جای دیگری بسته شده)، آن رد شدن به‌عنوان **تعارض** نمایش داده می‌شود — هرگز بی‌صدا حذف یا بازنویسی نمی‌شود.

### موارد باقی‌مانده (صادقانه)
- استنتاج روی خود گوشی (llama.cpp بومی/NDK) و تست روی شبیه‌ساز/گوشی فیزیکی — در این محیط ممکن نیست (نه شبیه‌ساز هست نه SDK رسمی اندروید). نصب روی گوشی واقعی و دیدن «مغز فروشگاه» در منو، آزمون نهایی این بخش است.

## v4.1 — موتور استنتاج روی خود گوشی + «یک مدل، یک سیستم» (وضعیت: **ساخته‌شده — APK واقعی 4.1.0**)

درخواست مالک (۲۰۲۶-۰۹-۲۳): مدل روی اندروید **فعال** باشد؛ وقتی گوشی با رایانه جفت است «یک سیستم» باشند نه دو مغز جدا؛ و مدل به‌شکل کامل روی اندروید باشد. پاسخ:

### الف) موتور واقعی روی گوشی — ✅ llama.cpp داخل APK
- `llama-server` (خانوادهٔ llama.cpp، همان تگ `b6283` که نصب‌کنندهٔ ویندوز هم داخل خودش می‌گذارد) از روی سورس رسمی برای arm64 کامپایل شد — **بدون NDK**: با cross-compiler رسمی zig (پکیج `ziglang` از PyPI) به‌صورت باینری کاملاً static با libc مستقل؛ اندروید آن را از `nativeLibraryDir` اجرا می‌کند (`lib/arm64-v8a/libllamaserver.so`، ~۷ مگابایت، APK فقط ۵٫۳ مگابایت).
- معماری همان رایانه: زیرفرآیند + HTTP روی `127.0.0.1:8081` — پرت دقیق کلاس `LlamaCppProvider` در `runtime.py`: همان پرچم‌های اجرا (`-m/--host/--port/-c/-t/--no-webui`)، همان `/health`، همان بدنهٔ `/v1/chat/completions`، همان preset «fast» (temperature 0.7, top_p 0.8, top_k 20)، همان حذف `<think>`.
- ساخت مجدد: `scripts/android/build-engine.sh` (سورس پین‌شده با sha256؛ بدون نیاز به dl.google.com). تست پاریتی (`test_v41_android_parity.py`) تگ موتور را با نصب‌کنندهٔ ویندوز برابر قفل می‌کند.

### ب) یک مدل، یک سیستم — ✅ قفل‌شده با تست
- فایل مدل روی گوشی **بایت‌به‌بایت همان فایل رایانه** است: همان شناسه، همان URLها (Hugging Face + منبع رسمی جایگزین ModelScope — جدید در v4.1 برای اندروید هم)، همان sha256، همان حداقل رم — تست پاریتی هر کدام را با `model_registry.py` مقایسه می‌کند؛ واگرایی = شکست تست.
- **جفت با رایانه** (رایانه در دسترس): مغزِ رایانه جواب می‌دهد — کل خط لولهٔ قطعی روی دادهٔ زندهٔ فروشگاه. موتور محلی برای پاسخ استفاده نمی‌شود تا دو مغز از هم جدا نشوند.
- **مستقل** (بدون رایانه): گفت‌وگو با همان فایل مدل روی خود گوشی جواب می‌دهد — با برچسب صریح «پاسخ از مدل محلی روی خود گوشی — بدون دادهٔ زندهٔ فروشگاه»؛ پرامپت سیستم همان قواعد محصول است (عدد از خود نساز / وضعیت-دلیل-پیشنهاد-اقدام بعدی) + اعتراف صادقانه به نبود دادهٔ زنده.

### ج) دانلود مدل — ✅ مثل قبل، با منبع جایگزین
- مدل همچنان **بعد از نصب** دانلود می‌شود (خواستهٔ قبلی مالک)؛ OBB لازم نیست — OOB سازوکار پلی‌استور برای بازی‌هاست، برای نصب دستی هیچ مزیت ندارد و حریم خصوصی ضعیف‌تری دارد؛ حافظهٔ خصوصی برنامه همان تجربه را می‌دهد (دانلود پس از نصب) امن‌تر.
- جدید: اگر Hugging Face پاسخ ندهد، همان فایل (همان sha256) از ModelScope گرفته می‌شود — همان رفتاری که دانلود منیجر ویندوز در v4.0.1 گرفت.

### تأیید واقعی در همین محیط
- بیلد دوقلوی x86_64 با **همان سورس، همان فلگ‌ها، همان کامپایلر**: `--version` اجرا شد (`version: 6283 (b6283)`)، یک GGUF معتبر Qwen2 (تولیدشده با پکیج رسمی gguf) بارگذاری شد و `/v1/chat/completions` با همان بدنهٔ جاوا **واقعاً توکن تولید کرد** (۲۴ توکن). باینری arm64: ELF استاتیک aarch64 (بدون PT_INTERP/PT_DYNAMIC) تأیید شد.
- **صادقانه:** اجرای واقعی روی گوشی فیزیکی (ARM) اینجا ممکن نیست — نسخهٔ نهایی روی گوشی خودتان: نصب 4.1.0 روی نسخهٔ قبلی، دانلود مدل از تب «مدل محلی»، «روشن‌کردن و آزمایش موتور محلی»، بعد قطع وای‌فای رایانه و پرسیدن یک سؤال در گفت‌وگو (باید برچسب «پاسخ از مدل محلی» بیاید).
## v4.1.1 — رفع نصب‌نشدن (۲۰۲۶-۰۹-۲۳)

گزارش مالک: 4.1.0 روی گوشی نصب نمی‌شد. علت فایل: APK فقط arm64-v8a داشت و گوشی ۳۲ بیتی کل بسته را رد می‌کند.
رفع: موتور برای **هر دو ABI** ساخته و بسته‌بندی می‌شود (arm64-v8a + armeabi-v7a استاتیک با NEON)؛ پیش‌پرواز
نصب جدید (`scripts/android/verify-apk.py`) همان چیزهایی را چک می‌کند که PackageManager چک می‌کند (تراز
resources.arsc برای اندروید ۱۱+، ABIها، CRC) و بیلد بدون PASS تمام نمی‌شود. اگر باز هم نصب نشد، به این
ترتیب بررسی کنید: (۱) نسخهٔ قبلی با کلید متفاوت نصب است؟ اول حذفش کنید؛ (۲) Play Protect ← «جزئیات» ←
«نصب به هر حال»؛ (۳) sha256 فایل را با `releases/android/SupermarketMobile-4.1.1.apk.sha256` مقایسه کنید؛
(۴) اندروید ۷ به بالا لازم است.
