# چک‌لیست کامل برنامهٔ اندروید — v1.9.0

**قاعدهٔ صداقت (§57):** ✅ = کد + اجرا + آزمون + نتیجهٔ مورد انتظار (در سندباکس، ویوپورت 390×844، پل اندروید شبیه‌سازی‌شده یا آزمون JVM).
⚠️ NOT VERIFIED = فقط روی دستگاه فیزیکی قابل تأیید است (در این محیط گوشی/دوربین واقعی وجود ندارد).

آزمون‌های مرجع: `scripts/uitest/phone-full-panel.js` (۳۲/۳۲ ✅)، `scripts/uitest/smoke-android.js` (✅)، `backend/tests` (325 passed)، `docs/screenshots/v19_phone_*.png`.

## معماری (چرا «هیچ بخشی کم نیست»)
گوشی جفت‌شده **همان پنل ویندوز** (`index.html + app.js + accounting.js`) را از داخل APK اجرا می‌کند و به API رایانهٔ فروشگاه وصل می‌شود. هیچ کپی/بازنویسی صفحه‌ای وجود ندارد؛ پس هر قابلیت ویندوز به‌طور تعریفی در اندروید هست. `mobile-shell.css/js` فقط چیدمان را برای انگشت بازطراحی می‌کند.

## ۱) بخش‌ها (۱۶/۱۶) — همه در گوشی رندر شدند، بدون خطا، بدون سرریز افقی
| # | بخش | وضعیت | شاهد |
|---|---|---|---|
| 1 | داشبورد (۱۲ بلوک) | ✅ | `v19_phone_dashboard.png` |
| 2 | صندوق (POS): اسکن، پیشنهاد، سبد، تخفیف، مشتری، کوپن، نگه‌داشتن/بازگردانی، پرداخت نقدی/کارت/ترکیبی/نسیه، قفل | ✅ اسکن بومی←سبد، hold←restore، پرداخت←`INV-20260909-000001` | `v19_phone_pos_cart.png`, `v19_phone_pay_sheet.png` |
| 3 | کالاها: ثبت/ویرایش/بازیابی خودکار/بانک اولیه/CSV/تاریخچهٔ قیمت | ✅ ثبت از گوشی ← همان لحظه در API رایانه | `v19_phone_products.png` |
| 4 | ورود کالا (بچ/قیمت/انقضا) | ✅ رسید ۷ عدد از گوشی ← موجودی رایانه = ۷ | — |
| 5 | انبار و انبارگردانی (ضایعات/اصلاح/انتقال/انبارها/گردش/شمارش قابل ازسرگیری) | ✅ رندر | — |
| 6 | فاکتورها (ابطال/مرجوعی/چاپ مجدد) | ✅ رندر | — |
| 7 | مشتریان و دفتر حساب (تسویهٔ جزئی/کامل، پیامک یادآوری) | ✅ ایجاد مشتری از گوشی ← در رایانه | — |
| 8 | جشنواره و کوپن | ✅ رندر | — |
| 9 | گزارش‌ها | ✅ | `v19_phone_reports.png` |
| 10 | حسابداری (صندوق/هزینه/چک/دفتر) | ✅ | `v19_phone_accounting.png` |
| 11 | سخت‌افزار (چاپگر/کشو/بارکدخوان) | ✅ رندر (سخت‌افزار فیزیکی روی گوشی ⚠️) | — |
| 12 | کاربران و نقش‌ها | ✅ | — |
| 13 | تنظیمات (~۲۱ دسته، پوسته، زمان تهران، بروزرسانی، ابر، جفت‌سازی) | ✅ | `v19_phone_settings.png` |
| 14 | تست اتصالات | ✅ | — |
| 15 | درخواست پشتیبانی (گفتگوی دوطرفه، پیوست، نشان پاسخ) | ✅ رندر | — |
| 16 | لاگ حسابرسی | ✅ | — |

## ۲) UI/UX موبایل
| قابلیت | وضعیت |
|---|---|
| نوار پنج‌تبی پایین (خانه/فروش/کالاها/انبار/بیشتر) با نشانگر فعال | ✅ |
| کشوی «همهٔ بخش‌ها» گروه‌بندی‌شده + نام فروشگاه/کاربر + وضعیت اتصال | ✅ `v19_phone_drawer.png` |
| نوار عنوان چسبان با ☰ و دکمهٔ «راهنما» | ✅ |
| جدول‌ها → کارت‌های برچسب‌دار (بدون اسکرول افقی) | ✅ (۱۶ بخش، `scrollWidth ≤ 390`) |
| مودال‌ها → bottom-sheet با دستگیره و انیمیشن | ✅ |
| صندوق تک‌ستونه: اسکن ← سبد ← مشتری/کوپن ← جمع ← پرداخت بزرگ | ✅ |
| دکمهٔ شناور اسکنر بومی در همهٔ صفحات (به فیلد بارکد همان صفحه می‌رود) | ✅ |
| اهداف لمسی ≥ 42px، فونت ورودی 16px (بدون زوم ناخواسته) | ✅ CSS |
| پوستهٔ تیره/روشن/خودکار مشترک با ویندوز | ✅ |
| راهنمای گام‌به‌گام (خودکار بار اول + دکمه) با اشاره به نوار پایین | ✅ |
| دکمهٔ Back اندروید: کشو/شیت ← خانه ← تأیید خروج | ✅ (`__androidBack` + Java) |
| زیبایی روی صفحهٔ واقعی (رندر فونت/رنگ‌ها) | ⚠️ فقط اسکرین‌شات headless |

## ۳) اتصال و همگام‌سازی
| قابلیت | وضعیت |
|---|---|
| باز شدن برنامه ← ورود خودکار با توکن جفت‌سازی (بدون لاگین/بارگذاری مجدد) | ✅ |
| همگام لحظه‌ای دوطرفه وقتی متصل است (یک پایگاه‌داده): کالا گوشی→رایانه، کالا رایانه→گوشی، رسید، فاکتور، مشتری | ✅ ۵ سناریو |
| Probe `/health` هر ۱۵ ثانیه، نمایش وضعیت در کشو | ✅ |
| رایانه در دسترس نیست ← گذار خودکار به «حالت سریع گوشی» (local-first v1.8.1) | ✅ منطق؛ ⚠️ روی دستگاه |
| حالت سریع/آفلاین: فروش، ورود کالا، کالای جدید، مشتری روی IndexedDB + صف؛ بازپخش با اولین اتصال LAN/Drive | ✅ (v1.8.1 E2E: `M-000001 → INV`) |
| حالت مستقل بدون رایانه («شروع مستقل») | ✅ |
| جفت‌سازی با QR (اسکنر بومی) یا کد دستی | ✅ منطق؛ ⚠️ دوربین |
| Google Drive اختیاری برای خارج از شبکه | ✅ (v1.7) |

## ۴) بومی اندروید
| قابلیت | وضعیت |
|---|---|
| APK امضاشده `releases/android/SupermarketMobile-1.9.0.apk` (versionCode 10900) با پنل کامل + `/mobile/` داخل assets | ✅ |
| اسکنر بومی Camera2 + ZXing (رمزگشایی JVM ~3ms/فریم) | ✅ منطق؛ ⚠️ دوربین فیزیکی |
| نصب روی گوشی واقعی | ⚠️ NOT VERIFIED |
| مجوزها: دوربین، موقعیت (پشتیبانی)، لرزش | ✅ Manifest |

## نکات صادقانه
- قابلیت‌های وابسته به سخت‌افزار رایانه (چاپگر ESC/POS، کشوی پول، بارکدخوان USB) از گوشی **فرمان داده می‌شوند** و روی رایانه اجرا می‌شوند؛ چاپ مستقیم از گوشی به چاپگر بلوتوث در این نسخه نیست.
- هر آنچه با ⚠️ علامت خورده نیاز به آزمون روی دستگاه فیزیکی دارد.
