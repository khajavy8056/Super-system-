# Release Audit — v4.4.0 (2026-09-24)

**موضوع دوره:** قدرت‌های مدیر — چتِ انسانی، دسترسی مغز به همه‌جای برنامه، یادآوری با ارتقای پیامکی، دکمه‌های تأیید شیشه‌ای در چت، و ریشهٔ باگ «آماده است ولی می‌گوید مدل تأییدشده نیست».

## 1. خواسته‌های مدیر ← تحویل‌شده

| # | خواسته (عین اظهار مدیر) | وضعیت | کجا |
|---|---|---|---|
| ۱ | دانلود مدل کامل می‌شود ولی «هنوز مدل تایید شده روی گوشی نیست» | ✅ ریشه‌ای | `BrainModel.stateOf()` — `optJSONObject(id).optString("state","")` |
| ۲ | بعد از دانلود، کارت‌های دانلود دیگر نمایش داده نشوند | ✅ | `BrainScreens.modelTab()` — یک کارت «نصب و تأیید شده» + return |
| ۳ | «سلام» جواب سلام بگیرد؛ مدل اول یک مدل زبانی است | ✅ | `_smalltalk_answer` (planner) + `SYSTEM_PROMPT` بازنویسی‌شده |
| ۴ | مغز به همهٔ بخش‌های برنامه دسترسی داشته باشد (تنظیم‌ها، متن پیامک…) | ✅ | ابزارهای `get_setting`/`set_setting`/`sms_draft` + لیست‌سفید |
| ۵ | یادآوری → نوتیف/پاپ‌آپ → «دوباره یادآوری کن» → چند ساعت بعد پیامک مدیر → بی‌پاسخ → دوباره پیامک | ✅ | `ReminderRx` + `followups.notify_due/snooze/acknowledge` + ورکر ۱۵ دقیقه‌ای |
| ۶ | زیر پیامِ چتِ نیازمند تأیید، دو دکمهٔ شیشه‌ای «تایید/ویرایش» | ✅ (+ «رد») | `BrainScreens.approvalButtons()` — «تأیید» = اجرا |
| ۷ | مدل همیشه در حال تحلیل فروشگاه باشد | ✅ | `_start_brain_worker()` در main.py — پاس پرواکتیو هر ۱۵ دقیقه |

## 2. ریشه‌یابی باگ آماده-ولی-تأییدنشده

`Prefs` برای هر مدل یک **JSONObject** در `brain_model_state` نگه می‌دارد: `{"<id>": {"state":"READY","done":N,"note":"…","at":T}}`. تلهٔ org.json: `optString()` روی مقدارِ object بی‌صدا fallback برمی‌گرداند — پس `ready()`/`readySpec()`/`autoSetup` همیشه «» می‌دیدند؛ در حالی که بِج (از listener زندهٔ دانلود) درست می‌گفت آماده. نویسنده (`putState`) از همان ابتدا درست می‌نوشت؛ **خواننده** اصلاح شد. تست رگرسیون: `test_ready_bug_stays_dead` (هم قالب درست، هم ممنوعیت الگوی قدیمی).

## 3. صحت‌سنجی

- **مجموعهٔ کامل: ۷۸۸ passed / 1 skipped** (رکورد؛ ۴.۳.۳ → ۷۷۱/۱).
- جدید: `tests/test_v44_owner_powers.py` — ۱۶ تست.
- به‌روزرسانی تست‌های پین‌شده: شمارش ابزارها (۳۷→۴۰)، مجموعهٔ ابزارهای نوشتاری (+`set_setting`/`sms_draft`)، هدِ مهاجرت (`b7e9f1a3c5d8`)، تست downgrade گام-یک (ستون `next_sms_at`)، تست نسخه → version-agnostic.
- مهاجرت: `b7e9f1a3c5d8` — idempotent، downgrade امن، PRAGMA با ایندکس ستون درست (`r[1]`).
- باگ‌های یافت‌شده و رفع‌شده حین تست: import ماژول اشتباه (`models.setting`→`models`)، امضای `queue_sms` (keyword-only)، `full_name`→`name` روی Customer، اتصال db در `_write` (lambda binding).

## 4. APK

| فیلد | مقدار |
|---|---|
| فایل | `releases/android/SupermarketMobile-4.4.0.apk` |
| حجم | ۹٬۶۸۰٬۴۷۹ بایت (~۹.۳M) |
| versionCode / Name | **40400 / 4.4.0** (aapt2 badging تأیید شد) |
| ABIها | arm64-v8a + armeabi-v7a (موتور llama.cpp هر دو، STATIC) |
| گواهی | CN=Khajavy Supermarket — SHA-256 `18b4ab18…21fa5` (همان گواهی → به‌روزرسانی درجا) |
| preflight | PASS (ساختار نصب‌مسدودکننده سالم) |
| sha256 | `3d11c35c2131f41bfa0711698993133897d06b0d16219bca4419fec0592ab518` |
| idsig | `SupermarketMobile-4.4.0.apk.idsig` کنار APK |

## 5. ویندوز

تغییر این دوره فقط نسخه + بک‌اند است (موتور دست‌نخورده). نصب‌کننده یک‌بار از همین درخت ساخته شود (`setup.iss` → 4.4.0). مهاجرت هنگام اولین اجرا خودکار اجرا می‌شود (`alembic upgrade head` در بوت، idempotent).

## 6. نکات اجرا

- شمارهٔ مدیر برای پیامکِ ارتقا: تنظیم `brain.manager_phone`؛ فاصلهٔ تکرار: `brain.reminder_escalate_hours` (پیش‌فرض ۳).
- kill-switch ورکر تحلیل: `SUPERMARKET_BRAIN_WORKER=0`.
- «ویرایش» در دکمه‌های چت، تغییر خواسته‌شده را به‌صورت پیام جدید به مغز می‌فرستد («همان کار را با این تغییر انجام بده: …»).
