# 🛒 Supermarket System — سیستم مدیریت سوپرمارکت

**Supermarket ERP / Smart Inventory / POS** — یک نرم‌افزار واقعی، مستقل و قابل نصب روی Windows برای مدیریت کامل چرخه عملیاتی فروشگاه: کالا، Batch، موجودی، قیمت قدیم/جدید، انقضا، انبارگردانی، صندوق فروشگاهی (POS)، سود واقعی، پرینتر حرارتی، پیامک و کار آفلاین.

این پروژه پیاده‌سازی سند «Master Blueprint» است (فایل `نقشه_و_بلوپرینت.txt`). اصل بنیادی:

> سیستم نباید چیزی را که از نظر فیزیکی نمی‌تواند تشخیص دهد، به‌عنوان واقعیت قطعی ثبت کند.

بنابراین تفکیک دقیقی بین **موجودی سیستمی / فیزیکی**، **Batch**، **قیمت خرید تاریخی**، **قیمت فروش با تاریخچه**، **تاریخ انقضا** و **فروش** برقرار شده است.

---

## ✨ امکانات (نسخهٔ 1.9.0 — وضعیت تک‌به‌تک ۳۵۰ قابلیت: [`docs/FEATURE_CHECKLIST_350.md`](docs/FEATURE_CHECKLIST_350.md))

| حوزه | امکانات |
|---|---|
| **کالا** | ثبت با بارکد، دسته‌بندی، برند، واحد، حداقل موجودی |
| **Batch** | ورود کالا → Batch جدید (بدون بازنویسی)، قیمت خرید/مصرف‌کننده/فروش، تاریخ تولید/انقضا |
| **قیمت** | Price Version با تاریخچه کامل؛ قیمت قدیم/جدید هم‌زمان قابل فروش |
| **موجودی** | موجودی Product = Σ Batch؛ Stock Movement برای هر تغییر؛ Waste / Adjustment |
| **انبارگردانی** | Stocktaking + تطبیق فیزیکی/سیستمی + Audit |
| **انقضا** | موتور Expiry با سطل‌های قابل تنظیم؛ مسدودسازی فروش کالای منقضی |
| **POS** | اسکن بارکد، انتخاب Batch/قیمت، سود واقعی بر اساس Batch، Checkout تراکنشی، Void، Return |
| **صندوق** | FEFO/FIFO/Hybrid پیشنهاد Batch؛ فروش ترکیبی از چند Batch |
| **گزارش** | داشبورد، فروش، سود Batch، گردش کالا، قیمت قدیم، کم‌موجودی |
| **سخت‌افزار** | درایور ESC/POS واقعی (`tcp://`, USB, اسپولر ویندوز)، رسید فارسی cp1256 با لوگو، کشوی پول `ESC p`، تشخیص اسکنر — [`docs/HARDWARE.md`](docs/HARDWARE.md) |
| **پیامک** | ملی‌پیامک (REST رسمی، خط/الگو) و کاوه‌نگار؛ صف، Retry، فاکتور/بدهی/کوپن/هشدار انبار/گزارش مدیریت — [`docs/SMS.md`](docs/SMS.md) |
| **مشتری و بازاریابی** | مشتری آزاد/ثبت‌شده، حساب دفتری با تسویهٔ جزئی/کامل، جشنواره، کوپن شرطی، تخفیف سطری/فاکتور |
| **چند انبار** | انبارها، محل نگهداری، انتقال موجودی (`TRANSFER`) |
| **بانک کالا** | ۱۹۱ کالای رایج در ۱۳ دسته/۳۰ زیردسته با موجودی صفر + ورود CSV فروشگاه |
| **موبایل / اندروید** | **v2.1 «سوپری من»: اپ اندروید کاملاً بومی (Java، بدون WebView/PWA)** — ویزارد راه‌اندازی مثل ویندوز (با رایانه / فقط گوشی + لایسنس روی گوشی + لودینگ نصب یک‌باره)، جفت‌سازی با QR یا **کد ۶ رقمی**، پیدا کردن خودکار رایانه با «کلید اتصال» حتی پس از تغییر IP، قفل هم‌زمان با پایان لایسنس رایانه، پشتیبانی در همه حالت‌ها، تقویم شمسی برای همهٔ تاریخ‌ها. — `SupermarketMobile-<ver>.apk`، همهٔ ۲۷ بخش ویندوز با UI موبایلی اختصاصی، پایگاه‌دادهٔ SQLite روی گوشی، اسکن دوربین ZXing، جفت‌سازی با QR از تنظیمات ویندوز یا ورود با نام کاربری، حالت مستقل بدون رایانه، و همگام‌سازی خودکار دوطرفهٔ داده در LAN بدون اینترنت ([`docs/ANDROID_CHECKLIST.md`](docs/ANDROID_CHECKLIST.md)). پنل وب موبایل (`/m`) برای مرورگر همچنان در دسترس است |
| **به‌روزرسانی** | GitHub یا سرور داخلی، رمز مدیر، پشتیبان خودکار، SHA-256، Rollback — [`docs/UPDATE.md`](docs/UPDATE.md) |
| **امنیت** | JWT، نقش/مجوز گرانولار، Audit Log، تنظیمات رمزنگاری‌شده (بدون Hard-Code) |
| **آفلاین** | پایگاه‌داده محلی SQLite (WAL)، POS بدون اینترنت کار می‌کند |

---

## 🧱 معماری

```
Frontend (Web Panel)  →  REST API (FastAPI)  →  Application Services  →  Domain
        ↓                                          (کسب‌وکار)           (مدل‌ها)
   POS / Dashboard / Inventory                                            ↓
                                                                      Repositories
                                                                          ↓
                                                          Database (SQLite/PostgreSQL)
                                                                          ↓
                                                     External Services / Hardware
```

- **Product ≠ Batch** — یک کالا چند Batch دارد؛ هر Batch قیمت خرید/فروش و تاریخ انقضای خودش را دارد.
- **قیمت تاریخی هرگز بازنویسی نمی‌شود** — هر تغییر، Batch یا Price Version جدید می‌سازد.
- **FIFO/FEFO = سیاست تخصیص، نه واقعیت فیزیکی** — صندوق‌دار Batch واقعی را انتخاب می‌کند.
- **InvoiceItem Snapshot** — قیمت لحظه فروش در فاکتور ذخیره می‌شود و هرگز تغییر نمی‌کند.
- **Checkout اتمیک** — فاکتور + آیتم‌ها + کسر Batch + حرکت موجودی + پرداخت + سود، با هم Commit می‌شوند؛ شکست یکی = Rollback.
- **چاپ هرگز فروش را خراب نمی‌کند** — اگر پرینتر خطا داد، فاکتور PAID می‌ماند و `print_status=FAILED` ثبت می‌شود.

مستندات کامل معماری: [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md)

---

## 🚀 اجرای سریع (توسعه)

پیش‌نیاز: Python 3.11+

```bash
cd backend
python -m venv .venv
source .venv/bin/activate        # Windows: .venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env             # در صورت نیاز ویرایش کنید

python -m scripts.seed_demo      # داده نمونه (اختیاری)
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

سپس در مرورگر: **http://localhost:8000** (پنل وب) و **http://localhost:8000/docs** (Swagger).

**ورود پیش‌فرض (توسعه):** `admin` / `admin123` — بعد از اولین ورود عوض کنید.

### اجرای تست‌ها

```bash
cd backend
.venv/bin/python -m pytest tests/ -q
```

### مهاجرت‌های پایگاه‌داده (Alembic)

```bash
cd backend
.venv/bin/alembic upgrade head        # اعمال مهاجرت‌ها
.venv/bin/alembic revision --autogenerate -m "..."   # مهاجرت جدید
```

---

## 📁 ساختار مخزن

```
supermarket-system/
├── backend/                 # FastAPI + SQLAlchemy + Alembic
│   ├── app/
│   │   ├── models/          # 26 مدل دامنه (Product, Batch, Invoice, ...)
│   │   ├── services/        # منطق کسب‌وکار (POS, Inventory, Pricing, Expiry, ...)
│   │   ├── routers/         # اندپوینت‌های REST
│   │   ├── config.py        # تنظیمات از محیط (بدون Hard-Code)
│   │   ├── security.py      # JWT / bcrypt / مجوزها
│   │   └── main.py          # اپلیکیشن
│   ├── alembic/             # مهاجرت‌ها
│   ├── tests/               # تست‌های خودکار (21 تست)
│   └── scripts/seed_demo.py # داده نمونه
├── frontend/                # پنل وب (HTML/CSS/JS ساده — بدون وابستگی)
├── installer/               # اسکریپت‌های ساخت نصب‌کننده Windows
├── docs/                    # مستندات
├── .env.example
└── نقشه_و_بلوپرینت.txt      # سند اصلی معماری
```

---

## 🪟 نصب روی Windows

نسخه نهایی به‌صورت `Setup.exe` توزیع می‌شود که Runtime و وابستگی‌ها را خودش مدیریت می‌کند (کاربر نیازی به نصب Python ندارد). راهنما: [`docs/INSTALL.md`](docs/INSTALL.md) و [`installer/`](installer/).

---

## 🔗 API

خلاصه اندپوینت‌ها در [`docs/API.md`](docs/API.md) — مستندات تعاملی کامل در `/docs`.

---

## 📜 مجوز

MIT — به `LICENSE` مراجعه کنید.

## 🧭 نقشه راه

### نصب APK اندروید و هشدار Play Protect
1. فایل `SupermarketMobile-x.y.z.apk` را از GitHub Releases یا `releases/android/` دانلود کنید.
2. بار اول اندروید می‌گوید «این توسعه‌دهنده را نمی‌شناسد» — این پیام برای هر برنامهٔ خارج از Google Play عادی است؛ روی **جزئیات بیشتر → نصب به‌هرحال** بزنید. مجموع SHA-256 فایل را با `.sha256` کنار آن مقایسه کنید.
3. از 2.3.0 به بعد همهٔ نسخه‌ها با **یک کلید ثابت داخل مخزن** (`mobile-android/keystore/supery-release.jks`) امضا می‌شوند؛ نسخهٔ جدید مستقیم روی قبلی نصب می‌شود و داده‌ها می‌ماند. (برای ارتقا از 2.2 یا قدیمی‌تر یک‌بار حذف/نصب لازم است — قبلش پشتیبان بگیرید.)

- **v2.5.0 (فعلی)** — تصویر خودکار کالا با نام (ویندوز + اندروید، منابع بدون کلید، بندانگشتی در صندوق/انبارگردانی/جست‌وجو)، بارکد EAN-13 درون‌فروشگاهی معتبر برای همهٔ اقلام پایگاه اولیه، تمام‌صفحهٔ قطعی ویندوز. APK: `releases/android/SupermarketMobile-2.5.0.apk`.
- **v2.4.0 (اندروید)** — استقلال کامل اندروید از رایانه: API داخلی گوشی (`Local.java`) همهٔ بخش‌ها را از SQLite گوشی پاسخ می‌دهد (حسابداری کامل، انبارگردانی، انبارها، کاربران/نقش‌ها، حسابرسی، تنظیمات همهٔ دسته‌ها، تست اتصالات، ...)؛ پرسش شمارهٔ موبایل در پرداخت و ارسال پیامک فاکتور از خود گوشی. APK: `releases/android/SupermarketMobile-2.4.0.apk` (نصب مستقیم روی 2.3).
- **v2.3.0 (ویندوز + اندروید)** — پیامک فاکتور مستقل از چاپ + راهنمای داخلی ملی‌پیامک (هر دو سامانه)، راه‌اندازی اولیهٔ اندروید در پس‌زمینه با اعلان پیشرفت، ورود با اثر انگشت، پورت ثابت 8765 + کشف خودکار پس از تغییر IP/شبکه، اتصال آنلاین (LAN رایگان / Google Drive رایگان / رلهٔ خودمیزبان `relay/`)، ویندوز تمام‌صفحه بدون نوار عنوان. APK: `releases/android/SupermarketMobile-2.3.0.apk` (کلید امضای جدید — ارتقا از 2.2 یک‌بار حذف/نصب). Setup.exe: از CI ویندوز.
- **v2.2.0 (فقط اندروید)** — نصب به‌روزرسانی روی نسخهٔ قبل (کلید امضای ثابت)، اعلان‌های نوار بالا (پشتیبانی/انقضا/موجودی/همگام‌سازی) با صداهای ملایم، بازطراحی کامل UI/UX (منوی گروهی با آواتار، داشبورد کاشی‌ای، صندوق جدید، پوستهٔ روشن/تیرهٔ آرام). APK: `releases/android/SupermarketMobile-2.2.0.apk`. نسخهٔ ویندوز همان 2.1.0 است.
- **v2.1.0** — «سوپری من»: ویزارد راه‌اندازی اندروید (PC / فقط گوشی)، لایسنس روی گوشی و قفل با انقضای رایانه، کد ۶ رقمی + کلید اتصال/بیکن LAN (مقاوم به تغییر IP)، ورود Google داخل برنامه (PKCE loopback)، Drive از خود گوشی، انتخاب‌گر تاریخ شمسی، پشتیبانی بدون رایانه، رفع برش QR رایانه. APK: `releases/android/SupermarketMobile-2.1.0.apk`.
- **v2.0.0** — اپ اندروید کاملاً بومی (Java، بدون WebView)، ۲۷ بخش، SQLite، همگام دوطرفه. APK: `releases/android/SupermarketMobile-2.0.0.apk`.
- **v1.9.0** — اندروید = تمام ۱۶ بخش ویندوز با پوستهٔ موبایل (تب‌بار، کشو، کارت‌ها، bottom-sheet، اسکنر شناور)، اتصال خودکار با توکن جفت‌سازی، همگام لحظه‌ای دوطرفه (یک پایگاه‌داده)، عقب‌نشینی خودکار به حالت آفلاین گوشی. APK: `releases/android/SupermarketMobile-1.9.0.apk`.
- **v1.8.1** — اسکنر بومی اندروید (Camera2 + ZXing جاوا در `ScanActivity`؛ بدون WebView، سریع) ، **حالت مستقل گوشی**: برنامه بدون رایانه و بدون شبکه کار می‌کند (کالا، ورود کالا، فروش، مشتری روی IndexedDB) و با اولین اتصال LAN یا Drive خودکار همگام می‌شود (`PRODUCT_CREATE` + فروش با بارکد در `/mobile/sync`)، منوی پیشنهاد کالا در صندوق روی جمع‌ها شناور است. APK: `releases/android/SupermarketMobile-1.8.1.apk`.
- **v1.8.0** — گفتگوی دوطرفه با پشتیبانی از داخل ربات (Reply یا `TCK-xxxxxx@INSTALLID`، پیوست دوطرفه، نشان پاسخ جدید)، راهنمای گام‌به‌گام همهٔ بخش‌ها (خودکار در اولین بازدید + دکمهٔ «راهنما»)، اسکن سریع بارکد اندروید (BarcodeDetector/ZXing)، منطقهٔ زمانی پیش‌فرض تهران، صندوق بدون سود/بدون اسکرول با نوار فاکتورهای نگه‌داشته‌شده. APK: `releases/android/SupermarketMobile-1.8.0.apk`.
- **v1.7.0** — درخواست پشتیبانی داخل برنامه (ویندوز و گوشی، با موقعیت مکانی و صف ارسال)، همگام‌سازی اینترنتی اختیاری از راه Google Drive (پوشهٔ خصوصی برنامه، Device Flow)، برابری کامل امکانات در اپ اندروید (۲۴ صفحهٔ «بیشتر»)، QR جفت‌سازی در مرورگر، صفحهٔ اتصال گوشی پس از اولین ورود. مستندات: `docs/SUPPORT.md`, `docs/CLOUD_SYNC.md`.
- **v1.6.0** — نگه‌داشتن تا ۱۰ فاکتور در نوار پایین صندوق (F6)، دکمهٔ خروج با بکاپ، تمام‌صفحه/ریسپانسیو، جینگل خوش‌آمد و صداهای مجزا، لایسنس آفلاین تا تاریخ انقضا + ZIP رمزنگاری‌شده هنگام قفل، اپ اندروید APK با جفت‌سازی QR و همگام‌سازی آفلاین.
- **v1.5.1** — لودینگ ۴۵ دقیقه‌ای فقط داخل ویزارد (ورودهای بعدی ۲ دقیقه، بدون ذکر دقیقه)، افکت‌های صوتی رویدادها (تنظیمات → ظاهر → صداها)، پالت آرام‌تر و اصلاح خوانایی پوستهٔ روشن.
- **v1.5.0** — لایسنس آنلاین با بررسی ۲۴ساعته و قفل برنامه، ویزارد راه‌اندازی اولیهٔ ۱۰مرحله‌ای، لودینگ نصب (۴۵ دقیقه بار اول / ۲ دقیقه بعدی)، نوتیفیکیشن انقضا و لایسنس پایین صفحه (`docs/LICENSING.md`).
- **v1.4.2** — رفع خطای PyInstaller `not enough values to unpack (expected 3, got 2)` در app.spec (TOC صحیح برای فایل‌های pywebview).
- **v1.4.1** — رفع شکست ساخت نصب‌کننده وقتی pypi.org در دسترس نیست (pywebview اختیاری شد، retry/آینه/wheel آفلاین).
- **v1.4.0** — حسابداری دوطرفه کامل (دفتر روزنامه/کل، تراز، سود و زیان، ترازنامه، هزینه‌ها، چک، تأمین‌کننده، شیفت صندوق) با ثبت خودکار از فروش و خرید؛ داشبورد و صندوق با طراحی نئون/شیشه‌ای؛ شناسایی خودکار بارکدخوان USB.
- **v1.3.0** — تقویم شمسی در همهٔ فیلدهای تاریخ، طراحی جدید «ورود کالا» با پیشنهاد زنده (بدون قیمت تأمین‌کننده)، زمان‌بندی انبارگردانی + هشدار + ویزارد شمارش، تم حرفه‌ای، پنجرهٔ بومی WebView2 در ویندوز.
- **v1.2.6** — رفع شروع بی‌صدای برنامهٔ نصب‌شده (stdout=None در حالت بدون کنسول)؛ پیام خطای فارسی برای هر شکست سرور؛ تک‌نمونه.
- **v1.2.5** — خوددرمانی اسکیمای پایگاه داده در شروع (ستون‌های غایب از نسخه‌های قدیمی اضافه می‌شوند)؛ رفع کرش `units.allow_decimal`؛ پیام خطای خوانا در لانچر ویندوز.
- **v1.2.4** — رفع توقف مرحلهٔ PyInstaller در سازندهٔ نصب‌کننده (stderr زیر حالت Stop در PowerShell 5.1)؛ فقط کد خروج معیار است.
- **v1.2.3** — رفع ریشه‌ای تشخیص پایتون در سازندهٔ نصب‌کننده (باگ نقل‌قول PowerShell 5.1) + خودتشخیصی با گزارش دلیل رد هر مسیر.
- **v1.2.2** — سازندهٔ نصب‌کننده پایتون نصب‌شده را از رجیستری/py launcher/PATH/پوشه‌های استاندارد پیدا می‌کند و بی‌جهت دانلود نمی‌کند؛ نصب خودکار در مسیر ثابت و بدون نیاز به ریستارت.
- **v1.2.1** — رفع خطای پارس اسکریپت‌های ساخت نصب‌کنندهٔ ویندوز (BOM UTF-8) و جای اشتباه `MinVersion` در `setup.iss`.
- **v1.2.0** — همهٔ ۳۵۰ قابلیت فهرست پیاده‌سازی و با ۲۷۴ آزمون خودکار + اسموک UI تأیید شده‌اند؛ مواردی که فقط روی سخت‌افزار/ویندوز/اینترنت واقعی قابل اثبات‌اند در چک‌لیست با برچسب NOT VERIFIED مشخص شده‌اند (`CHANGELOG.md`).
- **بعدی** — همگام‌سازی چندشعبه با سرور مرکزی (§255) و دسترسی امن از اینترنت (§260) که در فهرست به‌عنوان «آینده» تعریف شده‌اند؛ hookهای آن (`sync_jobs`, JWT/CORS) آماده است.

مستندات: [`docs/DATABASE.md`](docs/DATABASE.md) · [`docs/RESOLVER.md`](docs/RESOLVER.md) · [`docs/DIAGRAMS.md`](docs/DIAGRAMS.md) · [`docs/SCREENSHOTS.pdf`](docs/SCREENSHOTS.pdf) · [`docs/INSTALL.md`](docs/INSTALL.md)

---

*این سیستم صرفاً یک «فاکتورزن + جدول موجودی» نیست؛ یک POS آگاه از موجودی (Inventory-Aware) است که ارتباط بین Product، Batch، قیمت خرید، قیمت فروش، انقضا، موجودی فیزیکی/سیستمی، فاکتور، سود و انبارگردانی را حفظ می‌کند.*
